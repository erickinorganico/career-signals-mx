---
phase: 01-official-sources-and-research-contract
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - contracts/research-v2.schema.json
  - contracts/research-v2-public.schema.json
  - brujula/research_contract.py
  - tests/test_research_contract.py
  - docs/CONTRACT-V2.md
  - pyproject.toml
autonomous: true
requirements: [CTR-01, CTR-02]
must_haves:
  truths:
    - "A strict separate v2 record validates distinct source, population, field, occupation, industry, geography, recorded sex, period, metric and method references at one unique grain."
    - "Every estimate has method/design/version, observed sample_size, weighted denominator, support, SE/CV/IC90, precision method and level, singleton policy, unit, price basis, status/reason and resolvable evidence."
    - "A strict internal v2 schema requires diagnostic estimate; a separate strict public v2 schema rejects estimate and validates the projected record."
    - "Suppressed or unsupported cells have public value null and reason; diagnostic estimate, weighted denominator, SE, CV, intervals and any nested value-equivalent weighted aggregate are absent or null in public projection; observed n and design support counts remain labeled and visible for coverage."
    - "V2 does not change v1 schema, quality behavior, synthetic fixture or tests; synthetic content remains marked synthetic."
    - "Edge CTR-01 boundary/adjacency/empty/ordering/precision has explicit duplicate-grain, empty-ref, adjacent-period, deterministic-order and nonfinite/rounding negative checks."
  artifacts:
    - path: contracts/research-v2.schema.json
      provides: "Strict v2 syntax and state conditions"
    - path: contracts/research-v2-public.schema.json
      provides: "Strict public v2 projection syntax and suppression conditions"
    - path: brujula/research_contract.py
      provides: "Semantic validation and public projection"
    - path: tests/test_research_contract.py
      provides: "Schema, semantic and suppression tests"
    - path: docs/CONTRACT-V2.md
      provides: "Published v2 interface and compatibility rules"
  key_links:
    - from: brujula/research_contract.py
      to: contracts/research-v2.schema.json
      via: "resources.contract_path and jsonschema Draft 2020-12"
    - from: brujula/research_contract.py
      to: contracts/research-v2-public.schema.json
      via: "resources.contract_path and public validation after projection"
    - from: brujula/research_contract.py
      to: brujula/populations.py
      via: "import POPULATION_DEFINITIONS and the two population ID constants as the only authoritative IDs"
    - from: brujula/research_contract.py
      to: docs/CONTRACT.md
      via: "v1 compatibility and separate v2 reference"
  prohibitions:
    - statement: "Do not present project singleton handling as official INEGI precision or automatically activate a cell because a diagnostic estimate exists."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase1_prohibitions_5.test.cjs
      check_violation_fixture: tests/fixtures/phase1_prohibitions/p5.bad.json
      check_clean_fixture: tests/fixtures/phase1_prohibitions/p5.clean.json
    - statement: "Do not silently turn unknown/null, synthetic or suppressed research cells into measured official claims."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase1_prohibitions_6.test.cjs
      check_violation_fixture: tests/fixtures/phase1_prohibitions/p6.bad.json
      check_clean_fixture: tests/fixtures/phase1_prohibitions/p6.clean.json
---

<objective>
Define and enforce the separate v2 official-research record and one safe public projection.

Purpose: Later estimators and publishers need an exact grain, resolvable provenance and suppression boundary without weakening v1.
Output: strict internal and public v2 schemas, semantic validators/projector, focused tests and interface documentation.
</objective>

<execution_context>
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@.planning/phases/01-official-sources-and-research-contract/01-CONTEXT.md
@.planning/phases/01-official-sources-and-research-contract/01-RESEARCH.md
@.planning/phases/01-official-sources-and-research-contract/01-PATTERNS.md
@.planning/phases/01-official-sources-and-research-contract/01-EDGE-PROBE.json
@contracts/dataset.schema.json
@brujula/quality.py
@brujula/populations.py
@.planning/phases/01-official-sources-and-research-contract/01-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish strict v2 record and semantic validation</name>
  <read_first>docs/CONTRACT.md; contracts/dataset.schema.json; brujula/data.py; brujula/quality.py; brujula/resources.py; brujula/populations.py; 01-RESEARCH.md Strict v2 grain proposal</read_first>
  <files>contracts/research-v2.schema.json, brujula/research_contract.py, tests/test_research_contract.py, pyproject.toml</files>
  <behavior>Reject extra/missing keys, duplicate grain, orphan refs, empty evidence, nonfinite values, status/value contradiction, blank IDs and unrecognized population/metric/method IDs. An internal record without `estimate` is invalid even when the estimate is null; the public schema will instead forbid the key. Accept exactly one record per source_snapshot_id × population_id × field_of_study_id × occupation_id × industry_id × geography_id × recorded_sex_id × period_id × metric_id × method_id, with explicit `all` or `unknown` aggregation IDs. Order of input records does not affect validation result.</behavior>
  <action>Create Draft 2020-12 internal `research-v2.schema.json` with `$id`, `schema_version="2.0"`, strict root/nested `additionalProperties:false`, catalog/reference arrays and record array. Define documented root keys `schema_version`, `sources`, `populations`, `fields_of_study`, `occupations`, `industries`, `geographies`, `recorded_sexes`, `periods`, `metrics`, `methods`, `evidence`, `records`; define record grain exactly as in must_haves plus `unit`, `price_basis`, `method_version`, `design_id`, `sample_size`, `weighted_denominator`, `support`, `precision`, `status`, `reason`, required nullable `estimate`, `value`, `evidence_refs`, `synthetic`. Precision carries standard_error, cv, ci90_lower/upper, method, level, singleton_policy and official_precision boolean, with null allowed where unavailable. Set `sample_size` to observed n only; require each method/design/version and evidence reference to resolve. Import `POPULATION_DEFINITIONS` from `brujula.populations` to check population IDs and semantics; do not duplicate definitions. Expose `validate_research_v2(payload: Mapping[str, object]) -> list[dict]`, returning deterministic check IDs/messages after jsonschema and semantic validation. Check duplicate IDs, orphan refs, unique grain, finite numerics, status/value combinations and source/period/method compatibility. Package both v2 schemas through existing `pyproject.toml` resource mapping; do not alter v1 schema or active v1 validator. Cover CTR-01 boundary, adjacency, empty, ordering and precision through tests.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_research_contract.py tests/test_quality.py tests/test_data.py -q</automated></verify>
  <acceptance_criteria>Strict v2 fixtures accept complete records and reject duplicate/orphan/extra/malformed or unsupported records; existing v1 tests remain green.</acceptance_criteria>
  <done>V2 can be validated independently of v1, with every research dimension and evidence link explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce public projection and document the contract</name>
  <read_first>docs/CONTRACT.md; 01-CONTEXT.md Contract and public values; .planning/research/ARCHITECTURE.md; tests/test_quality.py</read_first>
  <files>contracts/research-v2-public.schema.json, brujula/research_contract.py, tests/test_research_contract.py, docs/CONTRACT-V2.md</files>
  <behavior>Public projection of a supported MEASURED or REVIEW record contains gated value and evidence but no diagnostic estimate. A suppressed/UNKNOWN/BLOCKED record has value null plus reason and no numeric weighted_denominator, weighted support total, standard error, CV, interval endpoint, diagnostic estimate or nested copy of them. Retain observed sample_size and unweighted design support counts (UPM, strata, degrees of freedom) as coverage metadata; these are not weighted estimates. If `estimate` equals `weighted_denominator` or a nested weighted support total, neither may reveal the sentinel via the public payload. The strict public schema accepts this projection, rejects diagnostic keys/nonfinite numbers and numeric suppression leaks. Singleton policy cannot set official_precision or publication status by itself. Synthetic remains explicitly synthetic.</behavior>
  <action>Create separate Draft 2020-12 `research-v2-public.schema.json` with a distinct `$id`, required `schema_version="2.0"`, strict `additionalProperties:false` at root/record/nested support/precision levels, and the same referential grain. Public records must forbid `estimate` and other diagnostic-only keys. Expose `validate_public_research_v2(payload: Mapping[str, object]) -> list[dict]` with schema plus duplicate-grain/ref/finite-number/status semantic checks. Expose `public_research_projection(payload: Mapping[str, object]) -> dict`: require `validate_research_v2` success, construct every public object from an allowlist and require `validate_public_research_v2` success before return. For suppressed, REVIEW without an authorized value, UNKNOWN or BLOCKED rows, set `value=null` and `weighted_denominator=null`; retain observed `sample_size`, unweighted UPM/strata/design_df counts and fixed confidence level as explicit coverage/design metadata. Remove or null all weighted support totals and point-derived SE/CV/CI, and retain precision state/reason/method. Supported MEASURED and REVIEW rows may retain authorized `value` and safe support/precision; REVIEW alone is not suppression (singleton approximation or CV15–30 may have a supported value). Do not erase the n/support evidence that STAT-02 and ANA-05 require. Add a sentinel test with `estimate == weighted_denominator == nested support weighted total`, plus a separate hidden interval sentinel, that scans the entire serialized public payload and fails on either. Public-schema negatives must reject internal records, diagnostic keys, suppressed weighted support totals or SE/CV/CI, nonfinite values and orphan refs; public round-trip must validate. Document both schemas, internal versus public field tables, unique grain, `all`/`unknown`, imported named population definitions, denominator/sentinel meanings, singleton caveat, attribution/provenance, v1 compatibility and downstream consumer APIs in `docs/CONTRACT-V2.md`. Record that geography alias concept equivalence remains Phase 3 ANA-03 review and full person encoding/income-state numerical audit remains Phase 2 STAT-01/06, per resolved routing in `01-RESEARCH.md`; do not claim those facts established here. Keep v1 bytes unchanged and link from v2 to v1.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_research_contract.py tests/test_quality.py tests/test_data.py -q</automated></verify>
  <acceptance_criteria>Public output passes its own strict schema and semantic validator; suppressed diagnostic and value-equivalent weighted sentinels are absent throughout the serialized payload; negative public fixtures fail, v1 regression stays unchanged, and the v2 document specifies both interfaces.</acceptance_criteria>
  <done>Downstream consumers have a strict v2 input and safe public output contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Internal survey diagnostic records cross a validation and projection boundary before any public aggregate can be emitted.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Tampering | v2 record/reference | mitigate | Strict schema, resolvable refs, duplicate grain and nonfinite negative tests. |
| T-03-02 | Information disclosure | public projection | mitigate | Build from allowlist, null/omit suppressed weighted and support aggregates, then validate against strict public schema; sentinel tests prove estimate-equivalent totals and intervals absent. |
| T-03-03 | Spoofing | source/method/evidence | mitigate | Require source snapshot, method/design/version and resolvable evidence IDs. |
| T-03-04 | Repudiation | precision state | mitigate | Require reason, support and singleton policy; prevent policy flag from asserting official precision. |
| T-03-SC | Tampering | package install | accept | Existing jsonschema only; no new package install. |
</threat_model>

<verification>
Run focused v2 schema/projection tests and unchanged v1 data/quality regression; verify package resource lookup in checkout and installed wheel if the existing packaging test supports it.
</verification>

<success_criteria>
CTR-01 and its CTR-02 population/denominator references are explicit in a separate validated v2 contract; diagnostic suppression is fail closed.
</success_criteria>

<artifacts_created>
`validate_research_v2`, `validate_public_research_v2` and `public_research_projection` in `brujula/research_contract.py` are the stable downstream v2 interfaces; the internal and public JSON schemas are distinct packaged artifacts. Population IDs come only from `brujula.populations`; `brujula/research.py` remains free for later orchestration.
</artifacts_created>

<output>
Create `.planning/phases/01-official-sources-and-research-contract/01-03-SUMMARY.md` when done.
</output>
