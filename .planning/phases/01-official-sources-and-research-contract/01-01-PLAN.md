---
phase: 01-official-sources-and-research-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brujula/source_inventory.py
  - tests/test_source_inventory.py
  - docs/SOURCES.md
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04]
must_haves:
  truths:
    - "Exactly eight registry-approved 2024-Q3 through 2026-Q2 ZIPs resolve through current and their immutable attempt receipts, with no network access."
    - "An absent, failed, malformed, mismatched or concurrent current attempt blocks inventory; historical success never substitutes."
    - "Each quarter has one exact SDEM member, dictionary, study catalog and recorded revision status; an extra similarly named CSV cannot change selection."
    - "The 2024-Q3 four and 2024-Q4 nine corrections, 2025-Q3 geography header transition and per-quarter code mapping are inspectable."
    - "Inventory exposes only metadata, attribution, terms, acquired date and transformation notes; person records and ZIP bytes stay local."
    - "Edge SRC-01 adjacency/empty/ordering/idempotency/concurrency, SRC-02 idempotency/concurrency, SRC-03 adjacency/empty/encoding/ordering, and SRC-04 empty/encoding have explicit negative and determinism checks."
  artifacts:
    - path: brujula/source_inventory.py
      provides: "Offline exact-member inventory and metadata-only projection"
    - path: tests/test_source_inventory.py
      provides: "Synthetic member and custody controls plus eight-local-ZIP integration"
    - path: docs/SOURCES.md
      provides: "Eight-quarter package and licensing/provenance account"
  key_links:
    - from: brujula/source_inventory.py
      to: brujula/acquisition.py
      via: "resolve_snapshot(snapshot_id, output_root, registry_path)"
    - from: brujula/source_inventory.py
      to: data/catalog/enoe-snapshots.json
      via: "approved IDs, periods, URLs, SHA-256, terms and authority"
  prohibitions:
    - statement: "Do not represent an acquisition receipt or inventory metadata as approval to publish microdata or numerical findings."
      status: unverified
      flagged: true
    - statement: "Do not omit known 2024 corrections or silently treat an ENT/CVE_ENT spelling alias as proof of conceptual equivalence."
      status: unverified
      flagged: true
---

<objective>
Provide auditable metadata for all eight immutable official packages while retaining the existing acquisition authority and local-only person data boundary.

Purpose: Analysts need verified source custody and edition details before using any ENOE numbers.
Output: `brujula/source_inventory.py`, focused tests, and the eight-period source account in `docs/SOURCES.md`.
</objective>

<execution_context>
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@.planning/ROADMAP.md
@.planning/phases/01-official-sources-and-research-contract/01-CONTEXT.md
@.planning/phases/01-official-sources-and-research-contract/01-RESEARCH.md
@.planning/phases/01-official-sources-and-research-contract/01-PATTERNS.md
@.planning/phases/01-official-sources-and-research-contract/01-EDGE-PROBE.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Verify exact package members and custody</name>
  <read_first>brujula/acquisition.py; data/catalog/enoe-snapshots.json; tests/test_acquisition.py; 01-RESEARCH.md Source and Package Inventory</read_first>
  <files>brujula/source_inventory.py, tests/test_source_inventory.py</files>
  <behavior>Eight approved IDs in chronological registry order resolve offline through `resolve_snapshot`; unknown, missing, FAILED/RUNNING, mismatched current, missing immutable receipt or tampered raw ZIP fails. Duplicate or ambiguous canonical members, absent/empty dictionary/catalog, malformed metadata UTF-8 and duplicate headers fail. Repeated reads are byte-identical and do not create new attempts; concurrent/failing acquisition cannot be masked.</behavior>
  <action>Expose `inventory_snapshot(snapshot_id: str, output_root: Path, registry_path: Path | None = None) -> dict` and `inventory_all(output_root: Path, registry_path: Path | None = None) -> list[dict]`; the real local integration uses `Path('artifacts/enoe')`. Call the existing resolver for every ID; no downloader, network fallback or historical-success lookup. Derive exact period paths for SDEM, sibling dictionary and `catalogos/cs_p14_c.csv` from the approved registry period, compare full ZIP member names exactly and require uniqueness. Decode only metadata members and the SDEM header; stream the entire SDEM member as raw bytes through SHA-256 for its full member hash and byte count, without parsing or emitting person rows. Record complete path, full member hash/size, header and receipt run ID/hash/timestamps. Refuse duplicate member names and conflicting ZIP entries. Follow bounded ZIP safety already enforced by the resolver. Treat blank/null registry input and catalog with other than the approved eight as errors. Preserve deterministic period order and output across repeated calls; never write person rows or raw ZIP bytes to an output artifact. Cover edge probe SRC-01 adjacency, empty, ordering, idempotency, concurrency; SRC-02 idempotency/concurrency; SRC-03 adjacency, empty, encoding and ordering.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_acquisition.py tests/test_source_inventory.py -q</automated></verify>
  <acceptance_criteria>All eight exact approved packages can be inventoried offline; negative custody/member fixtures fail closed; repeated reads produce the same metadata and preserve attempt receipts.</acceptance_criteria>
  <done>Resolver-backed inventory returns eight metadata records in chronological order and no person observations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record revision, code, geography and provenance evidence</name>
  <read_first>01-RESEARCH.md Source and Package Inventory; docs/SOURCES.md; docs/research/ENOE-METHOD-REVIEW.md; data/catalog/enoe-snapshots.json</read_first>
  <files>brujula/source_inventory.py, tests/test_source_inventory.py, docs/SOURCES.md</files>
  <behavior>2024-Q3 has four `cs_p14_c` corrections and Q4 has five `cs_p14_c`, one `par_c`, one `cs_p20a_c`, two `cs_p20b_c`; missing bitácora in other quarters is explicit. 2025-Q1 dictionary is distinct; ENT through 2025-Q2 becomes CVE_ENT from 2025-Q3. CMPE raw 31300/32100/33100 map to six-digit 031300/032100/033100; 999999 and absent remain unknown.</behavior>
  <action>Extend each returned metadata record with official catalog identity, terms URL, authority/license, acquisition timestamp, source/receipt/raw/member hashes, dictionary-derived coding, CMPE catalog version, revision count/date/member hash, geography header aliases and an explicit `concept_equivalence_review` state. Inspect only packaged metadata; validate ASCII-digit catalog keys, unique six-digit normalization and membership, never infer a field from absent or 999999. Record 2024 replacement bitácoras and their 2025-05-27 date; explicitly record lack of SDEM bitácora elsewhere without claiming no revisions ever occurred. List all eight periods and verified member/header/revision facts in `docs/SOURCES.md`, with INEGI attribution, exact terms link, transformation explanation, local raw restriction and unresolved person-row encoding. Add tests for absent terms/acquired date, malformed encoding, conflicting alias, unknown code, and a metadata export scan that contains neither person rows nor a ZIP payload. Cover edge SRC-04 empty and encoding. Do not label metadata as publication approval.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_source_inventory.py -q</automated><offline_integration>.venv/Scripts/python.exe -c "from pathlib import Path; from brujula.source_inventory import inventory_all; assert len(inventory_all(Path('artifacts/enoe'))) == 8"</offline_integration></verify>
  <acceptance_criteria>An integration test calls `inventory_all(Path('artifacts/enoe'))` without network and checks all eight cached ZIPs, full SDEM member hashes, 114-to-115 header shift, both 2024 revision logs, catalog keys, provenance and terms; malformed fixtures fail. The test skips only when its documented local cache is absent, and execution must report that as incomplete rather than a pass.</acceptance_criteria>
  <done>Every period's source identity, edition, coding, geography alias and transformation provenance is reviewable from metadata only.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Registry and ZIP bytes enter local custody from an official remote host; metadata crosses from the ignored raw area to shareable research evidence.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | current/raw/member | mitigate | Resolve current against immutable receipt, approved URL/SHA and ZIP safety; exact member/hash negative tests. |
| T-01-02 | Information disclosure | inventory projection | mitigate | Allow metadata only and assert absence of person records and ZIP payload. |
| T-01-03 | Spoofing | registry/source identity | mitigate | Require approved eight IDs, host and pinned SHA through resolver. |
| T-01-04 | Denial of service | ZIP/member read | mitigate | Keep acquisition size/ZIP bounds and read only named metadata plus header. |
| T-01-SC | Tampering | package install | accept | No new package install in this plan. |
</threat_model>

<verification>
Run focused tests and explicitly call `inventory_all(Path('artifacts/enoe'))` as the eight-current offline integration; inspect produced metadata for all quarters and confirm no network access or person-row output. If the local cache is absent or any current is failed, report the gate blocked.
</verification>

<success_criteria>
SRC-01 through SRC-04 are backed by exact resolver-verified packages, complete metadata/provenance, and failure controls.
</success_criteria>

<artifacts_created>
`brujula/source_inventory.py` is the Phase 2 source metadata interface; `inventory_snapshot`/`inventory_all` and their stable output fields above are the downstream contract.
</artifacts_created>

<output>
Create `.planning/phases/01-official-sources-and-research-contract/01-01-SUMMARY.md` when done.
</output>
