---
phase: 01-official-sources-and-research-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brujula/populations.py
  - tests/test_population_rules.py
autonomous: true
requirements: [CTR-02]
must_haves:
  truths:
    - "National context accepts valid R_DEF and C_RES with operational EDA 15 through 98; 98 is always unknown age, never a person's stated 98 years."
    - "Completed-professional cohort accepts known age 15 through 97, CS_P13_1=07, CS_P16=1 and valid response/residence only."
    - "Technical, incomplete, postgraduate, unknown age, unknown completion and unknown field counts remain separate exclusions."
    - "Study fields come from period-specific six-digit CMPE keys; missing and 999999 remain unknown, and field, occupation and industry never become interchangeable."
    - "Income zero or ING7C sentinels cannot make an unknown wage zero or claim an income-positive-known denominator is all occupied people."
    - "Observed n, valid-response n, excluded/unknown n by reason and weighted denominators have different names and meanings."
    - "Edge CTR-02 boundary/empty/encoding/precision has explicit tests at 15, 97, 98, 99, empty rows, malformed codes and denominator calculations."
  artifacts:
    - path: brujula/populations.py
      provides: "Pure population, code and denominator rules"
    - path: tests/test_population_rules.py
      provides: "Synthetic boundary and sentinel controls"
  key_links:
    - from: brujula/populations.py
      to: data/catalog/enoe-snapshots.json
      via: "period identity; caller provides the verified per-period study catalog"
    - from: brujula/populations.py
      to: brujula/survey.py
      via: "Phase 2 consumes eligible domain masks, without changing its estimator here"
  prohibitions:
    - statement: "Do not call the completed-professional cohort everyone who ever attended a licenciatura or silently fold postgraduate and unknown education into it."
      status: unverified
      flagged: true
    - statement: "Do not turn missing income, age or study field into zero or a fabricated category-specific finding."
      status: unverified
      flagged: true
---

<objective>
Fix the two analysis populations, study-code mapping and denominator semantics as pure, testable rules.

Purpose: Phase 2 must receive stable eligibility definitions before computing survey statistics.
Output: `brujula/populations.py` and boundary-focused tests.
</objective>

<execution_context>
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@.planning/phases/01-official-sources-and-research-contract/01-CONTEXT.md
@.planning/phases/01-official-sources-and-research-contract/01-RESEARCH.md
@.planning/phases/01-official-sources-and-research-contract/01-EDGE-PROBE.json
@brujula/survey.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Encode named population eligibility and exclusion reasons</name>
  <read_first>01-CONTEXT.md; 01-RESEARCH.md Architecture Patterns; brujula/survey.py; docs/research/ENOE-METHOD-REVIEW.md</read_first>
  <files>brujula/populations.py, tests/test_population_rules.py</files>
  <behavior>`national_15_plus_context`: normalized R_DEF=00, C_RES 1 or 3, EDA 15..98 inclusive with 98 tagged unknown age. `completed_professional_known_age`: same response/residence, EDA 15..97, CS_P13_1=07 and CS_P16=1. 14/15, 97/98/99, 06/07/08/09, absent/blank, valid/invalid residency are distinct tests. A postgraduate or technical row never enters the completed-professional cohort.</behavior>
  <action>Make `brujula/populations.py` the single authoritative export of population IDs and definitions: expose string constants `NATIONAL_15_PLUS_CONTEXT='national_15_plus_context'` and `COMPLETED_PROFESSIONAL_KNOWN_AGE='completed_professional_known_age'`, plus immutable `POPULATION_DEFINITIONS` keyed by those IDs with response/residence, age and education rules. Expose `classify_eligibility(row: Mapping[str, object], population_id: str) -> dict` and `summarize_denominators(rows: Iterable[Mapping[str, object]], population_id: str) -> dict`. Return explicit eligible boolean, `age_unknown` boolean and a stable exclusion-reason list; keep unknown and invalid input distinct. Normalize documented ENOE two-digit codes without accepting arbitrary coercion; use dictionary-derived sentinels from all eight snapshots in research. The denominator summary reports observed eligible n, valid-response n, exclusion counts by reason, and weighted eligible denominator separately; never call weighted sum `sample_size` or effective n. Make empty input return zero counts with unavailable weighted denominator `null`, not a fabricated measure. Keep population logic pure and do not modify the existing `survey.py` estimator. Cover edge CTR-02 boundary and empty.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_population_rules.py -q</automated></verify>
  <acceptance_criteria>All response, residence, age, education and completion boundaries have fixture assertions, including 98 unknown age and 08/09 exclusion; outputs have reason-specific counts.</acceptance_criteria>
  <done>Both named populations and their exclusions are deterministic and cannot be confused by a caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate study codes and measure denominators</name>
  <read_first>01-RESEARCH.md Source and Package Inventory and Architecture Patterns; docs/research/ENOE-METHOD-REVIEW.md; tests/test_survey.py</read_first>
  <files>brujula/populations.py, tests/test_population_rules.py</files>
  <behavior>Raw CMPE 31300/32100/33100 map to 031300/032100/033100 only when present in the supplied period catalog. 999999, missing, duplicate normalized catalog key and nondigit key do not invent fields. ING7C 6 means no income and 7 unspecified; INGOCUP=0 alone is not zero wage. Valid positive-known income denominator differs from all occupied and remains null for empty/unknown-only rows.</behavior>
  <action>Expose `normalize_cmpe_key(raw: object, valid_catalog_keys: Collection[str]) -> str | None` with exact six-digit digit-only membership, `classify_income_state(row: Mapping[str, object]) -> dict`, and `summarize_measure_denominator(rows: Iterable[Mapping[str, object]], measure_id: str) -> dict`. Require the caller to pass catalog keys from the verified period inventory; reject duplicate keys after normalization and never accept 999999 as a named field. Define explicit measure eligibility for employment and positive-known income from the eight SDEM dictionaries, documenting unknown/no-income/nonresponse states in return fields. Keep field_of_study, occupation, industry, geography, sex and period as separate keys; no bridge creation. Weighted denominator remains null when unavailable or invalid and is never inferred from observed n. Cover edge CTR-02 encoding and precision with malformed keys, overflow/nonfinite weights and finite accumulation tests. Phase 2 owns estimate and variance calculations.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_population_rules.py tests/test_survey.py -q</automated></verify>
  <acceptance_criteria>Known fields resolve only through supplied catalog membership, all sentinels preserve unknown/no-income meaning, and denominator outputs distinguish observed, weighted and excluded counts.</acceptance_criteria>
  <done>Phase 2 can use pure rules for domain masks and named denominators without interpreting missing values as zero.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Untrusted ENOE row values and official metadata codes enter eligibility/denominator rules; their outputs may influence public estimates later.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | code and sentinel inputs | mitigate | Strict documented code normalization, catalog membership and negative fixtures. |
| T-02-02 | Information disclosure | domain outputs | mitigate | Return masks/counts only; never emit person rows from summary functions. |
| T-02-03 | Repudiation | exclusions | mitigate | Reason-specific counts make omitted/unknown rows visible. |
| T-02-04 | Tampering | weighted denominator | mitigate | Reject nonfinite/invalid weights and distinguish observed n from weighted sum. |
| T-02-SC | Tampering | package install | accept | No new package install in this plan. |
</threat_model>

<verification>
Run population and existing survey fixture tests; compare rule wording with the eight official SDEM dictionaries captured in the inventory research.
</verification>

<success_criteria>
CTR-02 eligibility, exclusions, code normalization and denominator rules are explicit and boundary tested.
</success_criteria>

<artifacts_created>
The named constants and pure helper signatures above are the Phase 2 population interface; numeric estimation remains outside this phase.
</artifacts_created>

<output>
Create `.planning/phases/01-official-sources-and-research-contract/01-02-SUMMARY.md` when done.
</output>
