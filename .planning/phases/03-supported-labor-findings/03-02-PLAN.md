---
phase: 03-supported-labor-findings
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - brujula/comparisons_v2.py
  - data/catalog/enoe-geography-equivalence.json
  - tests/test_comparisons_v2.py
  - tests/phase3_prohibitions_02.py
  - tests/phase3_prohibitions_02.test.cjs
  - tests/fixtures/phase3_prohibitions/02.clean.json
  - tests/fixtures/phase3_prohibitions/02-p3.bad.json
  - tests/fixtures/phase3_prohibitions/02-p4.bad.json
autonomous: true
requirements: [ANA-02, ANA-03, ANA-04]
must_haves:
  truths:
    - "All eight national/focal/cohort series expose ordered quarter cells; each complete series has seven adjacent-quarter and four like-quarter annual pair slots."
    - "Only compatible supported public endpoints produce descriptive deltas; blocked or unavailable pairs retain null absolute and relative deltas plus explicit reasons."
    - "A reviewed ENT/CVE_ENT state concept signature can permit a cross-2025-Q3 descriptive comparison while edition, universe and method mismatches still block it."
    - "ANA-02 boundary: pair generator creates exactly q→q+1 and q→q+4 inside the eight-quarter window, never a ninth or reversed pair."
    - "ANA-02 adjacency: touching adjacent quarters receive seasonality caveat; same-quarter year pairs receive like-quarter label and no independent-sample claim."
    - "ANA-02 empty: a series with missing quarter emits its expected pair slot blocked with null deltas rather than silently shortening the series."
    - "ANA-02 ordering: shuffled periods/records yield the same chronologically ordered pair IDs and verdicts."
    - "ANA-02 precision: rate differences use percentage points; income differences are nominal; a zero prior value gives null relative percent without false infinity."
    - "ANA-03 boundary: every source-family/edition, population/version, normalized geography, sex, concept/classification, metric/denominator/version, unit, price basis, estimator/design/method and precision-policy mismatch blocks with null deltas."
    - "ANA-03 adjacency: 2025-Q2 ENT and 2025-Q3 CVE_ENT state keys compare only when normalized code, official name and reviewed concept evidence all match."
    - "ANA-03 empty: missing definition version, incompatible revision evidence or suppressed endpoint blocks the pair rather than defaulting to equivalence."
    - "Longitudinal pairs require the same sex and geography selection; a separate same-period cross-slice comparator permits exactly one declared sex or entity axis difference with every other signature member and snapshot fixed."
    - "ANA-03 encoding: old state codes 1..32 and new 01..32 normalize to two ASCII digits while native aliases and raw source identity remain provenance."
    - "ANA-03 ordering: mismatch reasons and endpoint IDs have stable order under metadata permutation."
    - "ANA-03 precision: no comparison SE, CI, p-value or significance claim is computed from marginal quarter intervals."
  artifacts:
    - path: brujula/comparisons_v2.py
      provides: "Strict versioned comparison signatures and descriptive QoQ/YoY ledger"
    - path: data/catalog/enoe-geography-equivalence.json
      provides: "Reviewed ENT/CVE_ENT code/name/provenance rule"
    - path: tests/test_comparisons_v2.py
      provides: "Signature mismatch matrix and boundary tests"
    - path: tests/phase3_prohibitions_02.test.cjs
      provides: "Executable nominal-income/rotating-sample controls with bad and clean subjects"
  key_links:
    - from: brujula/comparisons_v2.py
      to: brujula/analysis_v2.py
      via: "canonical public record IDs and exact-grain profile cells"
    - from: brujula/comparisons_v2.py
      to: data/catalog/enoe-geography-equivalence.json
      via: "verified state code/name normalization and edition evidence"
  prohibitions:
    - statement: "A comparison must not describe nominal income change as purchasing-power change or compare minimum-wage income bands across years as a stable real-price class."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase3_prohibitions_02.test.cjs
      check_violation_fixture: tests/fixtures/phase3_prohibitions/02-p3.bad.json
      check_clean_fixture: tests/fixtures/phase3_prohibitions/02.clean.json
    - statement: "Rotating quarterly samples must not be summed as independent people or used to invent significance from separate marginal intervals."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase3_prohibitions_02.test.cjs
      check_violation_fixture: tests/fixtures/phase3_prohibitions/02-p4.bad.json
      check_clean_fixture: tests/fixtures/phase3_prohibitions/02.clean.json
---

<objective>
Produce strictly comparable descriptive time and geography comparisons over accepted public estimates.
Purpose: Changed definitions, revisions, sparse endpoints and overlapping samples must remain visible limits on interpretation.
Output: Versioned comparison signatures, an evidence-backed geography equivalence table and QoQ/YoY ledger.
</objective>

<execution_context>
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/phases/03-supported-labor-findings/03-CONTEXT.md
@.planning/phases/03-supported-labor-findings/03-RESEARCH.md
@.planning/phases/03-supported-labor-findings/03-EDGE-PROBE.json
@.planning/research/GEOGRAPHY-COMPARABILITY.md
@.planning/phases/03-supported-labor-findings/03-01-SUMMARY.md
@data/catalog/enoe-metrics.json
@brujula/analysis_v2.py
@brujula/quality.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resolve geography and complete comparison signatures</name>
  <files>brujula/comparisons_v2.py, data/catalog/enoe-geography-equivalence.json, tests/test_comparisons_v2.py</files>
  <behavior>Different quarter snapshot hashes remain distinct provenance while compatible `INEGI ENOE` family, edition and definition versions can compare. Any missing or unequal signature member blocks with deterministic reason and null delta. ENT/CVE_ENT code/name equality is necessary but not sufficient; reviewed concept evidence is required. National and professional populations never compare to each other.</behavior>
  <action>Implement `comparison_signature(record: dict, provenance: dict, definition_registry: dict) -> dict` and longitudinal `compare_public_records(previous: dict, current: dict, *, registry: dict) -> dict` plus `compare_public_slices(left: dict, right: dict, *, axis: str, registry: dict) -> dict` using only accepted public index records. Longitudinal pairs retain equal recorded sex and geography key. The separate cross-slice comparator requires same period and exact snapshot ID/hash, source family/edition, population/version, field/occupation/industry concept/classification, metric numerator/denominator/version, unit/basis, method/design, singleton/precision/suppression policy, and geography type/concept; only declared axis may differ. Sex contrast fixes national geography and compares recorded 1 versus 2; entity contrast fixes recorded sex and compares state keys only when one equals mandatory reviewed registry entity_reference_code. A missing reference or any second differing axis blocks. Both endpoints must be supported. Resolve source family plus reviewed edition/correction compatibility tied to each exact approved pinned snapshot hash/version; reject a hash or revision outside the approved set and expose unknown edition date as a limitation, never an invented fact; for longitudinal pairs, population ID/definition version; normalized geography concept/key, recorded sex and field/occupation/industry concept/classification versions; metric numerator/denominator definition version from the Phase 2 manifest; unit/price basis; estimator/design/method version; singleton/precision/suppression-rule versions. Missing registry/version values block, even if label/unit match. For longitudinal pairs, snapshot IDs and SHA-256 are unequal provenance on endpoints, not equal-required signature fields; same-period cross-slice pairs require exact equality. Build `enoe-geography-equivalence.json` from the verified Phase 1 state catalogs and `.planning/research/GEOGRAPHY-COMPARABILITY.md`: ENT 1..32 → ASCII 01..32, CVE_ENT 01..32 unchanged, exact key/name mapping, native alias plus dictionary/catalog hashes and the official 2025-Q3 cross-boundary evidence. Do not treat this as a source activation or universal state-level equivalence; require all other signature fields and a reviewed concept assertion. Table-drive every mismatch, supported and blocked same-period sex/entity pair, missing reference, unsupported endpoint and wrong alias/code test; assert only one declared axis changes and all other signature members including exact snapshot and period remain equal. Cover all ANA-03 edge truths.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_comparisons_v2.py -q</automated></verify>
  <done>Every compatible or blocked pair has a complete, versioned reasoned signature; the geography bridge is evidence-backed and conditional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate quarter and sex/territory descriptive ledgers</name>
  <files>brujula/comparisons_v2.py, tests/test_comparisons_v2.py</files>
  <behavior>Each full eight-quarter national series yields seven adjacent and four like-quarter annual slots; sparse endpoints keep blocked slots with null deltas. Rate deltas use percentage points, other deltas their unit, and relative percent is null for zero prior. Same-period sex contrasts use recorded 1 versus 2 at fixed national geography; entity contrasts use an explicit reviewed reference state at fixed recorded sex. Supported one-axis contrasts pass; two-axis, cross-period/source or mismatched metric/denominator/method contrasts block. There is no change precision, significance, summed quarterly person count or sex×state inference.</behavior>
  <action>Implement `build_comparison_ledger(profiles: dict, *, registry: dict) -> list[dict]` for q→q+1 and q→q+4 only across the eight known periods, plus supported same-period recorded-sex and explicit-reference entity descriptive contrasts for focal/cohort questions through compare_public_slices. Emit stable comparison ID, ordered endpoint record IDs and snapshot SHA-256, status, comparable flag, all reasons, absolute change, relative percent where defined, display-unit change and evidence refs. For rates label percentage points; for positive-known income label nominal MXN and prohibit purchasing-power language; adjacent quarters carry a seasonality caveat, annual pairs are like quarters. No SE/CI/p-value/significance field for changes, no combined quarterly population/sample size, and no minimum-wage-band cross-year comparison. Keep every expected pair slot visible even where endpoint null. Cover all ANA-02 boundary/adjacency/empty/ordering/precision truths and ANA-04 same-period slice support.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_comparisons_v2.py -q</automated></verify>
  <done>Deterministic, complete comparison slots contain only supportable descriptive deltas and explicit blocked causes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove nominal-income and rotating-sample prohibitions</name>
  <files>tests/phase3_prohibitions_02.py, tests/phase3_prohibitions_02.test.cjs, tests/fixtures/phase3_prohibitions/02.clean.json, tests/fixtures/phase3_prohibitions/02-p3.bad.json, tests/fixtures/phase3_prohibitions/02-p4.bad.json</files>
  <behavior>Named Node tests pass current comparison ledger and clean subject. p3 bad subject changes a computed nominal MXN income change to purchasing-power wording or allows a cross-year minimum-wage-band class and the income test fails. p4 bad subject adds a cross-quarter distinct-person sum, delta SE/CI/p-value or significance interpretation to a computed ledger and the rotating-sample test fails.</behavior>
  <action>Use a portable Node wrapper and Python runner to construct real supported public comparison fixtures and call `compare_public_records`/`build_comparison_ledger`. Resolve `BRUJULA_TEST_PYTHON` or platform venv path. Assert nominal income unit/qualifier and absence or blocked status of minimum-wage-band cross-year pairs; assert no independent combined-person quantity or delta precision/significance in the typed ledger. Apply p3/p4 bad mutation only to computed results after the API call. The clean JSON subject leaves results unmodified; each bad fixture must turn its matching named test red for a substantive output assertion, not merely for its case label. Invoke the canonical GSD producer for both projected descriptors and require current green, bad red, clean green and non-vacuity before closure. These controls are planned, not previously passed.</action>
  <verify><automated>node --test tests/phase3_prohibitions_02.test.cjs</automated></verify>
  <done>Both comparison prohibitions have enforceable live API checks and content-dependent bad/clean subjects.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Public estimates and separately verified source/catalog metadata enter a comparison decision; blocked metadata may never be interpreted as numeric change.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-05 | Spoofing | source-family/edition signature | mitigate | Preserve snapshot hash; verify edition and method/definition registry before equality. |
| T-03-06 | Tampering | geography alias | mitigate | Exact 32 code/name mapping and official cross-boundary evidence with negative key/name fixtures. |
| T-03-07 | Repudiation | blocked delta | mitigate | Stable ordered endpoint IDs, all reason codes and null numeric fields. |
| T-03-08 | Information disclosure | sparse pair | mitigate | Public-only endpoints; no diagnostic fallback or delta from null. |
| T-03-SC | Tampering | dependency install | accept | No new package install in this plan. |
</threat_model>

<verification>Run focused tests, both Node prohibition controls and full pytest after Wave 2, then inspect the real cross-2025-Q3 national/state candidate against the verified edition and code/name registry. Require canonical producer `located:true`, bad red, clean green, non-vacuous current pass and `flagged:false` for both descriptors; an absent planned check blocks closure.</verification>

<success_criteria>ANA-02/03 and the comparison part of ANA-04 yield only compatible, descriptive and fully attributed deltas.</success_criteria>

<artifacts_created>
Artifacts this phase produces in this plan: `brujula/comparisons_v2.py` exports `comparison_signature(record: dict, provenance: dict, definition_registry: dict) -> dict`, longitudinal `compare_public_records(previous: dict, current: dict, *, registry: dict) -> dict`, same-period `compare_public_slices(left: dict, right: dict, *, axis: str, registry: dict) -> dict`, and `build_comparison_ledger(profiles: dict, *, registry: dict) -> list[dict]`. Each ledger entry has `comparison_id`, ordered `previous_record_id`/`current_record_id`, `status`, `comparable`, `absolute_change|null`, `relative_change_pct|null`, `display_unit`, `reasons`, `evidence_refs`, `source_snapshot_ids`, and `source_sha256s`; no change-precision field exists. `data/catalog/enoe-geography-equivalence.json` carries versioned state aliases, exact 32 key/name mappings, native catalog/member hashes, source revision evidence and official cross-boundary reference.
`tests/phase3_prohibitions_02.test.cjs` and `.py` inspect current comparison APIs; `tests/fixtures/phase3_prohibitions/02.clean.json`, `02-p3.bad.json`, and `02-p4.bad.json` are planned producer subjects.
</artifacts_created>

<output>Create `.planning/phases/03-supported-labor-findings/03-02-SUMMARY.md` when done.</output>
