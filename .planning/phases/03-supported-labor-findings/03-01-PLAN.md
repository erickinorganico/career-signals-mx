---
phase: 03-supported-labor-findings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brujula/analysis_v2.py
  - tests/test_analysis_v2.py
  - data/fixtures/analysis-v2-golden.json
  - tests/phase3_prohibitions_01.py
  - tests/phase3_prohibitions_01.test.cjs
  - tests/fixtures/phase3_prohibitions/01.clean.json
  - tests/fixtures/phase3_prohibitions/01-p1.bad.json
  - tests/fixtures/phase3_prohibitions/01-p2.bad.json
autonomous: true
requirements: [ANA-01, ANA-04, ANA-05]
must_haves:
  truths:
    - "After accepted Phase 2 input, eight national quarters contain distinct national 15+ context, completed-professional cohort and three focal-field profiles with all accepted labor metrics."
    - "Latest-quarter coverage includes every official identifiable professional field, 32 entity slots and two recorded-sex slots for the cohort and three focal fields; unsupported cells retain null and reason."
    - "Phase 3 preflight rejects a missing Phase 2 requested/evaluated computation; only a present computed suppression can become a sparse null with its actual reason."
    - "Observed support, response, field/age exclusions and exact-income coverage are joined by explicit period/population/field/metric keys; weighted quantities are labeled estimates."
    - "ANA-01 adjacency: a catalog field code shared with an occupation or industry ID remains a separate field-of-study concept."
    - "ANA-01 empty: a computed suppressed public record produces a visible null cell with its actual reason rather than an invented field value; an absent computation blocks."
    - "ANA-01 encoding: official CMPE labels and normalized six-digit keys remain linked; blank and 999999 stay unknown, never named fields."
    - "ANA-01 ordering: reordering public records or catalog rows leaves the complete profile grid and canonical IDs unchanged."
    - "ANA-04 boundary: latest-quarter state grid has exactly 32 keys 01..32 and exactly two recorded-sex selections for each focal field and cohort."
    - "ANA-04 adjacency: state and sex slices remain separate even where a source record could satisfy both selectors."
    - "ANA-04 empty: an unsupported entity/sex cell stays present with null, precision state and reason; absent coverage never becomes zero."
    - "ANA-04 ordering: all 32 state and both sex slots sort by canonical codes independent of source input order."
    - "ANA-04 precision: available values retain their public support and uncertainty, while sparse cells expose no hidden weighted denominator or confidence limits."
    - "ANA-05 boundary: observed n, response denominator and exact-income response coverage use their own named denominator at zero/full/partial coverage."
    - "ANA-05 adjacency: age unknown, field unknown, technical, postgraduate, incomplete and income-unavailable exclusions remain distinct even when one person meets multiple reasons."
    - "ANA-05 empty: unavailable audit bucket or zero denominator yields null coverage with reason, not a zero percent."
    - "ANA-05 ordering: exclusion and coverage counts are keyed and stable under audit entry permutation."
    - "ANA-05 precision: weighted population estimates are never presented as observed n or effective sample size, and complementary cells cannot disclose suppressed values."
  artifacts:
    - path: brujula/analysis_v2.py
      provides: "Validated public record index, expected-cell profiles and coverage"
    - path: tests/test_analysis_v2.py
      provides: "Profile completeness, support and suppression tests"
    - path: data/fixtures/analysis-v2-golden.json
      provides: "Synthetic or public aggregate-only accepted fixture"
    - path: tests/phase3_prohibitions_01.test.cjs
      provides: "Executable sparse-cell/cohort-label controls with bad and clean subjects"
  key_links:
    - from: brujula/analysis_v2.py
      to: brujula/research_contract.py
      via: "GRAIN and validate_public_research_v2 before indexing"
    - from: brujula/analysis_v2.py
      to: data/catalog/enoe-metrics.json
      via: "accepted Phase 2 manifest identity and metric definitions"
  prohibitions:
    - statement: "A sparse state, sex or field cell must not disappear from coverage or become an observed zero to make a profile look complete."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase3_prohibitions_01.test.cjs
      check_violation_fixture: tests/fixtures/phase3_prohibitions/01-p1.bad.json
      check_clean_fixture: tests/fixtures/phase3_prohibitions/01.clean.json
    - statement: "The completed-professional cohort must not be described as all graduates or as all people working in a profession."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase3_prohibitions_01.test.cjs
      check_violation_fixture: tests/fixtures/phase3_prohibitions/01-p2.bad.json
      check_clean_fixture: tests/fixtures/phase3_prohibitions/01.clean.json
---

<objective>
Create a complete, evidence-bound public aggregate index and coverage grid for the labor analysis.
Purpose: Readers need the full requested scope and visibly missing cells before comparisons or claims can be trusted.
Output: Stable record IDs, all-period national/focal profiles, latest-quarter field/entity/sex coverage and exclusion metadata.
</objective>

<execution_context>
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-supported-labor-findings/03-CONTEXT.md
@.planning/phases/03-supported-labor-findings/03-RESEARCH.md
@.planning/phases/03-supported-labor-findings/03-PATTERNS.md
@.planning/phases/03-supported-labor-findings/03-EDGE-PROBE.json
@.planning/phases/02-defensible-survey-estimates/02-03-SUMMARY.md
@brujula/research_contract.py
@contracts/research-v2-public.schema.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Gate Phase 2 input and index canonical public records</name>
  <files>brujula/analysis_v2.py, tests/test_analysis_v2.py, data/fixtures/analysis-v2-golden.json</files>
  <behavior>Absent or failed Phase 2 eight-snapshot acceptance, changed source/manifest hash, invalid public v2 payload, mixed period/source refs or duplicate grain blocks construction. One accepted public record maps to one stable `v2r:` ID derived from canonical JSON serialization of all ten `GRAIN` fields; IDs and sorted index remain identical under record reordering and no record `id` is presumed. Synthetic fixture stays synthetic and aggregate-only.</behavior>
  <action>Before implementation, inspect the actual completed Phase 2 `estimate_snapshot` return shape, metric manifest/version, public payloads and `scripts/accept_enoe_estimates.py` ledger; require verified Phase 1 and Phase 2 summaries and a passing eight-period gate. Check the accepted `requested_cells`/`evaluated_cells` sets against exact public record grains: all eight national context/cohort/focal-three metric cells, latest national every verified identifiable field, latest 32 entity and two recorded-sex slices for cohort and each focal field separately. A missing or unevaluated computation blocks Phase 3 even if a profile could synthesize a null. Keep dependent output blocked if signatures/acceptance/definition versions are absent, and revise this plan only if actual interfaces materially differ. Implement `index_public_estimates(public_by_snapshot: Mapping[str, dict], acceptance: Mapping[str, object]) -> dict`: validate every public v2 root with `validate_public_research_v2`, match accepted snapshot IDs/hashes/method manifest, sort records by all ten `GRAIN` keys, derive version-prefixed SHA-256 IDs from canonical UTF-8 JSON of the tuple, retain the tuple itself, and reject any collision or duplicate. Never open person rows or internal `estimate` payloads. Put only public aggregates in the fixture, with explicit synthetic labeling where applicable. Cover ANA-01 encoding/ordering and the Phase 2 dependency gate.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_analysis_v2.py -q</automated></verify>
  <done>Indexing is deterministic and blocked unless the accepted strict public v2 input and matching Phase 2 manifest exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize complete profiles, sparse slices and coverage</name>
  <files>brujula/analysis_v2.py, tests/test_analysis_v2.py</files>
  <behavior>Eight national context/cohort/focal periods carry every Phase 2 accepted metric; latest quarter includes all identifiable official fields and all 32 state plus two sex slots for cohort/focal fields. Null cells have a controlled reason. No implicit sex×state cross grid. Observed response and exclusions retain separate denominators and all labels; public-only data cannot recreate a suppressed estimate through complements.</behavior>
  <action>Implement `build_profiles(index: dict, coverage_audits: Mapping[str, dict], *, latest_period_id: str) -> dict`. Build the expected-cell matrix from the verified eight-period source catalog, Phase 2 metric manifest and accepted request/evaluation ledger, per-quarter CMPE catalog and official 32-state/two-sex code sets; preserve national operational 15+ context separate from completed-professional known-age cohort. For the three focal CMPE keys `031300`, `032100`, `033100`, include employment, participation, unemployment, positive-known nominal income and coverage, main-job informality, sex composition, position, suboccupation and known weekly hours; latest other identifiable fields have official labels and visible support status. A missing Phase 2 computation or public record is a blocking integrity error, never a constructed sparse cell. Preserve null/reason only from an actually computed suppressed/unsupported public record with its source support cause; do not substitute n<30 for an absent calculation. Join aggregate-only observed n, responding/eligible counts and exclusion reasons by exact period/population/field/metric; separately label weighted quantities as estimates. Suppress or omit complementary coverage totals that reconstruct a hidden value; add a nested numeric sentinel test. Cover every ANA-01/04/05 boundary, adjacency, empty, ordering and precision truth listed in frontmatter.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_analysis_v2.py -q</automated></verify>
  <done>Complete requested grids and coverage pass aggregate-only tests without dropping sparse cells or leaking suppressed values.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove sparse-cell and cohort-label prohibitions</name>
  <files>tests/phase3_prohibitions_01.py, tests/phase3_prohibitions_01.test.cjs, tests/fixtures/phase3_prohibitions/01.clean.json, tests/fixtures/phase3_prohibitions/01-p1.bad.json, tests/fixtures/phase3_prohibitions/01-p2.bad.json</files>
  <behavior>Named controls pass against the actual `build_profiles` result and the clean fixture. p1 bad subject deletes a required sparse cell or turns its null into zero, making only the sparse-cell test red. p2 bad subject changes the computed professional-cohort label to all graduates or all workers in a profession, making only the cohort-label test red.</behavior>
  <action>Build a portable Node wrapper over a Python runner that calls `index_public_estimates` and `build_profiles` on the validated aggregate fixture, resolving `BRUJULA_TEST_PYTHON` or the platform venv path. Assert exact 32/two-sex/focal expected slots and controlled null reasons; compare cohort labels to the actual `POPULATION_DEFINITIONS` known-age completed-professional definition and retain the separate national context. Apply bad-subject mutations only after the real API returns; do not construct a canned passing packet or make the test fail merely because an environment variable is set. The shared clean subject leaves API output intact. Run normal and clean green, each bad subject red in its matching named test, then require the canonical GSD prohibition-enforcement producer to machine-prove the bad/clean/current behavior for both descriptors. Controls are planned until implemented and proven.</action>
  <verify><automated>node --test tests/phase3_prohibitions_01.test.cjs</automated></verify>
  <done>Both statements have content-dependent current API checks and known-bad/clean fixtures.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Accepted Phase 2 public aggregates and aggregate audits enter an analytical packet; person rows and internal diagnostics must remain outside.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing | Phase 2 acceptance/input | mitigate | Verify all eight snapshot hashes, method manifest and public v2 roots before indexing. |
| T-03-02 | Tampering | record identity | mitigate | Canonical ten-key grain serialization, duplicate/collision negative tests. |
| T-03-03 | Information disclosure | sparse coverage | mitigate | Public-only input, complementary suppression and nested sentinel tests. |
| T-03-04 | Repudiation | missing profile cell | mitigate | Preserve controlled reason, source, population and metric keys in every expected slot. |
| T-03-SC | Tampering | dependency install | accept | No new package install in this plan. |
</threat_model>

<verification>Run the focused synthetic aggregate tests, both Node prohibition controls and full pytest after Wave 1. Real profiles require the accepted Phase 2 all-eight ledger; a fixture cannot substitute for that gate. Require canonical producer `located:true`, bad red, clean green, non-vacuous current pass and `flagged:false` for both descriptors before canonical closure.</verification>

<success_criteria>ANA-01/04/05 produce complete, traceable and suppression-safe public profiles and coverage.</success_criteria>

<artifacts_created>
Artifacts this phase produces in this plan: `brujula/analysis_v2.py` exports `index_public_estimates(public_by_snapshot: Mapping[str, dict], acceptance: Mapping[str, object]) -> dict` and `build_profiles(index: dict, coverage_audits: Mapping[str, dict], *, latest_period_id: str) -> dict`. Index entries carry `record_id`, exact ten-key `grain`, public `record`, `snapshot_sha256` and `method_version`; profile cells carry `record_id|null`, `value|null`, controlled `reason`, public precision/support, `source_snapshot_id`, population/field/geography/sex/period/metric IDs and coverage/exclusion counts. `data/fixtures/analysis-v2-golden.json` contains accepted aggregate-only synthetic controls and no individual rows.
`tests/phase3_prohibitions_01.test.cjs` and `.py` check current profile/label APIs; `tests/fixtures/phase3_prohibitions/01.clean.json`, `01-p1.bad.json`, and `01-p2.bad.json` are planned clean/violation subjects for the canonical producer.
</artifacts_created>

<output>Create `.planning/phases/03-supported-labor-findings/03-01-SUMMARY.md` when done.</output>
