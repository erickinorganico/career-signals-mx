---
phase: 03-supported-labor-findings
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - brujula/claims_v2.py
  - brujula/findings_v2.py
  - contracts/analysis-v2.schema.json
  - tests/test_claims_v2.py
  - tests/test_analysis_integration.py
  - tests/phase3_prohibitions_03.py
  - tests/phase3_prohibitions_03.test.cjs
  - tests/fixtures/phase3_prohibitions/03.clean.json
  - tests/fixtures/phase3_prohibitions/03-p5.bad.json
  - tests/fixtures/phase3_prohibitions/03-p6.bad.json
  - tests/fixtures/phase3_prohibitions/03-p7.bad.json
autonomous: true
requirements: [ANA-01, ANA-02, ANA-03, ANA-04, ANA-05, ANA-06]
must_haves:
  truths:
    - "A deterministic analytical packet contains the accepted eight-quarter profiles, latest field/entity/sex coverage, comparison ledger, typed claims and up to three supported opening findings."
    - "Every claim resolves exact public estimate/comparison IDs, source snapshots, evidence, method/version, universe and precision limits; validation regenerates canonical Spanish text exactly."
    - "No report-ready claim can use a suppressed or incompatible endpoint, restore hidden diagnostic quantities, add an unsupported number, imply causality/significance or offer personal career advice."
    - "ANA-06 boundary: selecting zero, one, two or three supported opening claims succeeds; a fourth is rejected."
    - "ANA-06 adjacency: identical displayed numbers from different grains or units remain bound to their exact record/comparison IDs and labels."
    - "ANA-06 empty: no eligible finding produces an empty opening list with a visible limitation, not an invented finding."
    - "ANA-06 encoding: canonical Spanish text preserves exact official labels, accents and units and rejects altered Unicode/lookalike or injected text."
    - "ANA-06 ordering: permuting public records or candidate claims leaves canonical claim IDs, text and opening selection unchanged."
    - "ANA-06 precision: extra numerals, rounded quantities outside the sanctioned formatter, changed uncertainty labels and endpoint-derived false certainty fail the claim gate."
  artifacts:
    - path: brujula/claims_v2.py
      provides: "Typed claim templates and exact evidence/text validator"
    - path: brujula/findings_v2.py
      provides: "Validated analytical packet assembly for Phase 4"
    - path: contracts/analysis-v2.schema.json
      provides: "Strict report-input shape and no diagnostic properties"
    - path: tests/test_analysis_integration.py
      provides: "Full aggregate-only Phase 2 to Phase 3 gate"
    - path: tests/phase3_prohibitions_03.test.cjs
      provides: "Executable canonical-claim/source-activation controls with bad and clean subjects"
  key_links:
    - from: brujula/claims_v2.py
      to: brujula/analysis_v2.py
      via: "canonical record IDs and accepted public profiles"
    - from: brujula/claims_v2.py
      to: brujula/comparisons_v2.py
      via: "only passing descriptive comparison IDs"
    - from: brujula/findings_v2.py
      to: contracts/analysis-v2.schema.json
      via: "strict JSON Schema and semantic validation before Phase 4 consumption"
  prohibitions:
    - statement: "A supported field-of-study observation must not be phrased as the causal effect of studying that field or as employment in the matching occupation."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase3_prohibitions_03.test.cjs
      check_violation_fixture: tests/fixtures/phase3_prohibitions/03-p5.bad.json
      check_clean_fixture: tests/fixtures/phase3_prohibitions/03.clean.json
    - statement: "A generated finding must not contain personal career advice, an unsupported number or a fabricated significance statement."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase3_prohibitions_03.test.cjs
      check_violation_fixture: tests/fixtures/phase3_prohibitions/03-p6.bad.json
      check_clean_fixture: tests/fixtures/phase3_prohibitions/03.clean.json
    - statement: "An agent proposal or template choice must not activate a source, bridge or suppressed figure as a public claim."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase3_prohibitions_03.test.cjs
      check_violation_fixture: tests/fixtures/phase3_prohibitions/03-p7.bad.json
      check_clean_fixture: tests/fixtures/phase3_prohibitions/03.clean.json
---

<objective>
Turn accepted public profiles and compatible comparisons into deterministic, evidence-bound research findings.
Purpose: Phase 4 needs a single validated analytical input whose claims and limitations can be traced exactly.
Output: Typed Spanish claim registry, strict analysis schema and aggregate-only findings packet.
</objective>

<execution_context>
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/phases/03-supported-labor-findings/03-CONTEXT.md
@.planning/phases/03-supported-labor-findings/03-RESEARCH.md
@.planning/phases/03-supported-labor-findings/03-EDGE-PROBE.json
@.planning/phases/03-supported-labor-findings/03-01-SUMMARY.md
@.planning/phases/03-supported-labor-findings/03-02-SUMMARY.md
@brujula/insights.py
@brujula/analysis_v2.py
@brujula/comparisons_v2.py
@contracts/research-v2-public.schema.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build typed Spanish claims and exact validation</name>
  <files>brujula/claims_v2.py, tests/test_claims_v2.py</files>
  <behavior>A typed observation or descriptive-comparison claim regenerates one exact canonical text with correct official label, unit, public value, universe, method/precision and caveat. Same number from another field or unit is not interchangeable. Suppressed/incompatible endpoints, extra numeral, changed label, causal/advice/significance phrase, free text, source/method/evidence mismatch and altered Unicode fail. Zero to three supported opening candidates are valid; four fail.</behavior>
  <action>Implement `make_claim(kind: str, subject_id: str, *, public_index: dict, comparison_ledger: dict) -> dict`, `validate_claim(claim: dict, *, public_index: dict, comparison_ledger: dict) -> list[dict]` and `select_opening_claims(candidates: list[dict], *, limit: int = 3) -> list[dict]`. Use an allowlist of typed templates and canonical formatter; regenerate title/observation/interpretation/limitation from exact public record or comparable ledger, then compare exact text and typed quantities. Every claim has a stable digest ID, original grain or pair ID, estimate IDs, source snapshot IDs/hashes, evidence refs, method ID/version, population and precision status/limitations. Only supported relevance and coverage determine deterministic selection after the data pass; permit fewer than three with a controlled limitation. Add observation, QoQ/YoY, sex and entity descriptive templates only where endpoints support them, including seasonality and nominal-income qualifiers. Reject arbitrary added digits, causal effects, personal recommendations, significance from marginal intervals and field→occupation substitution. Cover all ANA-06 boundary/adjacency/empty/encoding/ordering/precision truths.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_claims_v2.py -q</automated></verify>
  <done>Claim construction and validation bind every displayed statement to exact supported public evidence and reject altered prose or quantities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assemble and gate one report-ready analytical packet</name>
  <files>brujula/findings_v2.py, contracts/analysis-v2.schema.json, tests/test_analysis_integration.py</files>
  <behavior>One accepted eight-snapshot input produces complete profiles, coverage, comparison slots and validated claims under a strict schema; repeated input produces identical content apart from explicit run metadata. Missing Phase 2 acceptance, one invalid public root, duplicate record ID, changed definition version, unsupported claim or hidden numeric sentinel blocks packet creation. The packet contains no internal estimate or person row.</behavior>
  <action>Implement `build_analysis_packet(public_by_snapshot: Mapping[str, dict], acceptance: Mapping[str, object], coverage_audits: Mapping[str, dict], definition_registry: dict) -> dict` and `validate_analysis_packet(packet: dict) -> list[dict]`. Call the Plan 01 public index/profiles, Plan 02 ledger and claim registry; require exact eight accepted periods, current source/method hashes and validated public v2 roots before any analytical output. Define a strict `contracts/analysis-v2.schema.json`, load it through `resources.contract_path` so installed-wheel behavior matches checkout behavior, and allow only `schema_version`, `source_manifest`, `record_index`, `profiles`, `coverage`, `comparisons`, `claims`, `opening_claim_ids`, `limitations` and a deterministic `content_digest`; reject unknown diagnostic fields, nonfinite values, orphan refs, duplicate IDs and unsupported numeric claims semantically. Scan nested packet plus intentionally complementary coverage fixture for suppressed-number sentinels. Include complete period/field/state/sex expected-cell counts and explicit missing reasons; do not render HTML/PDF or promote current. Run the real offline integration only after Phase 2 numerical acceptance and preserve aggregate-only output for Phase 4.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_analysis_integration.py tests/test_claims_v2.py -q</automated></verify>
  <done>Phase 4 receives one deterministic, strict, fully traceable analytical packet and bad evidence blocks it before rendering.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove canonical-claim and source-activation prohibitions</name>
  <files>tests/phase3_prohibitions_03.py, tests/phase3_prohibitions_03.test.cjs, tests/fixtures/phase3_prohibitions/03.clean.json, tests/fixtures/phase3_prohibitions/03-p5.bad.json, tests/fixtures/phase3_prohibitions/03-p6.bad.json, tests/fixtures/phase3_prohibitions/03-p7.bad.json</files>
  <behavior>The three named tests pass a current generated claim/packet and the clean subject. p5 bad subject rewrites a computed field claim as causal or substitutes occupation; p6 adds personal advice, a new number or invented significance; p7 mutates a computed packet to activate a REVIEW bridge/source or insert a suppressed figure as a claim. Each bad subject makes only its matching content assertion go red.</behavior>
  <action>Implement portable Node/Python controls over actual `make_claim`, `validate_claim`, `build_analysis_packet` and `validate_analysis_packet` results from synthetic accepted-public aggregates. Resolve `BRUJULA_TEST_PYTHON` or platform venv path. For p5/p6 regenerate canonical typed Spanish text from underlying public IDs and assert exact equality plus no unsupported typed quantity; the bad subject mutates a computed text/label/typed value, not a prewritten fixture expected to fail. For p7 assert source/bridge statuses and suppressed cells in a generated packet cannot be promoted by template or agent proposal, and strict packet validation rejects the mutated output. Keep the clean subject mutation-free; make each bad control red at a named API-output assertion. Run normal/clean green, bad red and the canonical GSD producer for all three descriptors, requiring machine-proven content causation before closure. These checks do not authorize source activation or publication and are not yet green.</action>
  <verify><automated>node --test tests/phase3_prohibitions_03.test.cjs</automated></verify>
  <done>All three claim/publication-boundary prohibitions have current-output checks with known-bad and known-clean controls.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Validated public estimates and comparison decisions become human-readable claims; typed references and schema validation guard the prose and Phase 4 handoff.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-09 | Spoofing | claim evidence IDs | mitigate | Resolve exact record/comparison/source/method refs and reject swapped-grain fixtures. |
| T-03-10 | Tampering | canonical prose | mitigate | Rebuild text from typed template and compare exact output; extra-number/label tests. |
| T-03-11 | Information disclosure | packet/claim text | mitigate | Public-only input, strict schema and nested/complementary suppression sentinels. |
| T-03-12 | Repudiation | analytical finding | mitigate | Stable claim ID, evidence refs, content digest and explicit limitations. |
| T-03-SC | Tampering | dependency install | accept | No new package install in this plan. |
</threat_model>

<verification>Run all focused Phase 3 tests, all three Node prohibition control targets and `.venv/Scripts/python.exe -m pytest -q`; then consume the real accepted Phase 2 eight-period public output and inspect aggregate-only packet, claim IDs and full source audit. For all seven Phase 3 descriptors require the canonical GSD producer to report `located:true`, a non-vacuous current pass, machine-proven bad-fixture red, clean-fixture green and `flagged:false`; missing or failing control blocks canonical phase completion.</verification>

<success_criteria>ANA-01–06 have one validated public-only analytical packet with complete scope, descriptive comparisons and exact evidence-bound findings.</success_criteria>

<artifacts_created>
Artifacts this phase produces in this plan: `brujula/claims_v2.py` exports `make_claim(kind: str, subject_id: str, *, public_index: dict, comparison_ledger: dict) -> dict`, `validate_claim(claim: dict, *, public_index: dict, comparison_ledger: dict) -> list[dict]`, and `select_opening_claims(candidates: list[dict], *, limit: int = 3) -> list[dict]`. `brujula/findings_v2.py` exports `build_analysis_packet(public_by_snapshot: Mapping[str, dict], acceptance: Mapping[str, object], coverage_audits: Mapping[str, dict], definition_registry: dict) -> dict` and `validate_analysis_packet(packet: dict) -> list[dict]`. `contracts/analysis-v2.schema.json` is the strict Phase 4 input with `source_manifest`, `record_index`, `profiles`, `coverage`, `comparisons`, `claims`, `opening_claim_ids`, `limitations`, and `content_digest` fields; all claim/record/comparison references resolve and no internal diagnostic keys are allowed.
`tests/phase3_prohibitions_03.test.cjs` and `.py` check current claim/packet outputs; `tests/fixtures/phase3_prohibitions/03.clean.json`, `03-p5.bad.json`, `03-p6.bad.json`, and `03-p7.bad.json` are planned clean/violation subjects for the canonical producer.
</artifacts_created>

<output>Create `.planning/phases/03-supported-labor-findings/03-03-SUMMARY.md` when done.</output>
