# Phase 3 planning gates

- Context, primary research, pattern mapping and typed planning completed for the full ANA-01–06 scope. No numerical or report acceptance is inferred from plans.
- Shared edge engine produced 31 candidates; all 31 resolution predicates appear explicitly in flat plan must-have truths. Three sequential plans each have three tasks and artifact/interface declarations.
- Seven finite prohibitions have planned test-tier descriptors and real current-output/bad/clean control tasks. Their files are future implementation requirements, not existing green evidence. Canonical producer proof is required at verification.
- Independent checker found three blockers: temporal equality prevented sex/entity contrasts, a complete grid could mask uncomputed upstream domains, and research decisions remained unmarked. The targeted revision and recheck PASS resolved all three.
- Longitudinal comparisons retain equal selected sex/geography; the separate same-period contrast comparator permits exactly one declared axis, holds other signatures fixed and requires an explicit reviewed entity reference. No difference uncertainty or independent-quarter inference is fabricated.
- Phase 2 now owns a complete requested/evaluated exact-grain ledger. Phase 3 refuses missing computations rather than generating nulls that imply sample-based suppression. Genuine computed unsupported cells remain visible.
- Reviewed pinned editions and concept evidence define conditional comparability. Unknown publication dates remain a limitation; newly changed source hashes outside the reviewed set block automatic differences.
- GSD frontmatter/structure checks passed. The 11-file scope warning for Plan 03 reflects small control fixtures within three tasks and was reviewed without splitting substantive work unnecessarily.
- `03-COVERAGE.md` records the local-only integration boundary. Accepted Phase 2 implementation, actual interface readback and numerical gates are mandatory before Phase 3 execution.
- The mandatory reviewed entity reference is fixed to Baja California (`02`) before final analytical output, using the existing official-reconciliation scope as a methodological anchor. All 32 state profiles remain visible; a suppressed reference blocks that contrast and is never replaced opportunistically. This does not imply state representativeness or a labor-market ranking.

- Phase 2 canonical closure returned no warnings after final initialization/replay, clean source review, Nyquist/ASVS checks and independent 43/43 goal verification. Final upstream interface readback passes; actual public files, keyed audits and complete hash pins match the three reviewed plans. `init.execute-phase 3` finds three incomplete plans in sequential waves 1/2/3; pre and wave-pre hooks are empty. Begin 03-01 with scoped ownership and preserve numerical source.

- Wave 1 accepted: `03-01-SUMMARY.md` canonical verify-summary passes; 378 Python tests, two current Node controls and two canonical bad-red/clean-green prohibitions pass. Real aggregate-only readback yields 6,739 unique records and 6,762 profile appearances, with five unique complementary-suppressed parents propagated through the sanitized downstream record_index. No survey or R rerun. Plans 03-02/03-03 now name the exact implemented interface and acceptance wrapper; this readback alignment preserves the reviewed scope and criteria. Post-wave schema/codebase/UI/API detectors report no blocker or required action. Next wave consumes only the sanitized profiles record_index.

- Wave 2 accepted at 1e741d2: canonical summary verification, 403 Python tests, two current Node controls and two canonical producer proofs pass. Real aggregate-only readback yields 4,209 unique slots: 805 adjacent/460 annual/92 sex/2,852 entity; supported counts 692/393/66/969. Registry metadata is packaged and independently pinned, BC 02 is fixed, missing-slot IDs are unique and changed global policy/state definitions block. Root 03-01 coverage correction de69626 is included in full regression. Post-wave schema/codebase/UI gates have no blockers.
- Plan 03-03 interface/pin amendment independently re-reviewed PASS (78c6755). Its standalone validator must bind source_manifest, sanitized/enriched records and coverage to independent references, then regenerate comparisons, claims, opening selection and limitations; coherent caller digest repinning is not approval. Begin final analysis wave; publication remains Phase 4/5.

- Remote checkpoint `fbb55d4` failed both CI runs because its ancestry included the Plan 03-03 RED test commit before the corresponding claims module. The exact failure is `ModuleNotFoundError: brujula.claims_v2` in test collection. The GREEN implementation exists locally; publish the next verified complete wave and recheck Windows/Ubuntu. Failed runs: https://github.com/erickinorganico/career-signals-mx/actions/runs/35897383161 and https://github.com/erickinorganico/career-signals-mx/actions/runs/35897378621 .
- Independent completed-core review found CR-01: a coherently altered quarter-native geography alias and its matching catalog hash can pass the comparator. T-03-06 is reopened until the exact snapshot signature is independently enforced and re-reviewed. Claims execution continues on separate files.

- Final comparator integration fixes distinguish same-period contrast caveats from longitudinal changes (`d1515e6`) and independently pin chronology/geographic concept (`11ae33b`). RED reproductions are preserved in `80f5b39`/`76ed82a`; 28 focused tests and two Node controls pass. Valid source values/IDs remain unchanged. The final code reviewer is checking the corrected delta and the active claims/packet implementation.

- After `11ae33b`, canonical p3/p4 producers again return green, located=true, flagged=false, with violation-fixture proof. Unchanged p1/p2 evidence remains current. All four receipts live under `.cache/research/phase3-final-controls/`. Independent reviewer reran the period/concept reproductions and confirmed both now block; its final report retains the resolved history.

- Final persisted-artifact review found CR-02: JSON serialization changed metric_dictionary_refs signatures from tuples to arrays, so a saved packet failed standalone validation even though the in-memory packet passed. RED commit159cbcb reproduces the actual round trip. Root is changing only the public signature representation to JSON-native arrays, preserving the serialized numerical content and trusted pins; acceptance remains blocked until focused, real reload and independent checks pass.

- Final source a6696a1 passes 443 Python tests (zero skips) plus the final eight-case edge suite (one additional case after collection), covering444 distinct tests. All seven canonical producers are green/located/not flagged, with current violation-fixture proof; all three summary checks pass. The stored packet validates after JSON reload with unchanged content/file hashes. Independent 03-REVIEW is clean, Nyquist maps31/31 edge criteria, and ASVS L1 register has15 closed entries and0open. Aggregate-only durable evidence: docs/evidence/phase-03-analysis-acceptance.json. Schema/codebase/UI/API gates are nonblocking; final goal verification and canonical phase closure remain required.
