---
phase: 04-offline-publication-and-reproducible-operation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brujula/resources.py
  - pyproject.toml
  - brujula/oracle/enoe_survey_oracle.R
  - brujula/assets/fonts/DejaVuSans.ttf
  - brujula/assets/fonts/DejaVuSans-Bold.ttf
  - brujula/assets/fonts/DejaVuSerif.ttf
  - brujula/assets/fonts/DejaVuSerif-Bold.ttf
  - brujula/assets/fonts/LICENSE_DEJAVU
  - tests/test_installed_runtime.py
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "The installed wheel resolves approved authored catalogs, strict schemas, golden aggregate baseline and exact R oracle outside the checkout."
    - "Source ZIPs, attempt receipts, workbook, benchmark PDF, R library and run outputs remain explicit external paths and never enter the wheel."
    - "The synthetic v1 CLI and regression suite retain their existing meaning; missing package resources fail closed."
    - "OPS-01 adjacency: Acquisition refresh, numerical acceptance, analytical build and publication replay retain separate source, acceptance and run identities."
    - "OPS-01 empty: Missing approved snapshot/current receipt, R, benchmark or required package resource fails with a receipt and explicit reason, never a fixture fallback."
    - "OPS-01 ordering: Replaying identical eight approved inputs preserves canonical numerical/public content regardless of run IDs and timestamps."
    - "OPS-01 idempotency: Repeated installed CLI replay of the same frozen snapshots creates distinct immutable receipts with equal canonical numerical content."
    - "OPS-01 concurrency: Competing or interrupted operations obey the documented source/output lock boundaries and cannot certify mixed-input content."
  artifacts:
    - path: brujula/resources.py
      provides: "Typed package-owned resource paths and SHA-256 inventory"
    - path: pyproject.toml
      provides: "Wheel data and reviewed pdf extra"
    - path: tests/test_installed_runtime.py
      provides: "Outside-checkout resource and failure controls"
  key_links:
    - from: brujula/resources.py
      to: pyproject.toml
      via: "importlib.resources reads shipped authored files"
    - from: pyproject.toml
      to: brujula/resources.py
      via: "wheel ships every typed authored resource and audited font notice"
---

<objective>
Make authored numerical resources and PDF capability portable in a clean installed wheel.
Purpose: installed real acceptance cannot rely on checkout scripts or hidden caches.
Output: package resources, licensed font/PDF dependency declaration and outside-checkout resource tests.
</objective>

## Artifacts this phase produces

| Path / symbol | Exact contract for downstream plans |
|---|---|
| `brujula/resources.py::snapshot_catalog_path(), metric_catalog_path(), aggregate_golden_path(), analysis_reference_path(), coverage_pins_path(), oracle_script_path(), font_path(name), authored_resource_digests()` | Typed installed-wheel paths and SHA-256 map; absent/unshipped resource raises; `font_path` accepts only four audited DejaVu names and license notice. Coverage/reference/golden remain package-owned. |
| `brujula/oracle/enoe_survey_oracle.R` | Byte-identical authored copy of accepted `scripts/enoe_survey_oracle.R`; new executing-code hash inventory in 04-02 names this actual resource. |
| `brujula/assets/fonts/{DejaVuSans.ttf,DejaVuSans-Bold.ttf,DejaVuSerif.ttf,DejaVuSerif-Bold.ttf,LICENSE_DEJAVU}` | Audited local faces and complete unchanged notice for HTML/PDF/figure generation. |
| `pyproject.toml` | `pdf` extra pins `weasyprint==70.0`; wheel package-data includes typed authored resources/fonts, excludes source ZIPs, workbook/PDF, person rows, R binary/cache. |
| `tests/test_installed_runtime.py` | Outside-checkout resource inventory, missing-resource, native PDF capability and v1 synthetic regression checks. |

<assumption_delta_decision>no-change: detector matched alternative text only. Primary analytical identity remains the existing ten-key v2r grain; alternative text is a representation, not a new entity.</assumption_delta_decision>

<execution_context>
@${GSD_CORE}/workflows/execute-plan.md
@${GSD_CORE}/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/ROADMAP.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-CONTEXT.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-RESEARCH.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-PATTERNS.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-UPSTREAM-PREFLIGHT.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-EDGE-PROBE.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Package exact authored resources</name>
  <files>brujula/resources.py, brujula/oracle/enoe_survey_oracle.R</files>
  <behavior>Outside checkout, typed accessors resolve approved source/metric catalogs, aggregate golden, Phase 3 analysis reference, v2 schemas and byte-identical R oracle; missing resources raise.</behavior>
  <action>Copy the authored R oracle byte-for-byte into brujula/oracle and expose snapshot_catalog_path, metric_catalog_path, aggregate_golden_path, analysis_reference_path, coverage_pins_path, oracle_script_path, font_path(name) and authored_resource_digests through importlib.resources. Leave checkout scripts and their existing callable signatures untouched until 04-02 moves the implementation; do not hash a future wrapper as numerical implementation. Do not package ZIPs, person frames, official workbook/PDF, R binaries or local cache. Use 04-RUNTIME-PATH-MAP.md and retain historical Phase 2 hash inventory untouched.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_installed_runtime.py -q</automated></verify>
  <done>Wheel resource inventory resolves outside checkout and absent resource fails.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Declare and verify portable PDF and resource packaging</name>
  <files>pyproject.toml, tests/test_installed_runtime.py, brujula/assets/fonts/DejaVuSans.ttf, brujula/assets/fonts/DejaVuSans-Bold.ttf, brujula/assets/fonts/DejaVuSerif.ttf, brujula/assets/fonts/DejaVuSerif-Bold.ttf, brujula/assets/fonts/LICENSE_DEJAVU</files>
  <behavior>A clean wheel contains the named authored resources and DejaVu candidates/complete notice after license review; a missing PDF extra/native capability reports a failure, not a success. Existing synthetic package behavior remains green.</behavior>
  <action>Add reviewed weasyprint==70.0 pdf extra and package-data globs for authored oracle, schemas, catalogs and complete DejaVu Sans/Serif regular/bold plus LICENSE_DEJAVU. Verify exact candidate hashes and complete license in .planning/research/FONT-ASSET-AUDIT.md and dependency identity in 04-RESEARCH.md before copying. Record package resource digests and reject missing resource/font/Pango capability at installed runtime. Do not alter machine-wide PATH; document explicit Windows DLL directory/Fontconfig and Ubuntu Pango prerequisites in runtime help/tests. Build a fresh wheel, install it into isolated outside-checkout env using reviewed local wheels, and assert resource lookup, version and synthetic v1 smoke.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_installed_runtime.py tests/test_cli.py -q</automated></verify>
  <done>Fresh wheel passes outside-checkout resource/fixture checks and has no unreviewed inputs or font notices missing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Installed wheel → local source root | Only approved receipts and pinned ZIPs may be read. |
| PDF runtime → local assets | Native loader/font assets are explicit and local. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Tampering | package resource lookup | mitigate | Hash exact shipped catalogs, schema, oracle and font assets. |
| T-04-02 | Information disclosure | wheel contents | mitigate | Inspect wheel inventory to exclude ZIP, person rows, credentials and caches. |
| T-04-SC | Tampering | PyPI/Pango install | mitigate | Use reviewed WeasyPrint 70.0 registry identity, fixed version and local clean install; no assumed package. |
</threat_model>

<verification>
Build wheel and run tests/test_installed_runtime.py outside checkout; record wheel and resource SHA-256, license inventory, missing-resource failure and v1 synthetic regression.
</verification>

<success_criteria>
All authored resources are available in an installed wheel; external research inputs remain explicit and absent resources fail with a receipt.
</success_criteria>

<output>
Create .planning/phases/04-offline-publication-and-reproducible-operation/04-01-SUMMARY.md after execution.
</output>
