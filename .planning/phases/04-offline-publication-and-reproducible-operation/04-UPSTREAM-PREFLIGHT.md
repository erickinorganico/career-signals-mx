# Phase4 accepted upstream readback

Phase3 canonical `query phase.complete 3` passed on 2026-09-23 with no warnings. Independent `03-VERIFICATION.md` proves42/42 plan truths,4/4 roadmap criteria,6/6 ANA requirements and7/7 prohibitions. Its source review is clean; Nyquist covers31/31 edges; security has15 closed dispositions and0 open threats. Source fix a6696a1 and complete checkpoint59451d3 are verified; Windows and Ubuntu CI both passed at59451d3 (runs35904257228 and35904250523).

The actual handoff is `.cache/research/phase3-analysis/analysis.json`, loaded from disk then passed through `validate_analysis_packet` independently by root, reviewer and verifier with zero errors. Its content digest is `d2f28f26f993d052fb5f246db3262ad00b776081a663d608e0427962745a0c79`; fileSHA256 `9bbb5af3091c4cb02bec98d499ac522d77715f3a1316a30f906364fcc4c6537d`. It contains6739 sanitized records,4209 comparisons (2120 supported,2089 blocked with null changes),38 canonical claims and3 opening IDs. All5 complementary parents remain null. The durable aggregate-only receipt is `docs/evidence/phase-03-analysis-acceptance.json`.

Use `build_analysis_packet(public_by_snapshot, flat_acceptance, coverage_audits, definition_registry)` and `validate_analysis_packet(packet)` as implemented. The packet contains `record_index`, `profiles`, `coverage`, `comparisons`, `claims`, `opening_claim_ids`, `limitations`, `source_manifest` and `content_digest`, plus schema_version. Public comparison signature arrays are JSON-native after CR-02; a persisted round trip is now a required test. No original Phase2 public projection may restore complementary redactions.

The analytical input is accepted, but moving numerical implementation/resources into a wheel changes the code-inventory boundary. Follow `04-RUNTIME-PATH-MAP.md`: explicit source/output/audit/reference/R paths; authored resources from the wheel; independently hash the actual executing implementation; fresh installed numerical acceptance and unchanged replay before deliberately rebinding trusted analysis references. Preserve historical Phase2/3 evidence; do not bypass hashes, regenerate a golden from an unvalidated caller artifact or silently skip the real gate. Clean fixture/resource/PDF checks run on Windows and Ubuntu. Full real numerical installed acceptance/replay runs in the designated local Windows research environment; it is not duplicated across both operating systems.
