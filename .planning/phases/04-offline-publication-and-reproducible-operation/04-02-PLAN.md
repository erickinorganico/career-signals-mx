---
phase: 04-offline-publication-and-reproducible-operation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - brujula/enoe_acceptance.py
  - brujula/official_reconciliation.py
  - scripts/accept_enoe_estimates.py
  - scripts/official_reconciliation.py
  - brujula/analysis_v2.py
  - brujula/findings_v2.py
  - data/fixtures/enoe-analysis-reference.json
  - tests/test_installed_acceptance.py
  - tests/test_official_reconciliation.py
  - tests/test_analysis_integration.py
  - tests/test_analysis_v2.py
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "The installed implementation, not a checkout wrapper, passes fresh frozen eight-ZIP numerical acceptance, independent R survey comparison and official reconciliation."
    - "An unchanged real offline replay from installed code reproduces accepted numeric digest and public rows before any trusted Phase 2/3 reference is rebound."
    - "The actual executing package modules and oracle have a new explicit hash inventory; historical Phase 2/3 receipts remain immutable."
    - "Missing R, survey library, workbook, PDF, source receipt or resource creates a failed attempt receipt and never falls back to synthetic data."
    - "OPS-01 adjacency: Acquisition refresh, numerical acceptance, analytical build and publication replay retain separate source, acceptance and run identities."
    - "OPS-01 empty: Missing approved snapshot/current receipt, R, benchmark or required package resource fails with a receipt and explicit reason, never a fixture fallback."
    - "OPS-01 ordering: Replaying identical eight approved inputs preserves canonical numerical/public content regardless of run IDs and timestamps."
    - "OPS-01 idempotency: Repeated installed CLI replay of the same frozen snapshots creates distinct immutable receipts with equal canonical numerical content."
    - "OPS-01 concurrency: Competing or interrupted operations obey the documented source/output lock boundaries and cannot certify mixed-input content."
  artifacts:
    - path: brujula/enoe_acceptance.py
      provides: "Installed frozen numerical acceptance and replay"
    - path: brujula/official_reconciliation.py
      provides: "Installed workbook/PDF reconciliation"
    - path: tests/test_installed_acceptance.py
      provides: "Installed real and negative path controls"
  key_links:
    - from: brujula/enoe_acceptance.py
      to: brujula/resources.py
      via: "typed resources and executing-code digest"
    - from: brujula/findings_v2.py
      to: data/fixtures/enoe-analysis-reference.json
      via: "trusted pin rebind only after fresh real acceptance"
    - from: brujula/analysis_v2.py
      to: brujula/resources.py
      via: "fixed installed implementation inventory and independent package golden pins"
---

<objective>
Establish fresh numerical trust for the moved installed runtime and rebind the accepted analytical reference only after proof.
Purpose: moving the eleven hashed numerical paths changes which code and resources execute.
Output: installed acceptance/reconciliation, immutable new proof and validated Phase 3 handoff.
</objective>

## Artifacts this phase produces

| Path / symbol | Exact contract for downstream plans |
|---|---|
| `brujula/enoe_acceptance.py::accept(output_root, audit_dir, *, source_root, workbook, pdf, rscript, r_home, r_lib, generate_golden=False)` | Frozen real acceptance; source/output/audit are distinct local roots; default denies golden generation; receipt includes actual module/oracle/resource hashes, R identity, source IDs, numeric/public digests. |
| `brujula/enoe_acceptance.py::replay(source_root, sealed_run, audit_dir, *, rscript, r_home, r_lib)` | Read-only offline replay validates current eight source receipts and compares exact rows plus existing numeric digest; emits distinct immutable attempt receipt. |
| `brujula/official_reconciliation.py::{workbook_cells,reconcile,check_2026_pdf_benchmark}` | Retain old callable signatures; explicit caller workbook/PDF paths hash-checked before official comparison. |
| `scripts/{accept_enoe_estimates.py,official_reconciliation.py}` | Thin compatibility wrappers retaining prior script callables; neither is a substitute for hashing package implementation. |
| `data/fixtures/enoe-analysis-reference.json`, `tests/test_analysis_integration.py` | New trusted analysis reference only after installed real proof; historical acceptance explicitly rejected for the moved runtime, final real lane consumes new acceptance. |
| `brujula/analysis_v2.py::_required_code_files(), _check_codes(manifest), _approved_public_pins()` | Fixed versioned installed inventory: `brujula/enoe_acceptance.py`, `brujula/oracle/enoe_survey_oracle.R`, `brujula/official_reconciliation.py`, and the eight unchanged numerical package paths from the original eleven-file inventory. Resolve package-owned modules and oracle through `brujula.resources`/installed package location, normalize CRLF to LF before SHA-256, compare against fresh immutable installed acceptance; historical `scripts/*` inventory rejects. Golden public pins load only through `aggregate_golden_path()`, independently hashed by accepted resource inventory, never from caller manifest or checkout fallback. |
| `tests/test_analysis_v2.py`, `tests/test_analysis_integration.py` | Ordered installed subprocess gate: positive `index_public_estimates` with `_check_codes` active; private test-only `findings_v2._assemble` yields a candidate whose `validate_analysis_packet` has exactly `trusted_reference` against the old pin, and production `build_analysis_packet` rejects it; after unchanged installed acceptance/replay and intentional versioned reference rebind, production `build_analysis_packet` succeeds and persisted JSON reload validates with zero errors. Also reject old eleven-path manifest, changed module/oracle, missing/altered golden, attempted caller repin and checkout fallback. |

<assumption_delta_decision>no-change: alternative text does not alter the existing ten-key v2r analytical identity.</assumption_delta_decision>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/ROADMAP.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-CONTEXT.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-RESEARCH.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-PATTERNS.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-UPSTREAM-PREFLIGHT.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-EDGE-PROBE.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move acceptance and official reconciliation into package services</name>
  <files>brujula/enoe_acceptance.py, brujula/official_reconciliation.py, scripts/accept_enoe_estimates.py, scripts/official_reconciliation.py, tests/test_installed_acceptance.py, tests/test_official_reconciliation.py</files>
  <behavior>Installed entry points resolve explicit source/output/audit/workbook/PDF/R paths, run the same frozen numerical and official gates, and record package/R/resource identity; missing prerequisites leave failed receipts.</behavior>
  <action>Move actual implementations from scripts/accept_enoe_estimates.py and scripts/official_reconciliation.py into package modules, preserving accept(output_root,audit_dir,*,generate_golden=False), reconciliation callables and thin script compatibility. Add explicit source_root, workbook, pdf, rscript/r_home/r_lib configuration without changing approved numeric algorithms, golden public/official pins or tolerance rules. R resolution order is explicit BRUJULA_RSCRIPT/argument, configured R home, then shutil.which; validate executable/version, use explicit library only, package-owned oracle, no checkout cache/PATH mutation. Installed acceptance receipts name exact source, output, audit, workbook/PDF SHA, R and survey identity, package/resource versions and actual executing code inventory. Reject generate_golden in ordinary accept/replay; never auto-repin. Make the module runnable outside checkout and preserve failure receipt/current semantics.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_installed_acceptance.py tests/test_official_reconciliation.py -q</automated></verify>
  <done>Installed package performs frozen numerical and official checks with explicit roots and fails closed on every required missing dependency.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Run real installed acceptance and unchanged offline replay before reference changes</name>
  <files>brujula/analysis_v2.py, brujula/findings_v2.py, data/fixtures/enoe-analysis-reference.json, tests/test_installed_acceptance.py, tests/test_analysis_v2.py, tests/test_analysis_integration.py</files>
  <behavior>The newly installed wheel executes the existing eight accepted ZIPs/current receipts and R survey oracle, reproduces accepted numerical content and exact public records. Before reference rebind, the guarded installed public index passes but a candidate Phase 3 packet fails specifically at trusted_reference; after deliberate rebind, the production packet and persisted JSON validate.</behavior>
  <action>First migrate analysis_v2._required_code_files and _check_codes from ROOT/EXPECTED_CODE_FILES to the exact versioned installed implementation inventory named above: package-owned module locations plus packaged R oracle, never caller-supplied paths or silently discovered checkout files. Normalize CRLF to LF before SHA-256 exactly as the frozen gate did; the acceptance producer and analysis guard must use identical canonical byte rules. Replace analysis_v2._approved_public_pins ROOT/data/fixtures lookup with aggregate_golden_path() and bind that package resource to the independently accepted golden digest, not a caller-provided pin. Preserve package-portable source registry, metric, coverage and findings reference loaders; make absence/mutation of any resource fatal. Second, in designated local Windows research environment install wheel outside checkout, execute complete eight-snapshot acceptance with existing source receipts, reviewed workbook/PDF and verified R survey, then execute read-only offline replay; require unchanged numeric digest 8db575e9d664864d1513e3b9658bd9b060c4cb3bed8b51561f208adeb97b9a00 and exact public rows, or stop. Third, before reference rebind, run installed subprocess index_public_estimates over those accepted public roots with _check_codes and independent golden active and require success. In test harness only, assemble the new candidate using findings_v2._assemble; require validate_analysis_packet to return exactly trusted_reference against the historical reference and require production build_analysis_packet to reject. This expected rejection is a guard proof, not accepted output; never bypass/disable the reference check or publish the candidate. Fourth, after fresh numerical acceptance plus unchanged replay and those pre-rebind checks, deliberately write a versioned new hash-only Phase 3 reference from the independently accepted source/coverage/public inventory while preserving old reference/receipts as history. Fifth, run positive installed subprocess build_analysis_packet with the guard enabled, serialize/reload JSON and require validate_analysis_packet == [] plus unchanged 6739 sanitized records, 4209 comparisons, 38 claims and three opening IDs; legitimate differences need explicit reconciliation and review. tests/test_analysis_v2.py and tests/test_analysis_integration.py additionally reject old checkout-code manifest, changed executing module/oracle, missing/altered package golden, caller repinning and checkout fallback. No skip, golden regeneration, caller-pins-as-authority or other bypass. Record new immutable proof and source IDs separately from attempt clocks.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_installed_acceptance.py tests/test_analysis_integration.py tests/test_analysis_v2.py -q</automated></verify>
  <done>Fresh installed numerical acceptance and unchanged offline replay pass; guarded public indexing succeeds and historical trusted reference rejects the new candidate before rebind; after intentional versioned rebind, installed production packet and persisted JSON validate and feed Phase 4.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Installed service → pinned ZIP/receipt root | Local input requires exact current acquisition proof. |
| Installed service → external R/workbook/PDF | External tools and benchmarks require explicit paths and hashes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-03 | Tampering | installed numerical code | mitigate | Hash actual executing package modules/oracle, then rerun full frozen acceptance and replay. |
| T-04-04 | Spoofing | prior trusted references | mitigate | Rebind only after unchanged digest and row-level proof; preserve old receipts. |
| T-04-05 | Denial of service | missing R/benchmark/source | mitigate | Fail attempt with receipt and no synthetic fallback. |
</threat_model>

<verification>
Use installed wheel outside checkout for one full real eight-ZIP acceptance and read-only replay. Before rebind, require positive guarded `index_public_estimates` and exact expected `trusted_reference` rejection from candidate validation plus production builder rejection. After independently justified versioned rebind, require positive installed production `build_analysis_packet`, persisted JSON reload and `validate_analysis_packet == []`. Reject historical manifest, changed module/oracle, missing/altered golden and caller repinning; preserve old receipt hashes.
</verification>

<success_criteria>
New executing code identity has fresh proof and unchanged numerical/public content; no historical evidence or golden pin is overwritten.
</success_criteria>

<output>
Create .planning/phases/04-offline-publication-and-reproducible-operation/04-02-SUMMARY.md after execution.
</output>
