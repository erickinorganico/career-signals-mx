---
phase: 04
slug: offline-publication-and-reproducible-operation
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-23
---

# Phase 4 validation strategy

## Final 04-06 readback, 2026-09-24

The historical audit observations below were written before hosted CI finished. Run [36050222714](https://github.com/erickinorganico/career-signals-mx/actions/runs/36050222714) completed successfully on Windows and Ubuntu at commit `3b8b2d4e603d69f9d94f5ac45f6446156a17d14f`. Both downloaded installed-helper receipts report `PASS`; SHA-256 values and exact fixture/PDF scope are in `docs/evidence/phase-04-publication-acceptance.json`. `04-06-SUMMARY.md` is present. Together with the observed 45 edge mappings, four bad-red/clean-green prohibition producers and local full suite, these satisfy this plan's Nyquist evidence. Independent Phase 4 verification is still a separate gate.

This document retains the original planning map below and appends observed Phase 4 validation. Phase 3 was independently accepted at source a6696a1 and checkpoint 59451d3: 42/42 truths, persisted packet validation zero errors, Windows/Ubuntu CI passed. The accepted packet is `.cache/research/phase3-analysis/analysis.json` and its public-safe durable receipt is `docs/evidence/phase-03-analysis-acceptance.json`. Phase 4 reestablished numerical trust after installed-code/resource relocation before rebinding that packet's references. Phase 4 remains in progress until final native CI and the integrated summary are read back; the current public-safe acceptance receipt explicitly says `LOCAL_PASS_PENDING_CI`.

## Gate sequence and ownership

| Gate | Plan | Required proof |
|---|---|---|
| Wheel resource custody | 04-01 | Outside-checkout wheel resolves catalogs, schema, golden, oracle, fonts and notice; exact SHA inventory; no ZIP/person rows/cache; missing resource fails. |
| Installed numerical authority | 04-02 | Fresh full eight-ZIP acceptance, R survey oracle and official workbook/PDF reconciliation in designated Windows research environment; unchanged digest `8db575e9d664864d1513e3b9658bd9b060c4cb3bed8b51561f208adeb97b9a00` and exact public rows; read-only replay; then deliberate reference rebind and persisted Phase 3 validation. |
| Public-only model and exports | 04-03 | Exact 23 metric IDs, sanitized 6,739-record `v2r:` index, canonical joins, null/status/precision/provenance across CSV/Parquet/DuckDB; complement and nested-sentinel negatives. |
| Editorial and visual | 04-04 | Question-led Spanish complete report, HTML/MD/PDF semantic parity, paired SVG/PNG/table points and missingness; actual mobile/desktop/200%/A4 inspection and PDF accents/font/page evidence. |
| Sealed operation | 04-05 | Stage fault matrix, exact manifest and hashes, eight current acquisitions verified on each read; failed refresh invalidates access while history stays immutable; installed CLI replay compares content separately from clocks. |
| Integrated Phase 4 acceptance | 04-06 | All 45 edge resolutions mapped to behavioral checks; four bad/clean current-output prohibition controls; full suite, real sealed bundle and cross-platform clean fixture/resource/PDF checks; aggregate-only receipt. |

Focused commands are the `<verify><automated>` entries in each plan; the executor must run each after its task. The full `.venv/Scripts/python.exe -m pytest -q` suite follows affected fixes and before phase closure. `node --test tests/phase4_prohibitions.test.cjs` and the canonical GSD prohibition producer must show a current clean pass and targeted bad failure for each descriptor. A prior green result on changed inputs is not evidence. Tests use disposable local fixtures with no production access. The full real R survey workload runs in 04-02 in the designated Windows research environment. Plan 04-06 reuses its immutable evidence when the numerical implementation/resources, eight inputs, R/benchmarks and method inventory are exact matches; any change across that boundary requires fresh installed numerical acceptance/replay. Windows and Ubuntu both require installed-wheel fixture/resource/failure and small offline Spanish PDF checks, while 04-06 always runs new real publication/CLI/format integration.

`tests/test_analysis_integration.py` currently reads the historical Phase 2 acceptance and requires checkout hashes. After the move, that manifest must fail the new executing-code guard; this expected rejection is a transition check, not acceptance. The real installed lane consumes the new acceptance from the explicit audit root and keeps `_check_codes` enabled. Before any trusted-reference rebind, `index_public_estimates` must pass from the installed package; a test-only `findings_v2._assemble` candidate must yield exactly `trusted_reference` from `validate_analysis_packet` against the historical pin, while production `build_analysis_packet` rejects it. After unchanged installed numerical acceptance and replay, deliberately rebind a versioned independent hash-only reference. Then production `build_analysis_packet` must pass in an installed subprocess, and its persisted JSON reload must validate with zero errors, no skip or caller-supplied pin override. The expected pre-rebind rejection is never counted as accepted publication.

The installed guard itself must migrate: `analysis_v2._required_code_files` names a fixed versioned map of the actual package acceptance/reconciliation modules, packaged R oracle and eight unchanged numerical modules. `_check_codes` resolves those package-owned paths without `ROOT/scripts` or caller-provided path authority, normalizes CRLF to LF identically to the acceptance producer and compares exact SHA-256. `_approved_public_pins` reads the independently accepted package golden through `aggregate_golden_path()`; coverage and analysis references remain package-owned. Tests require positive guarded public indexing before rebind, positive installed production packet build after rebind, and targeted rejection of historical inventory, changed module/oracle, missing/altered golden and caller repinning.

## Exact edge mapping

`04-EDGE-PROBE.json` has 45 explicitly resolved *planning criteria*: PUB-01 six, PUB-02 five, PUB-03 six, PUB-04 seven, PUB-05 six, OPS-01 five, OPS-02 five, OPS-03 five. Each `resolution` string is copied verbatim to a flat `must_haves.truths` entry in its owning plan: PUB-01–03 → 04-04, PUB-04–05 → 04-03, OPS-01 → 04-01 and 04-02, OPS-02–03 → 04-05. Plan 04-06 repeats all 45 as an integrated gate. Resolution here means the criterion is specified, not that behavior passed. Before closure, `tests/test_phase4_edge_acceptance.py` must map each exact (requirement_id, category) to an executable assertion and `docs/evidence/phase-04-publication-acceptance.json` must record its observed result.

The four structured prohibitions in 04-06 are likewise planned. The GSD producer must locate the Node target, run it against current generated output, observe targeted bad-fixture red and clean-fixture green, and confirm content causation. These checks may become green only after execution; do not set `nyquist_compliant: true` from the plan.

## Format and visual evidence

Tests must read real emitted format values and keys: parse CSV, query DuckDB and Parquet, extract PDF text, inspect SVG text/metadata/path render and PNG metadata/render, and compare figure/table/alt content to canonical public row or accepted comparison. A file-exists check alone cannot establish parity. The intentionally suppressed internal point and nested weighted denominator/support/SE/CV/CI sentinels must be absent in each output and encoded representation; the five accepted complementary parent redactions must remain null wherever repeated. Direct measures can only bind to public records and descriptive changes to the validated Phase 3 ledger.

Visual review inspects actual real HTML at desktop, mobile width and 200% zoom, then A4 PDF cover, long tables, profiles, sparse states, source pages and section transitions. Record screenshot/page references, observed issues, fix and recheck in `docs/visual/phase-04-visual-review.md`. PDF text search, Spanish glyphs, repeated headers, page footers and embedded font status need explicit evidence. SVG glyph paths ensure standalone figures render without reader-installed fonts; semantic title/description and table alternatives retain text accessibility. Do not claim PDF/UA or full screen-reader conformance.

The offline font route is exact: 04-01 packages four audited DejaVu TTFs plus complete notice; 04-04 verifies and copies them to staged `assets/fonts/` under the run and emits CSS `@font-face` references only to those relative files; its `LocalOnlyFetcher` allowlists that validated staged inventory only. 04-05 hashes all five font assets in the exact manifest and rechecks them on current resolution; 04-06 reads the actual sealed assets and clean installed HTML/PDF on both operating systems. Missing/altered font, notice, copied byte, traversal or external filesystem request blocks rather than selecting a system font silently.

## Four-source coverage audit

| Source | Item | Plans | Status |
|---|---|---|---|
| GOAL | Consistent evidence-bound offline outputs from sealed replayable, fail-closed run | 04-03–06 | COVERED |
| REQ | PUB-01 Spanish question-led full-scope editorial report | 04-04, 04-06 | COVERED |
| REQ | PUB-02 same-run offline HTML/MD/PDF | 04-04–06 | COVERED |
| REQ | PUB-03 SVG/PNG/semantic tables and visual legibility | 04-04, 04-06 | COVERED |
| REQ | PUB-04 CSV/Parquet/DuckDB complete public keys and dictionary | 04-03, 04-06 | COVERED |
| REQ | PUB-05 one sanitized projection and no suppressed resurfacing | 04-03–06 | COVERED |
| REQ | OPS-01 explicit refresh/installed offline replay and numeric determinism | 04-01, 04-02, 04-05–06 | COVERED |
| REQ | OPS-02 sealed receipt/manifest before current, failure history | 04-05–06 | COVERED |
| REQ | OPS-03 live eight-acquisition/manifest verification on every read | 04-05–06 | COVERED |
| RESEARCH | Installed resource/wrapper migration, actual code hash inventory and fresh numerical acceptance | 04-01–02 | COVERED |
| RESEARCH | Single validated Phase 3 sanitized record_index with v2r/v2c/v2k identities | 04-03 | COVERED |
| RESEARCH | Complete editorial sections, paired figures/tables, offline secure PDF and DejaVu license | 04-01, 04-04 | COVERED |
| RESEARCH | Typed public exports, CSV formula protection and provenance keys | 04-03 | COVERED |
| RESEARCH | Exact v2 run manifest, eight live acquisitions, faults and replay identity split | 04-05–06 | COVERED |
| RESEARCH | Cross-platform fixture/PDF and actual visual/readback acceptance | 04-04, 04-06 | COVERED |
| CONTEXT | One validated v2 projection; no diagnostic estimate or complement/format reconstruction | 04-03–06 | COVERED |
| CONTEXT | Questions, up to three findings, all profiles/periods/states/sex/other fields, limits and sources | 04-04 | COVERED |
| CONTEXT | Print typography, visual/accessibility checks, offline font assets, no network | 04-01, 04-04, 04-06 | COVERED |
| CONTEXT | Synthetic v1 regression and explicit real CLI/source/output roots | 04-01, 04-05 | COVERED |
| CONTEXT | All required acquisition currents checked before build/open; later failure invalidates current | 04-05 | COVERED |
| CONTEXT | Final seal before current; failure receipt/history; same numeric content across replay | 04-02, 04-05–06 | COVERED |
| CONTEXT | Stable ten-key join, separate dimensions, null/status/provenance and public-only release bundle | 04-03, 04-05–06 | COVERED |

`04-CONTEXT.md` has locked decisions but no D-NN identifiers; each decision group above is traced by subject. Deferred GitHub release, bilingual onboarding and independent final audit belong to Phase 5. New sources, deflation, hosting, paid inference, personal advice, ranking and public microdata are out of scope. No source item is omitted for implementation convenience.

## Exit

Phase 4 stays blocked until fresh installed numerical proof, persisted analytical readback, all cross-format/fault/edge/prohibition checks, real visual inspection and public-safe evidence pass. Phase 5 alone decides release publication.

## Executed Nyquist audit (2026-09-24 UTC)

The preceding gate and four-source tables are the historical **plan-coverage** map, not a retroactive claim that every gate passed. The table below records executed behavior and the remaining evidence boundary. No implementation files were changed by this audit.

| Plan / requirement | Behavioral command or readback | Observed result | Audit status |
|---|---|---|---|
| 04-01 / OPS-01 | `tests/test_installed_runtime.py`, outside-checkout wheel/resource/PDF checks; `04-01-SUMMARY.md`; final installed helper in `04-INTEGRATED-OPERATION.md` | Current `c222f69` installed chain identifies 24 authored resources, 11 fixed numerical modules/oracle, seven fixed numerical resources, and a local searchable PDF; absent resource and altered font fail. Earlier 22-resource count in 04-01 was its then-current inventory, not the final inventory. | FILLED locally; cross-platform helper pending |
| 04-02 / OPS-01 | `tests/test_installed_acceptance.py tests/test_official_reconciliation.py tests/test_analysis_integration.py`; `04-02-SUMMARY.md` immutable acceptance/replay receipts | Actual installed eight-ZIP acceptance and full recomputation replay matched numerical digest `8db575e9d664864d1513e3b9658bd9b060c4cb3bed8b51561f208adeb97b9a00`, public rows, 26 R oracle cases, four analytical R cases and official workbook/PDF checks. Pre-rebind guarded rejection and post-rebind persisted packet validation were observed. | FILLED; source/method inventory unchanged afterward |
| 04-03 / PUB-04, PUB-05 | `tests/test_publication_v2.py tests/test_export_v2.py`; `04-WAVE1-CHECKS.md`; `04-INTEGRATED-OPERATION.md` | The real sealed output contains 6,739 public records, 4,209 comparisons, 38 claims and 22 typed tables across CSV/Parquet/DuckDB. The production model accepted reversed insertion order without a pin or function patch; all 22 CSV byte streams, typed Parquet/DuckDB rows and nine figure memberships/pixel buffers matched. The nonempty join readback checked 249 figure-record, 133 figure-comparison, three figure-claim, 16 figure-source and 65 claim-record links (`.cache/research/phase4-real-permutation/readback.json`, SHA-256 `68a65608a03483a0cb5c013c59cd7a88c114b4f53a57326ee2aeaca3d1111c2c`). | FILLED for checked projection/export behavior |
| 04-04 / PUB-01, PUB-02, PUB-03 | `tests/test_report_v2.py tests/test_pdf_v2.py`; `docs/visual/phase-04-visual-review.md`; `04-RENDERER-CODE-REVIEW.md` | Real report readback found the same 625 selected canonical keys in HTML, Markdown and the 97-page PDF, with nine SVG/PNG pairs. Actual local browser inspection covered desktop 1440 px, mobile 390 px and 200% zoom; A4 page/table/figure, Spanish text and embedded fonts were inspected. The review makes no PDF/UA or screen-reader certification claim. | FILLED for stated local visual/format checks |
| 04-05 / OPS-01, OPS-02, OPS-03 | `tests/test_pipeline_v2.py tests/test_cli_v2.py`; `04-WAVE3-CHECKS.md`; `04-INTEGRATED-OPERATION.md` | Installed analyze/build/open/reconstruction replay exited zero without network calls. The latest recovery proof terminated an actual installed CLI with exit 17, recovered its interrupted run as immutable BLOCKED/FAILED, then built and opened a new 73-artifact sealed run. A later required-source failure in an isolated eight-ZIP custody copy made installed open fail while prior sealed artifacts remained unchanged. | FILLED for observed installed operation; exact CI matrix pending |
| 04-06 / all eight IDs and 45 pairs | With the documented Windows native PDF variables, `.venv/Scripts/python.exe -m pytest tests/test_phase4_edge_acceptance.py -q --tb=short` | **46 passed in 78.48 s:** one exact pair-map assertion and all 45 behavioral pair assertions; no skips. The same command without `WEASYPRINT_DLL_DIRECTORIES`/`FONTCONFIG_FILE` failed 45 shared fixture setups because Pango could not load, then passed with the reviewed runtime configured. | FILLED locally with fixture limitations below |
| 04-06 / four prohibitions | With the same native variables, `node --test tests/phase4_prohibitions.test.cjs`; each `GSD_PROHIB_SUBJECT=tests/fixtures/phase4_prohibitions/<case>.bad.json` | Clean combined run: **5 passed in 19.87 s**. Four targeted bad runs (`suppressed`, `complement`, `pdf-fetch`, `stale-current`) each selected one computed subject and exited **1** at `active_subject_valid`, proving a red bad fixture. The public receipt records four located canonical GSD producers, all green with fail-first violation-fixture proof. | FILLED locally |
| 04-06 / integrated release gate | `.venv/Scripts/python.exe -m pytest -q`, clean Windows/Ubuntu installed-wheel jobs, `04-06-SUMMARY.md`, `docs/evidence/phase-04-publication-acceptance.json` | The executor reported 592 passes and one expected duplicate-ZIP warning for the full local suite. This auditor did not rerun that 496 s suite. The public receipt exists with status `LOCAL_PASS_PENDING_CI` and the local counts, while `hosted_ci` remains `PENDING` with null OS results. The final 04-06 summary was absent at this audit. | PENDING native CI and summary readback; do not mark compliant |

The focused edge command needs `WEASYPRINT_DLL_DIRECTORIES` pointed at the reviewed WeasyPrint 70 `_internal` directory and `FONTCONFIG_FILE` pointed at the local Fontconfig configuration. That dependency is part of the PDF runtime contract; the initial missing-DLL fixture error is not evidence that 45 publication behaviors failed. The second configured run is the recorded behavioral result.

### Coverage limits and independent controls

- `tests/test_phase4_edge_acceptance.py` runs all exact pairs against a disposable independently pinned synthetic packet. The zero-valued export, nonempty figure/claim links and associated render/export permutation checks bypass **only** `validate_publication_model` for their constructed fixture. They prove serializer, join and renderer behavior on nonempty data, but cannot prove that fixture is an independently accepted analytical packet. The unpatched installed 6,739-record permutation readback above supplies the complementary real-model proof. The probe's live-source mutation is a service double; the actual installed failed-acquisition/open result in `04-WAVE3-CHECKS.md` supplies the separate custody proof.
- `PUB-03 boundary`'s automated assertion checks responsive CSS and A4 dimensions; legibility is established by the actual browser/page observations in `docs/visual/phase-04-visual-review.md`. `OPS-03 concurrency` uses a verifier double to force differing first/second snapshots; the real resolver's repeated-check behavior is exercised, while a concurrent real eight-ZIP race is not claimed.
- The prohibition test computes bad and clean output in the same disposable execution. This audit observed four separate targeted bad-fixture red exits and the combined clean green exit. `docs/evidence/phase-04-publication-acceptance.json` separately records the canonical GSD producer result as PASS; hosted CI remains unverified.
- `04-INTEGRATED-CODE-REVIEW.md` originally recorded blockers in the Phase 4 prohibition/edge controls. Its rereview section reports focused corrections, but final source hashes and severity require the independent reviewer’s final readback. This audit does not silently close that review.

### Exact 45-pair behavioral map

The following rows reproduce each resolved `04-EDGE-PROBE.json` criterion and bind it to the current named assertion. The common command is `.venv/Scripts/python.exe -m pytest tests/test_phase4_edge_acceptance.py -q --tb=short` with the documented Windows PDF variables; every row passed in the 46-test run above. A passing pair is local behavioral evidence with the limits just stated, not cross-platform release acceptance.

| Requirement / category | Exact criterion | Test function | Observed |
|---|---|---|---|
| PUB-01 / boundary | PUB-01 boundary: Opening contains zero to three accepted claim IDs; a fourth, duplicated or unsupported opening claim blocks publication. | `pub01_boundary` | PASS |
| PUB-01 / adjacency | PUB-01 adjacency: Field, occupation, industry and population scopes remain distinct even when labels or numbers coincide; each sentence retains its accepted claim identity. | `pub01_adjacency` | PASS |
| PUB-01 / empty | PUB-01 empty: An empty supported-opening set produces the accepted limitation; suppressed profile cells remain visible as unavailable with reason. | `pub01_empty` | PASS |
| PUB-01 / encoding | PUB-01 encoding: Official Spanish labels, accents and source text survive output encoding; HTML/Markdown escaping does not replace the underlying evidence identity. | `pub01_encoding` | PASS |
| PUB-01 / ordering | PUB-01 ordering: Fixed question-led sections and field order are independent of input map order, and the eight quarterly sections remain chronological. | `pub01_ordering` | PASS |
| PUB-01 / precision | PUB-01 precision: Every displayed quantity uses a declared formatter over its exact public record or comparison, with nominal income, universe and nonofficial precision wording retained. | `pub01_precision` | PASS |
| PUB-02 / adjacency | PUB-02 adjacency: HTML, Markdown and extracted PDF refer to the same section/claim/figure/record keys and carry the same supported values and limitations. | `pub02_adjacency` | PASS |
| PUB-02 / empty | PUB-02 empty: Missing PDF capability, font or required local asset blocks the complete publication with a failure receipt; no blank-success or synthetic fallback. | `pub02_empty` | PASS |
| PUB-02 / encoding | PUB-02 encoding: Spanish accents and punctuation are searchable in the rendered PDF and match the semantic HTML/Markdown text. | `pub02_encoding` | PASS |
| PUB-02 / ordering | PUB-02 ordering: Section order and repeated table headings remain correct across page breaks and match the common editorial representation. | `pub02_ordering` | PASS |
| PUB-02 / concurrency | PUB-02 concurrency: Interrupted or concurrent rendering never promotes an incomplete set of HTML/Markdown/PDF artifacts as current. | `pub02_concurrency` | PASS |
| PUB-03 / boundary | PUB-03 boundary: Actual desktop, mobile-width and 200 percent zoom HTML and A4 PDF remain legible without clipped prose or figure labels; wide tables use contained scrolling. | `pub03_boundary` | PASS |
| PUB-03 / adjacency | PUB-03 adjacency: Trend lines never bridge an unavailable point or incompatible adjacent period; supported intervals and table rows remain attached to the same record. | `pub03_adjacency` | PASS |
| PUB-03 / empty | PUB-03 empty: Fully unavailable figure groups retain a clear unavailable explanation and complete semantic table, never an invented zero plot. | `pub03_empty` | PASS |
| PUB-03 / encoding | PUB-03 encoding: Figure titles, captions, alternatives and table header scopes preserve official Spanish labels, units, universe and source. | `pub03_encoding` | PASS |
| PUB-03 / ordering | PUB-03 ordering: SVG, PNG and accompanying tables use stable figure keys and declared classification/period order rather than an implicit outcome ranking. | `pub03_ordering` | PASS |
| PUB-03 / precision | PUB-03 precision: Plot points and intervals equal supported public values before formatting; null precision stays unavailable and no difference interval is inferred. | `pub03_precision` | PASS |
| PUB-04 / boundary | PUB-04 boundary: Zero, null and finite numeric precision round-trip distinctly through CSV, Parquet and DuckDB; nonfinite numbers are rejected. | `pub04_boundary` | PASS |
| PUB-04 / adjacency | PUB-04 adjacency: Stable v2 record keys join claims, comparisons and figures without conflating separate grain dimensions or duplicate profile appearances. | `pub04_adjacency` | PASS |
| PUB-04 / empty | PUB-04 empty: Suppressed values and precision fields remain SQL/Parquet null and documented CSV nulls, with their status/reason retained. | `pub04_empty` | PASS |
| PUB-04 / encoding | PUB-04 encoding: UTF-8 labels and leading-zero field/entity codes round-trip; spreadsheet-formula prefixes are neutralized under a documented reversible text convention. | `pub04_encoding` | PASS |
| PUB-04 / ordering | PUB-04 ordering: Export row order is canonical and deterministic under input permutation, and the data dictionary documents every public column and join. | `pub04_ordering` | PASS |
| PUB-04 / precision | PUB-04 precision: Numeric export values retain the canonical public precision independently of display rounding; weighted totals are not renamed observed sample counts. | `pub04_precision` | PASS |
| PUB-04 / concurrency | PUB-04 concurrency: An export failure or interrupted write cannot leave a sealed manifest claiming successful CSV, Parquet or DuckDB output. | `pub04_concurrency` | PASS |
| PUB-05 / boundary | PUB-05 boundary: A deliberately suppressed numeric sentinel remains absent from every artifact, including hidden metadata, alternative text and PDF text. | `pub05_boundary` | PASS |
| PUB-05 / adjacency | PUB-05 adjacency: Complementary parent redactions propagate through every duplicate record appearance; the publisher rejects reconstructing a suppressed cell from other public cells. | `pub05_adjacency` | PASS |
| PUB-05 / empty | PUB-05 empty: Null points, denominators and precision fields stay null everywhere and never use internal estimates or another format as fallback. | `pub05_empty` | PASS |
| PUB-05 / encoding | PUB-05 encoding: Serialized JSON/text/HTML/SVG and extracted PDF scans reject nested or encoded diagnostic sentinels while keeping ordinary safe public metadata. | `pub05_encoding` | PASS |
| PUB-05 / ordering | PUB-05 ordering: Reordering public records cannot alter suppression, selected claims, plotted membership or export eligibility. | `pub05_ordering` | PASS |
| PUB-05 / precision | PUB-05 precision: Suppressed standard errors, coefficients of variation, intervals and weighted support never reappear as numerical values in another output. | `pub05_precision` | PASS |
| OPS-01 / adjacency | OPS-01 adjacency: Acquisition refresh, numerical acceptance, analytical build and publication replay retain separate source, acceptance and run identities. | `ops01_adjacency` | PASS |
| OPS-01 / empty | OPS-01 empty: Missing approved snapshot/current receipt, R, benchmark or required package resource fails with a receipt and explicit reason, never a fixture fallback. | `ops01_empty` | PASS |
| OPS-01 / ordering | OPS-01 ordering: Replaying identical eight approved inputs preserves canonical numerical/public content regardless of run IDs and timestamps. | `ops01_ordering` | PASS |
| OPS-01 / idempotency | OPS-01 idempotency: Repeated installed CLI replay of the same frozen snapshots creates distinct immutable receipts with equal canonical numerical content. | `ops01_idempotency` | PASS |
| OPS-01 / concurrency | OPS-01 concurrency: Competing or interrupted operations obey the documented source/output lock boundaries and cannot certify mixed-input content. | `ops01_concurrency` | PASS |
| OPS-02 / adjacency | OPS-02 adjacency: Exact artifact inventory and final receipt are sealed before current promotion; failure at any promotion boundary leaves current invalid. | `ops02_adjacency` | PASS |
| OPS-02 / empty | OPS-02 empty: Empty, partial or corrupted receipt/manifest inventories cannot resolve as a successful publication. | `ops02_empty` | PASS |
| OPS-02 / ordering | OPS-02 ordering: Fault injection across input, export, PDF, receipt, manifest, pointer and final index stages verifies the prescribed write order and preserved history. | `ops02_ordering` | PASS |
| OPS-02 / idempotency | OPS-02 idempotency: A repeated attempt never overwrites an immutable historical run or receipt; reused/colliding run IDs are rejected. | `ops02_idempotency` | PASS |
| OPS-02 / concurrency | OPS-02 concurrency: A crash or concurrent builder cannot expose a partly sealed run as current; lock and recovery behavior are tested locally. | `ops02_concurrency` | PASS |
| OPS-03 / adjacency | OPS-03 adjacency: Each current publication binds the exact required eight source acquisitions; an unrelated successful snapshot cannot authorize it. | `ops03_adjacency` | PASS |
| OPS-03 / empty | OPS-03 empty: Missing source acquisition, missing artifact, bad hash or absent immutable success receipt blocks current resolution. | `ops03_empty` | PASS |
| OPS-03 / ordering | OPS-03 ordering: A later failed required acquisition invalidates previously successful current access; historical bundles remain historical and do not become fresh success. | `ops03_ordering` | PASS |
| OPS-03 / idempotency | OPS-03 idempotency: Repeated resolution verifies current dependencies and artifact hashes again, with no stale success cache across changed inputs. | `ops03_idempotency` | PASS |
| OPS-03 / concurrency | OPS-03 concurrency: Source or manifest changes during resolution are detected before a report/export path is returned as current. | `ops03_concurrency` | PASS |
