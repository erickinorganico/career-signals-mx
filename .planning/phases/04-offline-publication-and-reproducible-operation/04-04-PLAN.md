---
phase: 04-offline-publication-and-reproducible-operation
plan: 04
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - brujula/report_v2.py
  - brujula/pdf_v2.py
  - tests/test_report_v2.py
  - tests/test_pdf_v2.py
  - docs/visual/phase-04-visual-review.md
  - pyproject.toml
  - requirements.txt
  - THIRD_PARTY_NOTICES.md
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03]
must_haves:
  truths:
    - "A reader opens a complete Spanish question-led report with accepted opening claims, national context, three complete field profiles, eight quarters, recorded sex, all 32 states, other fields, coverage, limits, methods and exact sources."
    - "HTML, Markdown and PDF share exact canonical claim/record/figure keys and formatted public quantities; no prose, alternative or figure computes a new number."
    - "Every figure has equivalent SVG and PNG, a semantic table and source/evidence links; plotted gaps and intervals obey supported public state."
    - "Fonts, glyphs and page assets are local; the PDF fetcher rejects network, data URLs, traversal and missing assets as fatal errors."
    - "The four audited DejaVu TTF files and complete LICENSE_DEJAVU are copied to fixed assets/fonts paths inside the staged run, verified against package hashes and used by offline HTML/PDF; no renderer reads a generic external filesystem path."
    - "PUB-01 boundary: Opening contains zero to three accepted claim IDs; a fourth, duplicated or unsupported opening claim blocks publication."
    - "PUB-01 adjacency: Field, occupation, industry and population scopes remain distinct even when labels or numbers coincide; each sentence retains its accepted claim identity."
    - "PUB-01 empty: An empty supported-opening set produces the accepted limitation; suppressed profile cells remain visible as unavailable with reason."
    - "PUB-01 encoding: Official Spanish labels, accents and source text survive output encoding; HTML/Markdown escaping does not replace the underlying evidence identity."
    - "PUB-01 ordering: Fixed question-led sections and field order are independent of input map order, and the eight quarterly sections remain chronological."
    - "PUB-01 precision: Every displayed quantity uses a declared formatter over its exact public record or comparison, with nominal income, universe and nonofficial precision wording retained."
    - "PUB-02 adjacency: HTML, Markdown and extracted PDF refer to the same section/claim/figure/record keys and carry the same supported values and limitations."
    - "PUB-02 empty: Missing PDF capability, font or required local asset blocks the complete publication with a failure receipt; no blank-success or synthetic fallback."
    - "PUB-02 encoding: Spanish accents and punctuation are searchable in the rendered PDF and match the semantic HTML/Markdown text."
    - "PUB-02 ordering: Section order and repeated table headings remain correct across page breaks and match the common editorial representation."
    - "PUB-02 concurrency: Interrupted or concurrent rendering never promotes an incomplete set of HTML/Markdown/PDF artifacts as current."
    - "PUB-03 boundary: Actual desktop, mobile-width and 200 percent zoom HTML and A4 PDF remain legible without clipped prose or figure labels; wide tables use contained scrolling."
    - "PUB-03 adjacency: Trend lines never bridge an unavailable point or incompatible adjacent period; supported intervals and table rows remain attached to the same record."
    - "PUB-03 empty: Fully unavailable figure groups retain a clear unavailable explanation and complete semantic table, never an invented zero plot."
    - "PUB-03 encoding: Figure titles, captions, alternatives and table header scopes preserve official Spanish labels, units, universe and source."
    - "PUB-03 ordering: SVG, PNG and accompanying tables use stable figure keys and declared classification/period order rather than an implicit outcome ranking."
    - "PUB-03 precision: Plot points and intervals equal supported public values before formatting; null precision stays unavailable and no difference interval is inferred."
  artifacts:
    - path: brujula/report_v2.py
      provides: "Question-led shared editorial/figure model and HTML/Markdown/SVG/PNG"
    - path: brujula/pdf_v2.py
      provides: "Restricted local PDF rendering"
    - path: docs/visual/phase-04-visual-review.md
      provides: "Actual HTML and PDF visual inspection record"
  key_links:
    - from: brujula/report_v2.py
      to: brujula/publication_v2.py
      via: "only validated model values and canonical claims"
    - from: brujula/pdf_v2.py
      to: brujula/report_v2.py
      via: "same offline HTML with restricted asset resolution"
    - from: brujula/report_v2.py
      to: brujula/resources.py
      via: "copy four audited DejaVu faces and full notice into staged assets/fonts with hash equality"
---

<objective>
Produce the complete Spanish offline report and matching figures and printable PDF.
Purpose: accepted numerical findings become readable research without changing their evidence.
Output: HTML, Markdown, PDF, paired SVG/PNG, semantic tables and visual review.
</objective>

## Artifacts this phase produces

| Path / symbol | Exact contract for downstream plans |
|---|---|
| `brujula/report_v2.py::build_editorial_document(model), stage_report_fonts(output_dir), render_publication(model, output_dir)` | Accept only a validated 04-03 model. Typed document sections hold fixed ordered question IDs, canonical v2k claim IDs/text, v2r records, accepted v2c comparisons, limitations, figure specs and source refs. `stage_report_fonts` copies exactly the four audited package faces and full notice into `assets/fonts/` and verifies copied SHA against package SHA. Renderer returns exact relative paths including all five font assets plus figure_id/table_id/record_ids/claim_ids mapping. |
| `report.html`, `report.md`, `figures/<figure_id>.svg`, `figures/<figure_id>.png` | HTML/MD share document values/section keys. CSS `@font-face` resolves only `assets/fonts/DejaVuSans.ttf`, `assets/fonts/DejaVuSans-Bold.ttf`, `assets/fonts/DejaVuSerif.ttf`, `assets/fonts/DejaVuSerif-Bold.ttf` relative to report.html. Every figure_id equals a declared `model.figure_links[].figure_id`; its semantic table_id and record/comparison/claim/source links match model. Standalone SVG has glyph paths for local DejaVu rendered labels, text title/desc and no external font requirement. |
| `assets/fonts/{DejaVuSans.ttf,DejaVuSans-Bold.ttf,DejaVuSerif.ttf,DejaVuSerif-Bold.ttf,LICENSE_DEJAVU}` | Exact staged-run font asset set; copied bytes equal 04-01 packaged audited resources; complete notice shipped alongside faces. |
| `brujula/pdf_v2.py::render_pdf(html_path, asset_root, pdf_path, *, allowed_assets)` | Writes `report.pdf` from the same `report.html` through a strict local-only fetcher; `allowed_assets` is the exact validated staged relative inventory including four TTFs and notice, no wheel-external/generic filesystem read or external/data/traversal/symlink access. Missing, altered or unlisted asset/PDF capability raises and blocks publication. |
| `tests/test_report_v2.py`, `tests/test_pdf_v2.py`, `docs/visual/phase-04-visual-review.md` | Semantic and actual visual/PDF evidence including mobile, 200% zoom, A4, accents, pagination, tables and embedded fonts. |

<assumption_delta_decision>no-change: figure alternative text repeats accepted public record/comparison evidence; it does not introduce a new analytical identity beyond the ten-key v2r grain.</assumption_delta_decision>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-CONTEXT.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-RESEARCH.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-EDITORIAL-SPEC.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-RUNTIME-PATH-MAP.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-EDGE-PROBE.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build one question-led editorial and figure representation</name>
  <files>brujula/report_v2.py, tests/test_report_v2.py</files>
  <behavior>Fixed Spanish section sequence covers full agreed scope; 0–3 accepted opening IDs work; all numbers and limitations bind exactly to public v2r/v2c/v2k IDs. SVG/PNG/table use one figure specification and null gaps remain gaps.</behavior>
  <action>Implement build_editorial_document(model), stage_report_fonts(output_dir) and render_publication(model, output_dir) using the exact output and figure-link contract above. Before HTML generation, copy exactly four audited package TTFs and complete LICENSE_DEJAVU into the staged run's assets/fonts directory; verify source and copied SHA against 04-01 resource inventory, fail on missing/altered/colliding assets, and return all five in renderer inventory. CSS @font-face may refer only to these fixed relative paths for HTML and PDF; no generic filesystem or viewer-installed font dependency. Follow 04-EDITORIAL-SPEC.md's exact sequence: cover/window/version; reading rules and known-age cohort; national 15+ context separate from professional cohort; Derecho, Comunicación y periodismo, Ciencias políticas; eight-quarter adjacent/annual evolution with caveats; recorded sex and all 32 entity availability rows with Baja California 02 as descriptive reference; other fields by official code; coverage/exclusions/response; methods and exact sources. Include all 23 metrics in joined detail/appendix while narrative selects legible supported values; missing slots retain reason. Use canonical claim text and comparisons; no new numeric or causal prose, ranking, independent-quarter significance or synthetic fallback. HTML has h1–h3, caption, th scope, table alternative and labeled table scroll; Markdown has equivalent headings/tables/links. Draw point/interval and gap-aware trend figures with units, IC90/CV where available, status, universe, period and source. Pair SVG/PNG from one spec and compare actual point/key sets in tests. Embed figure glyphs as paths in SVG with local DejaVu fonts used during generation so standalone SVG accents do not depend on installed reader fonts; keep semantic title/desc and HTML alt/table text from canonical model, inspect actual accented text via metadata and raster. No external images, scripts, fonts or analytics.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_report_v2.py -q</automated></verify>
  <done>All required Spanish sections, evidence-bound figures and semantic tables render with complete coverage and no invented value.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Render secure, printable PDF from the common HTML</name>
  <files>brujula/pdf_v2.py, tests/test_pdf_v2.py, pyproject.toml, requirements.txt, THIRD_PARTY_NOTICES.md</files>
  <behavior>WeasyPrint 70 renders complete A4 PDF with searchable Spanish accents, page counters and repeated table headers using only the staged four-font set; network, file escape, data URI, symlink, missing or hash-altered font/asset attempts fail the entire publication.</behavior>
  <action>Implement render_pdf(html_path, asset_root, pdf_path, *, allowed_assets) with WeasyPrint 70.0 and a strict LocalOnlyFetcher whose allowlist is exact validated staged-run relative assets, including the four named TTFs and notice, under one resolved root; canonicalize paths, reject network/data schemes, traversal, symlink escape, unlisted files and reads outside the run, including wheel paths. Validate each font's staged hash against packaged audited hash before rendering; raise on missing/altered resource and treat WeasyPrint warnings/errors for resources as fatal. PDF @font-face resolves the same staged relative URLs as HTML. Verify actual PDF font embedding/subsetting and Spanish glyph extraction, and test missing TTF, changed copied byte, traversal and clean outside-checkout HTML/PDF render without system font assumption. Match editorial print CSS (A4, 18–20mm margins, 10.5–11pt body, 1.4 line height, repeating headers, keep-with-next headings, section-safe page breaks). Validate page count/text, all sections, first/last rows of long tables, source notes, numeric/claim parity with HTML/Markdown and no hidden sentinels. A missing PDF/native capability cannot produce a partial success. For portable test-only text/font readback, declare the already reviewed pypdf==6.19.0 in the test/audit dependency set and requirements, and retain its BSD-3-Clause attribution in THIRD_PARTY_NOTICES.md. Use docs/evidence/phase-04-pdf-audit-dependency.json for exact registry/wheel identity. Do not depend on the Codex bundled interpreter or make this inspection library a product rendering dependency. This bounded declaration supplements the rendering action above; no other new package is implied.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_pdf_v2.py tests/test_report_v2.py -q</automated></verify>

  <done>The same validated report becomes an offline searchable and printable PDF, and unauthorized/missing asset fetches block it.</done>
</task>
<task type="auto">
  <name>Task 3: Inspect actual visual layout and record fixes</name>
  <files>docs/visual/phase-04-visual-review.md, brujula/report_v2.py, brujula/pdf_v2.py</files>
  <action>Render actual accepted real-data output and inspect desktop, mobile width (including long tables), 200% zoom and A4 PDF cover, profiles, sparse states, section transitions and sources. Use local browser/PDF image evidence and record viewport/page, observed clipping/contrast/heading/table/glyph status, exact fixes and recheck. Check all actual CSS text and state colors meet calculated contrast, charts distinguish series without color, standalone SVG/PNG Spanish text renders, no whole-page horizontal overflow, PDF repeat headers/page footer and embedded fonts. Do not declare PDF/UA or screen-reader certification. Store bounded screenshots or contact sheets outside public bundle until license/privacy review; keep review Markdown public-safe.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_report_v2.py tests/test_pdf_v2.py -q</automated></verify>
  <done>Visual review records and fixes actual desktop/mobile/zoom/print issues against the real report.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Validated public model → renderer | Text and chart serialization must retain evidence and suppression. |
| HTML → PDF local fetcher | URL requests may otherwise read network or private files. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-09 | Information disclosure | renderer metadata/alt | mitigate | Render only validated model; scan actual SVG/PNG/HTML/MD/PDF for nested sentinels. |
| T-04-10 | Information disclosure | PDF URL fetcher | mitigate | Allowlist real staged assets and reject all external/data/traversal/symlink URLs; fatal missing asset. |
| T-04-11 | Tampering | figure/report parity | mitigate | Assert canonical figure-point/table/alt/claim keys and quantities match. |
</threat_model>

<verification>
Focused report/PDF tests plus extracted-PDF semantic parity and actual visual review; run again after layout fixes.
</verification>

<success_criteria>
A reader can inspect equivalent accessible static formats, full scope and uncertainty offline; actual visual gate has evidence.
</success_criteria>

<output>
Create .planning/phases/04-offline-publication-and-reproducible-operation/04-04-SUMMARY.md after execution.
</output>
