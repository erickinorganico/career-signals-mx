---
phase: 04-offline-publication-and-reproducible-operation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - brujula/publication_v2.py
  - brujula/export_v2.py
  - contracts/publication-v2.schema.json
  - tests/test_publication_v2.py
  - tests/test_export_v2.py
autonomous: true
requirements: [PUB-04, PUB-05]
must_haves:
  truths:
    - "Only a validated Phase 3 packet and its sanitized record_index can enter the public model; the original Phase 2 projection and diagnostic estimates are rejected."
    - "All 23 accepted metric IDs and all public records retain v2r ten-key identity, distinct dimensions, status, null, support, source/method and evidence in CSV, Parquet and DuckDB."
    - "A supported public row or accepted comparison is the only source of displayed numbers; a complement reconstruction, hidden diagnostic or suppressed precision cannot enter any public row or link."
    - "PUB-04 boundary: Zero, null and finite numeric precision round-trip distinctly through CSV, Parquet and DuckDB; nonfinite numbers are rejected."
    - "PUB-04 adjacency: Stable v2 record keys join claims, comparisons and figures without conflating separate grain dimensions or duplicate profile appearances."
    - "PUB-04 empty: Suppressed values and precision fields remain SQL/Parquet null and documented CSV nulls, with their status/reason retained."
    - "PUB-04 encoding: UTF-8 labels and leading-zero field/entity codes round-trip; spreadsheet-formula prefixes are neutralized under a documented reversible text convention."
    - "PUB-04 ordering: Export row order is canonical and deterministic under input permutation, and the data dictionary documents every public column and join."
    - "PUB-04 precision: Numeric export values retain the canonical public precision independently of display rounding; weighted totals are not renamed observed sample counts."
    - "PUB-04 concurrency: An export failure or interrupted write cannot leave a sealed manifest claiming successful CSV, Parquet or DuckDB output."
    - "PUB-05 boundary: A deliberately suppressed numeric sentinel remains absent from every artifact, including hidden metadata, alternative text and PDF text."
    - "PUB-05 adjacency: Complementary parent redactions propagate through every duplicate record appearance; the publisher rejects reconstructing a suppressed cell from other public cells."
    - "PUB-05 empty: Null points, denominators and precision fields stay null everywhere and never use internal estimates or another format as fallback."
    - "PUB-05 encoding: Serialized JSON/text/HTML/SVG and extracted PDF scans reject nested or encoded diagnostic sentinels while keeping ordinary safe public metadata."
    - "PUB-05 ordering: Reordering public records cannot alter suppression, selected claims, plotted membership or export eligibility."
    - "PUB-05 precision: Suppressed standard errors, coefficients of variation, intervals and weighted support never reappear as numerical values in another output."
  artifacts:
    - path: brujula/publication_v2.py
      provides: "Strict single public/editorial input with v2r/v2c/v2k joins"
    - path: brujula/export_v2.py
      provides: "Typed CSV/Parquet/DuckDB and dictionary exports"
    - path: contracts/publication-v2.schema.json
      provides: "Closed public publication model"
  key_links:
    - from: brujula/publication_v2.py
      to: brujula/findings_v2.py
      via: "validate_analysis_packet and consume sanitized record_index"
    - from: brujula/export_v2.py
      to: brujula/publication_v2.py
      via: "exports only model rows and stable v2r keys"
---

<objective>
Define one strict public projection and reusable aggregate export from accepted Phase 3 evidence.
Purpose: every later output must share one suppression and provenance boundary.
Output: typed publication model, stable joins, CSV/Parquet/DuckDB and dictionary.
</objective>

## Artifacts this phase produces

| Path / symbol | Exact contract for downstream plans |
|---|---|
| `brujula/publication_v2.py::build_publication_model(packet), validate_publication_model(model)` | Input is persisted `validate_analysis_packet`-accepted Phase 3 packet. Closed model keys: `schema_version, source_manifest, records, profiles, coverage, comparisons, claims, opening_claim_ids, limitations, figure_links, content_digest`. `records` is the sanitized `record_index` keyed by existing `v2r:` IDs, never original Phase 2 projection. `content_digest` covers canonical public model content excluding run clocks. |
| `contracts/publication-v2.schema.json` | Strict model schema and exact referential validation. `figure_links[]` has `figure_id, table_id, record_ids, comparison_ids, claim_ids, source_ids`; all IDs resolve in model, figures may select a subset but cannot add values. `v2r:` keeps the ten-key grain, `v2c:` and `v2k:` retain accepted Phase 3 IDs. |
| `brujula/export_v2.py::export_public_tables(model, output_dir)` | Writes `public-records.csv`, `public-records.parquet`, `public.duckdb`, `data-dictionary.md` plus relational comparisons/claims/figure-link tables within the same containers. Returns an exact relative-path inventory and row/key counts; rejects invalid model before I/O. |
| `tests/test_publication_v2.py`, `tests/test_export_v2.py` | Exact values, 23 metrics, null/zero, nonfinite, nested suppression, complement rejection, provenance and cross-container join assertions. |

<assumption_delta_decision>no-change: alternative text and figure links are representations of existing v2r/v2c/v2k evidence; no new analytical entity or grain is introduced.</assumption_delta_decision>

<execution_context>
@${GSD_CORE}/workflows/execute-plan.md
@${GSD_CORE}/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/ROADMAP.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-CONTEXT.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-RESEARCH.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-PATTERNS.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-UPSTREAM-PREFLIGHT.md
@.planning/phases/04-offline-publication-and-reproducible-operation/04-EDGE-PROBE.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate the public model and exact evidence joins</name>
  <files>brujula/publication_v2.py, contracts/publication-v2.schema.json, tests/test_publication_v2.py</files>
  <behavior>Persisted accepted packet validates; public model preserves 6739 sanitized record identities and links profiles, claims and supported comparison IDs. Suppressed nested value, weighted denominator/support, SE, CV and CI stay null; complement-derived invented measure is rejected.</behavior>
  <action>Implement build_publication_model(packet) and validate_publication_model(model) using validate_analysis_packet(packet), strict schema and the packet's sanitized record_index only. Use the closed keys and figure_links contract above. Reuse existing v2r IDs derived from exact ten-key grain; use v2c/v2k IDs for comparison/claim joins, never generate a second key or infer figures from diagnostic roots. Model complete coverage: 23 metrics; eight quarters; national 15+ versus known-age completed-professional cohort; three focal fields; latest other official fields; 32 entities/two recorded-sex slots; missing reasons; sources, method, limitations. Canonicalize input order, reject unknown diagnostic keys, nonfinite numbers, orphan references, changed evidence identity, a direct measure reconstructed by complement and any claim not regenerated by upstream gate. Keep formatters separate from unrounded public numeric values. Test nested sentinels, duplicate appearances, encoded variants and altered order against actual model serialization.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_publication_v2.py -q</automated></verify>
  <done>One complete, strict, order-stable public model binds every value and claim to accepted sanitized evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Emit fully joined aggregate exports and dictionary</name>
  <files>brujula/export_v2.py, tests/test_export_v2.py</files>
  <behavior>CSV, Parquet and DuckDB round-trip exact 23-metric public rows, supported comparison/claim/figure join keys, leading-zero codes, zero versus null, status/reason and provenance; formulas are neutralized only in CSV.</behavior>
  <action>Implement export_public_tables(model, output_dir) to write the exact named containers above, with canonical ordered public record, comparison, claim and figure-link tables and typed distinct source/population/field/occupation/industry/geography/sex/period/metric/method dimensions and evidence refs. DuckDB writes transactional typed tables; Parquet comes from DuckDB COPY; CSV uses UTF-8 and documented reversible apostrophe escape for = + - @ labels. Data dictionary documents every column, null/status, nominal basis, observed n versus weighted quantities and v2r/v2c/v2k/figure joins. Reject a non-public/internal payload before writing; exact output row values and keys must match in all formats, including null nested precision. Test interrupted export does not yield a complete declared artifact set. Do not copy v1 observation.id or five-key warehouse grain.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_export_v2.py tests/test_publication_v2.py -q</automated></verify>
  <done>CSV/Parquet/DuckDB and dictionary expose the full accepted public detail with exact join and suppression parity.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Phase 3 packet → public model | Only validated sanitized records cross into publication. |
| Public model → export files | Typed values and text enter CSV/Parquet/DuckDB. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-06 | Information disclosure | model/export | mitigate | Reject internal payload and scan suppressed point plus nested diagnostics in actual exports. |
| T-04-07 | Tampering | record joins | mitigate | Validate v2r ten-key identity and exact v2c/v2k references. |
| T-04-08 | Injection | CSV | mitigate | Neutralize spreadsheet formula prefixes and document reversible representation. |
</threat_model>

<verification>
Focused public-boundary and export round-trip tests include deliberately suppressed point and nested sentinels, complement attempt, all 23 metrics, 6739 real sanitized rows and join-key equality.
</verification>

<success_criteria>
All public exports are complete and numerically faithful to the validated model; an internal or unsupported number cannot be serialized.
</success_criteria>

<output>
Create .planning/phases/04-offline-publication-and-reproducible-operation/04-03-SUMMARY.md after execution.
</output>
