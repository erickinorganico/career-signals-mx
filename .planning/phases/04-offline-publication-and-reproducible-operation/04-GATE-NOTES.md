# Phase 4 planning gates

- Accepted upstream: Phases 1–3 complete; Phase 3 canonical closure returned no warnings. Exact interfaces and saved packet readback are in 04-UPSTREAM-PREFLIGHT.md.
- Reuse existing 04-RESEARCH/CONTEXT/EDITORIAL-SPEC/PACKAGING-READBACK/RUNTIME-PATH-MAP and the refreshed typed pattern map. New research is only needed for a concrete uncovered implementation question.
- plan:pre is active for research, pattern mapping, threat modeling, edge/validation and coverage contributions. The phase has no AI integration, frontend, hosted database or ORM; the UI detector returns frontend=false/block=false and codebase drift requires no action. Editorial visual checks remain explicit PUB acceptance requirements.
- assumption-delta scan detected `alternative` only in alternative text. Decision: no-change. Primary analytical identity remains the ten-dimensional v2r grain; alternative text is another representation of the same public records. Acquisition attempt, numerical acceptance, publication run and release IDs remain separate concepts as already specified.
- The runtime warns about baked model configuration and an optional missing mempalace curator. The actual native typed spawn schema supports the explicit Sol/medium selection required by AGENTS. No optional memory integration or global runtime reinstall is needed for this project; no memory writes are authorized.
- Eight PUB/OPS requirements yield 45 explicit edge candidates in the shared GSD engine. Draft requirements/resolutions are in `.cache/research/phase4-edge-*.json`; planner must review and lift exact criteria into flat must-have truths, and write the final edge/validation artifacts. Draft resolution denotes a specification, not a passed test.

- Package legitimacy follow-through: slopcheck 0.6.1 was verified against PyPI wheel SHA-256 and installed in an ignored disposable environment. Its read-only scan of current/PDF dependency names returned 34/34 OK, exit 0. Direct WeasyPrint 70.0 version/official-source/artifact hashes are verified; downloads are unknown, never assumed zero. This updates the earlier unavailable-helper note without adding a project runtime dependency. See 04-RESEARCH Package Legitimacy Audit and docs/evidence/phase-04-dependency-legitimacy.json.
- First independent plan checker returned two blockers: explicit migration of analysis_v2 checkout code/golden paths, and staged TTF font paths in the restricted PDF allowlist and sealed manifest. Planner is revising those exact seams; no source implementation has started and no plan acceptance is inferred from structural validation.

- Final independent plan check passes with 0 blockers and 0 warnings after two bounded revisions. The installed inventory/golden seam, staged fonts, and pre/post trusted-reference rebind order are explicit; historical issues remain in 04-PLAN-CHECK.md. All six plans pass canonical structure/frontmatter validation. Post-planning gap analysis passes 8/8 PUB/OPS requirements; the decision gate skips because CONTEXT contains no D-numbered decisions, while subject-level decision coverage is recorded in 04-VALIDATION.md.
- Execution follows typed native GSD executors. This Codex runtime has workflow.use_worktrees=false, so implementation plans run sequentially on the shared checkout, respecting the four dependency waves. Independent bounded documentation/review may proceed separately; owned paths and scoped commits preserve other contributions. No external inference or priority tier is introduced.

- Execution preflight: use_worktrees=false, MVP=false, stale auto-chain=false; no orphan 04-plan commits or continue-here blocker exist. No execute:pre/wave:pre hooks are active. Wave-post hooks require schema/codebase drift and UI checks; execute-post requests independent code review. Nyquist/security/goal verification remain required. The plan index recognizes all six executable plans; its warning misclassifies the checker report name 04-PLAN-CHECK.md, which is intentionally not an executable plan. No files are renamed or omitted to suppress that advisory.
