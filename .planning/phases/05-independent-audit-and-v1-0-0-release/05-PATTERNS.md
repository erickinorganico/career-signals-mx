# Phase 5 implementation patterns for 05-01

This focused map was prepared by the assigned general worker. A typed GSD pattern-mapper was unavailable in the current thread limit; these are inspected repository analogs, not an independent review or an execution pass.

| 05-01 concern | Existing analog and behavior to retain | Planned use |
|---|---|---|
| Reader entrypoints and current authority | `README.md`, `README.en.md`, `docs/STATUS.md`, `docs/RELEASE.md`, `docs/CONTRACT-V2.md`, `docs/OPERATIONS.md` distinguish the accepted eight-quarter numerical/analytical work, Phase 4 publication, and pending Phase 5 release. `docs/CONTRACT.md` is the historical v1 boundary. | Correct only remaining `FINAL-DOCUMENTATION-GAPS.md` findings in the 05-01 owned files. Link accepted receipts and exact installed CLI forms; keep the preview and synthetic examples visibly historical. |
| Documentation validation | `scripts/check_docs.py` checks local links, required reader files, JSON syntax, heuristic secrets, and personal paths; it explicitly does not certify a release. | Run after edits; pair with paragraph-level evidence readback and affected tests rather than treating a link check as a release gate. |
| Real publication evidence | `docs/evidence/phase-04-publication-acceptance.json` binds source, analysis, publication, manifest, installed wheel, recovery and hosted fixture receipts. `docs/evidence/phase-05-artifact-inventory-candidate.json` inventories 73 content files but remains `CANDIDATE_REVIEW` and `release_approved: false`. | Reference accepted facts and preserve their scope. Never infer final license, privacy, visual or public-release approval from these inputs. |
| Installed wheel identity | `scripts/check_installed_runtime.py` verifies outside-checkout import, wheel metadata and member hashes, authored resources, native PDF capability, Spanish searchable PDF, and fail-closed local assets. `pyproject.toml` is the package version/resource authority. | The new release checker compares downloaded Windows/Ubuntu receipts to one candidate target/version and matching code/resource and wheel identities. Rebuild/recheck after any payload change. |
| Hosted portability | `.github/workflows/verify.yml` builds a current wheel and runs the installed helper outside checkout on Ubuntu and Windows; both helper JSON artifacts are uploaded. It is a read-only input for 05-01. | Read back exact hosted run SHA, conclusions and downloaded JSON hashes into `phase-05-portability.json`. Fixture/PDF portability does not stand in for real R/numerical replay. |
| Local/offline operation | `docs/OPERATIONS.md` gives exact CLI commands, separate source/output/audit roots, recovery, native prerequisites, and no-network read paths. | Verify README/release instructions against this guide and `brujula/cli.py`; do not rewrite the guide in 05-01. |
| Release gate validation | Phase 5 plans and `05-EDGE-COVERAGE.md` require negative cases for changed target/missing host, extra or altered asset, private draft, missing trace ID, and review independence/dispositions. | Build standard-library, fixture-only `scripts/verify_release.py` subcommands with tests. Remote evidence checker may hash local captures; tests must not call GitHub. Independent human and legal/privacy gates remain distinct. |

The implementation/package/documentation payload must freeze before the final wheel CI run. Later inventory, readback and administrative evidence may cite that immutable payload SHA, but cannot silently change the release target or reuse a prior wheel result after changed bytes.
