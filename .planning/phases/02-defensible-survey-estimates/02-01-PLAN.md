---
phase: 02-defensible-survey-estimates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brujula/enoe_adapter.py
  - brujula/metrics.py
  - data/catalog/enoe-metrics.json
  - tests/test_enoe_adapter.py
  - tests/test_enoe_metrics.py
  - pyproject.toml
  - tests/phase2_prohibitions_01.py
  - tests/phase2_prohibitions_01.test.cjs
  - tests/fixtures/phase2_prohibitions/01.clean.json
  - tests/fixtures/phase2_prohibitions/01-p1.bad.json
  - tests/fixtures/phase2_prohibitions/01-p2.bad.json
autonomous: true
requirements: [STAT-01, STAT-06]
must_haves:
  truths:
    - "Every approved snapshot yields one complete responding and resident survey frame before any age, field, sex, geography, outcome or metric restriction."
    - "An analyst can inspect versioned numerator, denominator, unit, nonresponse, sentinel and official dictionary rules for every committed labor metric."
    - "A failed current, wrong member/hash, malformed code or design key prevents estimation and leaves aggregate-only diagnostics."
    - "STAT-01 boundary: R_DEF 0/00, EDA 14/15/97/98/99 and strictly positive FAC_TRI follow their exact eligibility or rejection rules."
    - "STAT-01 adjacency: repeated UPM within one stratum is one cluster, while the same UPM label in distinct strata remains two design clusters."
    - "STAT-01 empty: an empty or single-row valid response/resident frame cannot be accepted as an estimable complete design."
    - "STAT-01 ordering: permuting SDEM rows leaves aggregate frame counts and design support unchanged."
    - "STAT-01 precision: only ASCII U+0020 trimming is permitted and weight overflow or nonfinite values fail before estimation."
    - "STAT-06 boundary: INGOCUP 0/1/999998/999999 and HRSOCUP 0/1/168/169 take distinct documented eligibility paths."
    - "STAT-06 adjacency: SUB_O=1 counts only within occupied CLASE2=1 and hours zero with DUR9C=1 differs from hours zero with DUR9C=9."
    - "STAT-06 empty: no valid positive-income or known-hours denominator yields null with coverage and exclusion cause, never a numeric zero."
    - "STAT-06 encoding: metric codes accept ASCII digits and permitted U+0020 padding only; CMPE requires six-digit normalization and quarter catalog membership."
    - "STAT-06 ordering: metric and category counts retain stable IDs and values when input row order changes."
    - "STAT-06 precision: missing or sentinel inputs never become observed zero and weighted metric numerator/denominator values remain finite and reproducible."
    - "The individual-row prohibition covers returned audits, captured stdout/stderr and logger output, and tracked or packaged Phase 2 data artifacts; a disposable injected synthetic row is detected on each surface."
  artifacts:
    - path: brujula/enoe_adapter.py
      provides: "Strict bounded-memory complete-frame loader and typed column arrays"
    - path: brujula/metrics.py
      provides: "Metric masks, values and aggregate coverage from one loaded frame"
    - path: data/catalog/enoe-metrics.json
      provides: "Versioned metric definitions and dictionary references"
    - path: tests/phase2_prohibitions_01.test.cjs
      provides: "Executable p1/p2 negative controls with bad and clean subjects"
  key_links:
    - from: brujula/enoe_adapter.py
      to: brujula/source_inventory.py
      via: "inventory_snapshot(snapshot_id, output_root, registry_path=None) and verified exact member"
    - from: brujula/metrics.py
      to: brujula/populations.py
      via: "POPULATION_DEFINITIONS and period catalog CMPE normalization"
  prohibitions:
    - statement: "Individual ENOE person rows must not be committed, logged, or returned in aggregate audit artifacts."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase2_prohibitions_01.test.cjs
      check_violation_fixture: tests/fixtures/phase2_prohibitions/01-p1.bad.json
      check_clean_fixture: tests/fixtures/phase2_prohibitions/01.clean.json
    - statement: "A missing field, unknown age, income sentinel, or unspecified hour must not silently become an observed zero or a named professional field."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase2_prohibitions_01.test.cjs
      check_violation_fixture: tests/fixtures/phase2_prohibitions/01-p2.bad.json
      check_clean_fixture: tests/fixtures/phase2_prohibitions/01.clean.json
---

<objective>
Turn each of the eight verified ENOE ZIPs into a strict complete-frame column representation with all committed metric definitions.
Purpose: The full design and exact denominators must exist before variance or publication is computed.
Output: A reusable NumPy adapter, a versioned metric catalog, metric vectors and aggregate-only audits.
</objective>

<execution_context>
@${GSD_CORE}/workflows/execute-plan.md
@${GSD_CORE}/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/phases/02-defensible-survey-estimates/02-CONTEXT.md
@.planning/phases/02-defensible-survey-estimates/02-RESEARCH.md
@.planning/phases/02-defensible-survey-estimates/02-PATTERNS.md
@.planning/phases/02-defensible-survey-estimates/02-EDGE-PROBE.json
@.planning/phases/01-official-sources-and-research-contract/01-03-SUMMARY.md
@brujula/source_inventory.py
@brujula/populations.py
@brujula/survey.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Decode all eight snapshots into a complete survey frame</name>
  <files>brujula/enoe_adapter.py, tests/test_enoe_adapter.py</files>
  <behavior>All eligible valid-response/resident rows remain in the full design, including those outside requested domains. R_DEF ASCII 0/00 and C_RES 1/3 are accepted exactly; blank and ASCII-space-padded numeric lexemes are counted before normalization. Wrong source/current, duplicate or missing headers, ambiguous or wrong SDEM member, hash mismatch, unknown token, invalid FAC_TRI/EST_D_TRI/UPM and PSU nesting fail closed. An empty or single-row valid frame fails as an unusable design. Equal keys preserve deterministic counts independent of input ordering.</behavior>
  <action>Implement `load_snapshot_frame(snapshot_id: str, output_root: Path, registry_path: Path | None = None) -> (frame, audit)` using the implemented `inventory_snapshot` and `resolve_snapshot` custody APIs; inspect their returned metadata before coding and require the successful current receipt, registry/raw/member hashes and period-specific dictionary/catalog. Stream the exact Latin-1 SDEM member once with `csv.DictReader`, reject duplicate/missing required headers, and accumulate typed NumPy-ready columns in bounded column buffers; do not create a giant list of person dictionaries or reread the member per metric. Build FAC_TRI, EST_D_TRI and UPM arrays for all valid R_DEF/C_RES rows before deriving full-length age, CMPE, ENT/CVE_ENT, SEX and labor masks. Retain zero-domain PSUs. Audit raw lexeme classes and normalize only ASCII U+0020; blanks stay null, exact ASCII digits only, no fuzzy/decimal/nonfinite coercion. Reuse Phase 1 population and catalog IDs; national context includes operational EDA 98 as unknown age, while professional cohort ends at known EDA 97. Return only arrays in memory and aggregate counts/hashes/exclusions in audit; never serialize person rows. Cover STAT-01 and source boundary/adjacency/empty/ordering/precision edge cases.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_enoe_adapter.py -q</automated></verify>
  <done>Fixture frames prove full-design-first construction, exact source/code rules and aggregate-only output; malformed inputs fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define and vectorize every labor metric</name>
  <files>brujula/metrics.py, data/catalog/enoe-metrics.json, tests/test_enoe_metrics.py, pyproject.toml</files>
  <behavior>CLASE1=1 means PEA and CLASE2=1 means occupied; an unemployed PEA row never enters occupied numerator. SUB_O=1 is a numerator only over occupied. Weekly zero is known only for DUR9C=1 temporary absence; DUR9C=9 zero is unknown. ING7C 1..5 plus INGOCUP 1..999998 is positive known; 0, 999999, no income, unknown and conflicting combinations are separate. Empty denominators stay null. Domain vectors are full-frame length and stable across input order.</behavior>
  <action>Create versioned data definitions with ID, operation, numerator, denominator, unit, price basis, valid categories, sentinel/nonresponse rules and per-quarter official dictionary refs. Give the manifest a version and content SHA-256 that the estimator must include in its method identity; a definition change must change method_version and later block automatic comparability even when units match. Implement full-frame NumPy masks/vectors for population, occupied, PEA, unemployed totals; employment, participation, unemployment rates; positive-known nominal monthly income mean and coverage, no-income and unspecified-income counts/shares; main-job EMP_PPAL informality (not TIL1/sector), recorded-sex composition, official POS_OCU 1..4 shares, SUB_O count/rate over occupied, known weekly hours mean/coverage. For hours accept HRSOCUP 1..168 with DUR9C 2..8 and observed 0 only with DUR9C=1; exclude DUR9C=9. Count unsupported/unknown codes and exclusions without converting missing to zero. Keep field_of_study, occupation, industry and geography distinct, support selectors for national/all-fields/focal-fields/recorded sex/entity, and calculate masks once on the loaded frame. Declare NumPy 2.5.3 as the direct pinned dependency in pyproject; the installed local runtime is confirmed and the primary PyPI package was verified in research, so no fresh install is part of this plan. If a different environment later needs an install, deterministically verify registry identity, exact version and downloaded hash before the already-authorized free install. Cover STAT-06 boundary, adjacency, empty, encoding, ordering and precision edges.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_enoe_metrics.py tests/test_population_rules.py -q</automated></verify>
  <done>Every committed metric has a dictionary-backed definition and tested eligible numerator/denominator/sentinel path, including occupation versus PEA regression and hours zero distinction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove source and missing-value prohibitions with content-dependent controls</name>
  <files>tests/phase2_prohibitions_01.py, tests/phase2_prohibitions_01.test.cjs, tests/fixtures/phase2_prohibitions/01.clean.json, tests/fixtures/phase2_prohibitions/01-p1.bad.json, tests/fixtures/phase2_prohibitions/01-p2.bad.json</files>
  <behavior>The named Node checks pass against current adapter/metric APIs and the clean subject; p1 fails when a disposable synthetic person row is inserted into the returned audit, captured stdout/stderr or logger stream, or tracked/packaged Phase 2 data-artifact view. p2 fails when unknown field/age, income sentinel or DUR9C=9 zero is mutated into an observed value or named field. An unrelated bad fixture fails only its matching named test. The result depends on returned and emitted content, not a fixed regex or unconditional failure.</behavior>
  <action>Create the portable Node wrapper and Python runner after Tasks 1–2. Resolve Python as `BRUJULA_TEST_PYTHON` when set, otherwise project `.venv/Scripts/python.exe` on Windows or `.venv/bin/python` on POSIX; invoke files by absolute workspace path, use no network. For p1, call the real adapter on a disposable synthetic ZIP fixture while capturing returned audit, stdout, stderr and Python logger records. Inspect tracked and packaged Phase 2 data artifacts via a content-aware scanner: identify person-record structures/field combinations in data payloads, not mere variable names in source or explicitly synthetic test fixtures. The bad subject selects a disposable synthetic row injection into each of those surfaces, including a temporary package/artifact manifest view, without writing real rows or modifying Git. Assert every surface independently goes red and the clean/current surfaces pass. For p2, call real `classify_eligibility`, CMPE normalization and Phase 2 metric vectors with missing age/field, INGOCUP sentinel and DUR9C=9 zero; bad subject mutates a computed eligibility/mask/coverage result into observed zero/named field, which assertions reject. Keep each bad/clean JSON subject to `case` and `mutation`; never put person rows in tracked fixture JSON. The default and clean subject must pass, each bad subject must make the named test go red, then the canonical GSD prohibition-enforcement producer must machine-prove red/clean/current and return green for both projected descriptors. A currently nonexistent planned check must not be reported green before implementation.</action>
  <verify><automated>node --test tests/phase2_prohibitions_01.test.cjs</automated></verify>
  <done>Both unchanged must-NOT statements have live API assertions, clean controls and bad controls that genuinely fail their named tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Untrusted local ZIP bytes and catalog metadata cross into typed in-memory survey columns. Person rows remain local.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Spoofing | ZIP/current/member | mitigate | Reuse Phase 1 pinned resolver and inventory; exact path/member/hash and failed-current tests. |
| T-02-02 | Tampering | SDEM codes and weights | mitigate | ASCII-only lexical parser, dictionary membership and positive finite design checks with negative fixtures. |
| T-02-03 | Information disclosure | adapter audit and artifacts | mitigate | Capture returned audit/stdout/stderr/loggers and content-scan tracked/packaged data outputs with synthetic row-injection controls. |
| T-02-04 | Denial of service | large SDEM snapshot | mitigate | Single-pass column buffers and no per-metric row reread or row-dictionary retention. |
| T-02-SC | Tampering | NumPy dependency | mitigate | Pin confirmed installed NumPy 2.5.3; if a fresh install becomes necessary elsewhere, verify primary registry identity, version and artifact hash before authorized install. |
</threat_model>

<verification>
Run focused tests, the two Node prohibition controls and full pytest after the wave. The canonical GSD producer must show `located:true`, non-vacuous current pass, machine-proven bad-fixture failure, clean-fixture pass and `flagged:false` for both descriptors before canonical phase closure. Run the versioned all-eight offline aggregate adapter audit only after Phase 1 verification; compare observed counts, aliases, singleton design counts and hashes with approved Phase 1 inventory and the existing aggregate research audit. No network access or person-level committed output.
</verification>

<success_criteria>
STAT-01 and STAT-06 have a full-frame input and complete, versioned metric semantics for all eight quarters.
</success_criteria>

<artifacts_created>
Artifacts this phase produces in this plan: `brujula/enoe_adapter.py` exports `load_snapshot_frame(snapshot_id: str, output_root: Path, registry_path: Path | None = None) -> tuple[Frame, dict]`; its `Frame` holds in-memory full-length typed NumPy columns and never serializes rows. `brujula/metrics.py` exports `load_metric_manifest() -> dict` and `metric_vectors(frame: Frame, population_id: str, domain: dict, metric_id: str) -> dict` with full-length numerator, denominator and domain masks plus aggregate coverage/exclusion counts. `data/catalog/enoe-metrics.json` carries manifest version, metric IDs, numerator/denominator operations, unit, price_basis, sentinels, dictionary refs, and a canonical SHA-256 recorded in method identity. Exact returned mapping keys are `numerator`, `denominator`, `domain`, `coverage`, `exclusions`.
`tests/phase2_prohibitions_01.test.cjs` and `.py` execute named p1/p2 checks over real adapter/metric APIs; `tests/fixtures/phase2_prohibitions/01.clean.json`, `01-p1.bad.json` and `01-p2.bad.json` are known-clean and known-bad subjects consumed through `GSD_PROHIB_SUBJECT` by the GSD producer.
</artifacts_created>

<output>Create `.planning/phases/02-defensible-survey-estimates/02-01-SUMMARY.md` when done.</output>
