# Phase 2 planning gates

- User decision authority: proceed autonomously with GSD's recommended options, within the existing local research and named GitHub publication scope.
- Research and pattern mapper ran with typed native GSD agents. The separate input-audit helper used native Luna. No external inference, paid service or proprietary workspace code was used.
- Planning prepares Phase 2 while the Phase 1 contract is being implemented. Phase 2 execution waits for Phase 1 verification; the checker must confirm the final contract signatures and schemas before execution.
- No frontend or model integration belongs to this numerical phase. UI/AI spec generation is inapplicable; configured detector gates still run against final plans.
- Default-on spec-less edge probe ran through the shared engine. Authored supported shapes account for the Spanish requirements; `02-EDGE-PROBE.json` contains 30 candidate edges. Candidates are not passed checks: each must be lifted into concrete plan acceptance/tests.
- Existing project prohibitions remain binding. A descriptor-less prohibition is flagged for independent evidence review; no fabricated check descriptor establishes a pass.
- ASVS level 1 applies to source custody, strict code/weight parsing, aggregate provenance and suppression. Authentication and session controls are outside the local batch pipeline.
- The eight-period audit is now PASS with occupied defined as CLASE2=1, separate PEA, raw lexeme counts, strict ASCII-space normalization, and joint DUR9C/HRSOCUP and SUB_O/CLASE2 evidence. Final product code must preserve these meanings in fixtures and real integration, rather than relying on an ignored prototype.
- R and official comparisons remain acceptance gates. Their scope, fixed tolerances, rate-rounding rule and honest nonofficial precision policy are decided in CONTEXT; no numerical pass is inferred from planning.
- The API detector initially combined "integration" with statistical "interval endpoints", although this phase adds no external service. Reworded that statistical phrase to "confidence interval limits", preserving its test requirement; the unchanged detector rerun reports no API signal/block. UI gate likewise reports no frontend/block. No real integration capability was removed or waived.
- Typed plan checker passed the numerical/scope review and a targeted metadata recheck: 30 explicit flat edge truths (11/9/10), seven structured prohibitions (2/2/3), three artifact-interface blocks, final known-age R masks, fixed R tolerances, six exact official totals, separate rate rounding and raw variance discrepancies are preserved. Real execution remains gated on Phase 1 acceptance.
- Autonomous GSD verification treats descriptor-less judgment prohibitions as visible non-authoritative review flags, not a silent pass or a hard halt (`verify-phase.md`, ADR-550 B-with-guard). Existing deterministic project tests still enforce the relevant data/suppression invariants; no unsupported Node enforcement descriptors are invented.
- Phase 1 revealed that the canonical completion command still blocks judgment flags. Revised plans therefore include real future Node/Python current-output controls and bad/clean fixtures for all seven finite prohibitions. These are planned tests, not passing evidence; producer proof is mandatory before closure.
- The final control tasks correctly refer to local APIs, which triggered the broad integration/API detector. `02-COVERAGE.md` explicitly enumerates the complete local interfaces, approved offline file inputs and excluded remote services. No real integration is concealed by wording, no source is activated and the unchanged gate is rerun on the matrix.
- Final targeted checker PASS after closing the returned/logged/tracked/packaged data control gap and adding a complete requested/evaluated domain-grain inventory. Every eight-quarter national/cohort/focal cell and latest field/state/sex cell must be actually evaluated; placeholders cannot stand in for uncomputed estimates. All 30 edge truths and untuned numerical gates remain. Scope warnings for small control files were reviewed; each plan retains three tasks.
- Phase 1 canonical closure passed with no warnings. Phase 2 execution pre-hooks and wave pre-hooks are empty; UI has no frontend/block; API coverage matrix passes. Execute sequentially on the shared checkout, with scoped task commits and root-owned phase tracking.

## Execution checkpoints

- Plan 02-01 pre-handoff regression at `b6c2fcc`: **275 passed, zero skipped in 73.67 seconds**, with one expected duplicate-ZIP negative-fixture warning. The eight Node controls from Phases 1 and 2 also passed (3.65 seconds). Real all-eight adapter audit is separately required; these fixture/regression results do not establish numerical acceptance.
- Integrator readback caught unknown employment/participation/suboccupation being eligible rate denominators, income-band-known/exact-amount-unknown rows missing from income-state shares, and repeated full-frame/catalog work. Scoped corrections preserve missingness, distinguish income states from exact income amounts, reuse bounded frame masks, and validate the catalog once per snapshot. Parity and regression checks pass; the statistical formulas remain unchanged.
- Wave post-checks report no schema/codebase drift block and no frontend/UI block. The runtime hook name `ui.safety-gate` maps to canonical CLI `check ui-safety-gate 2`; the query/UI shorthand is unsupported and was corrected without waiving the gate.
- CI now discovers `phase*_prohibitions_*.test.cjs`, so subsequent phases' implemented controls join the existing Windows/Ubuntu fixture verification automatically.
- All-eight real adapter rerun passed after the lookup optimization, matching frame rows, strata, PSUs, singleton strata and member hashes. Plan 02-01 summary records the aggregate table; no individual rows are published. Numerical estimation and independent R/official acceptance remain Plans 02-02/03.
- Canonical summary verification initially misread an inline YAML file array; the executor converted it to supported multiline syntax and retained historical negative-control evidence. `verify-summary` then passed at `0f6356d` before Plan 02-02 dispatch.
- Published checkpoint `dbf5de7` ran [CI 35822127380](https://github.com/erickinorganico/career-signals-mx/actions/runs/35822127380). Both systems passed their fixture tests (274 pass, one real-cache test correctly skipped) but the overall check failed because the new license inventory linked to ignored `.venv`. That path is now plain text and the local documentation check passes. A subsequent green remote run remains required; the failed run is not reported as CI success.
- Preliminary independent metric review found M01–M03 (dictionary-hash binding, mutable manifest cache, missing explicit synthetic provenance). Fix `2936905` passed independent recheck with four targeted controls; a real 2026-Q2 smoke confirmed official dictionary binding, 407,107 frame rows and verified CMPE labels. The full regression after integration passed 289 tests with zero skips in 112.92 seconds; all ten existing Node controls passed. Final per-cell coverage audit additions and the Plan 02-02 handoff are still being completed; this is not full Phase 2 numerical acceptance.

- Plan 02-02 canonical summary verification passes. Its 69-cell real smoke is complete; no full numerical acceptance is inferred from that smoke.
- Source-catalog correction at `5d0b19a` and `1e31768`: all eight official education catalogs identify 0 as Ninguno and 99 as No sabe. Known levels 0-5 now count as other education; missing completion uses the same unknown sentinel as the adapter. Actual exclusions are distinct from observed missing-field and nationally included age-98 categories. Membership and numerical formulas are unchanged. Evidence: `02-EDUCATION-CORRECTION.md` and `docs/evidence/education-catalog-review.json`; the prior audit history remains visible.
- Final upstream regression at `1e31768`: **319 passed, zero skipped in 321.91 seconds**; the one warning is the expected duplicate-ZIP negative fixture. Ten existing Node controls passed in 7.35 seconds. The documentation link/JSON/heuristic scan passes. This test collection predates Plan 02-03's new tests and is recorded as the accepted 02-01/02-02 input checkpoint.
- Wave post schema and codebase drift checks report no block; UI reports no frontend/block; the seven-capability local API matrix passes. Wave pre hooks are empty. Typed native GSD executor is running 02-03 on the shared checkout with ownership limited to its scripts, tests, aggregate fixture and summary.
- Remote checkpoint `0868740` passed Windows and Ubuntu CI: https://github.com/erickinorganico/career-signals-mx/actions/runs/35823357094 . These are clean fixture/control runs, separate from local real-data numerical acceptance.

- First versioned all-quarter numerical attempt correctly **BLOCKED** at 2025-Q2 official reconciliation: the dynamically added ENT/CVE_ENT column was retained as text while entity masks expected integers, producing empty Baja California domains. The accepted national adapter audit and earlier 319-test suite had not exercised this actual loaded-frame state-selection path. The failed current/official receipts are preserved locally in `.cache/research/phase2-acceptance-history/2026-09-23-first-official-failure/`. A narrow adapter fix and real loaded-ZIP alias/selector regressions are required, followed by the complete final acceptance rerun. Official expected counts, tolerances and precision policy are unchanged.

- Corrective commit `c1973cc` converts both approved geographic aliases to canonical integer arrays while preserving lexical/custody evidence and leaving missing geography at -1. Loaded-ZIP regressions exercise old/new headers, padded codes, both selector forms and downstream metric masks. The 127 affected adapter/metric/estimate/population tests and two adapter prohibition controls pass. A real 2025-Q2 frame confirms a nonempty Baja California domain; its weighted official reconciliation still requires the complete acceptance rerun. The adapter source hash changes method identity, so prior-run digests are not reused as accepted results.
- Published checkpoint `0da5f60` passed both remote CI runs, including https://github.com/erickinorganico/career-signals-mx/actions/runs/35825375829 . This predates the geographic correction and is not evidence of its real numerical reconciliation.

- Corrected real rerun passed the 2025-Q2 official gate: all six national/Baja California population, PEA and unemployment totals match exactly. The two rates pass their predeclared four-decimal intervals while signed unrounded differences remain -0.000037776128897437644 and +0.000004263330166676127. The national population SE relative difference remains -0.0037148696737544095; all official_precision flags remain false. Root read the generated official ledger directly. Remaining-quarter, final-cohort R/PDF, replay and independent-review gates are still pending at this checkpoint.

- Frozen pre-review acceptance passed all eight quarters and 6,739 cells in 629.766 seconds. Entry/exit hashes matched for 11 numerical implementation files; 26 real R cases and four analytic controls passed at rtol=1e-10/atol=1e-8, and both pinned official editions passed. The ignored receipt is `.cache/research/phase2-acceptance/pre-core-review-pass.json`, numeric digest `4945b2656867e4de50319d9f8b2bfd41c681b246d7ddaaeabe36bd9824b78279`. This is evidence for the frozen pre-review implementation, not acceptance of later fixes.
- Independent upstream standard review records CR-01 (contradictory geography aliases), WR-01 (mutable Frame mappings with stale cached masks) and WR-02 (person-row control serialization/staging gaps). A typed GSD fixer owns bounded corrective changes. Integrator acceptance-script readback also requested keyed internal/public matching, explicit evaluated-cell consistency, official cell identity/duplicate checks and runtime versions before the final numerical freeze.

- Independent review and corrective rechecks now close all seven recorded findings at `86406f6`. Three earlier core issues, two complete-acceptance gaps, and two baseline immutability/canonicalization gaps remain documented in the review history. The final source review is clean. Frame inputs and returned exclusion counts cannot mutate cached state; required request domains are independently constructed; every quarter has complete public pins and explicit immutable internal hash initialization; acquisition clocks and harmless catalog/record ordering do not alter numerical identity.
- Final frozen-code regression at `86406f6`: **366 passed, zero skipped in 115.25 seconds**, with only the expected duplicate-ZIP fixture warning. All 18 Node controls passed at the prior baseline checkpoint; the three numerical controls passed again after the final baseline changes. Canonical seven-prohibition proofs are being refreshed while real acceptance runs.
- Published checkpoint `5662d8a` passed remote CI runs https://github.com/erickinorganico/career-signals-mx/actions/runs/35830202820 and https://github.com/erickinorganico/career-signals-mx/actions/runs/35830197764 . Its local evidence was 363 Python and 18 Node passes. The PR remains a draft; those checks precede the final two baseline fixes and real replay.
- The GSD stale-model warning is based only on config/oldest-agent modification times (`bin/lib/stale-bake-guard.cjs`), not a detected model override. This project uses `model_profile=inherit` without `model_overrides`; the available native spawn tool explicitly supports the Sol-medium routing required by AGENTS. No silent static-frontmatter model choice is used. The inventory's absent optional mempalace-curator role is unrelated to this research phase; required typed planner/executor/reviewer/fixer/verifier roles are available. The GSD runtime is unchanged.

- At frozen source `86406f6`, all seven canonical prohibition producers return `status=green`, `located=true`, `flagged=false`, with content-dependent `violation-fixture` proof. Receipts are `.cache/research/phase2-frozen-p1-proof.json` through `phase2-frozen-p7-proof.json`. The independent goal verifier additionally ran 30 oracle/official/integration Python tests and all 12 Phase 2 Node cases successfully; final numerical receipts are still required before its final verdict.

- Final frozen initialization at86406f6 passes all8quarters/6,739cells,26realR+4analyticcases and both official references. Attempt05dd6471-58a8-474e-8202-2a4b2a57d9fc elapsed795.094seconds; numerical digest8db575e9d664864d1513e3b9658bd9b060c4cb3bed8b51561f208adeb97b9a00. Only the absent internal hash map was initialized after checking every existing pin. All30 affected fixture tests pass after this fixture change. Unchanged replay remains running.
- Source86406f6 passed both GitHub runs: https://github.com/erickinorganico/career-signals-mx/actions/runs/35831937268 and https://github.com/erickinorganico/career-signals-mx/actions/runs/35831932614 . These Windows/Ubuntu fixture checks are independent of local R/real-data acceptance.

- Final unchanged replay passes: attempt `45fe0738-b981-4c9c-852b-28545bf4a9bf`, combined numeric digest `8db575e9d664864d1513e3b9658bd9b060c4cb3bed8b51561f208adeb97b9a00`, identical to final initialization. Every quarter's exact public payload digest, canonical public/internal content hashes, record digest and requested count match; all 11 entry/exit implementation hashes match the current source. Both R groups and both official references pass. The replay's elapsed wall time includes the prolonged interval during 2025-Q2 and is not a speed benchmark.
- The final summary passes canonical `verify-summary`; documentation/JSON checks pass. The Nyquist audit finds all six requirements and 30 edge candidates behaviorally covered, no partial/missing groups or manual-only gaps. ASVS L1 remains zero-open. Final schema/codebase drift checks report no block/action; UI reports no frontend/block; API matrix passes all seven capabilities. Independent goal verification remains the final phase-level gate.
- Published baseline checkpoint `43241d9` passes both GitHub runs: https://github.com/erickinorganico/career-signals-mx/actions/runs/35886913629 and https://github.com/erickinorganico/career-signals-mx/actions/runs/35886908696 . Final aggregate numerical evidence is tracked in `docs/evidence/phase-02-numerical-acceptance.json`; local source frames stay ignored.
