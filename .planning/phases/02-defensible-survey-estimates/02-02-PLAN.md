---
phase: 02-defensible-survey-estimates
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - brujula/survey.py
  - brujula/estimates.py
  - tests/test_survey.py
  - tests/test_estimates.py
  - tests/phase2_prohibitions_02.py
  - tests/phase2_prohibitions_02.test.cjs
  - tests/fixtures/phase2_prohibitions/02.clean.json
  - tests/fixtures/phase2_prohibitions/02-p3.bad.json
  - tests/fixtures/phase2_prohibitions/02-p4.bad.json
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-06]
must_haves:
  truths:
    - "Every metric/domain estimate uses the complete FAC_TRI/EST_D_TRI/UPM design and exposes observed n, contributing and design PSU/strata, design df, SE, IC90, CV, method and singleton policy."
    - "A supported singleton-adjusted REVIEW estimate may carry a value while official_precision remains false and the approximation remains visible."
    - "An unsupported estimate has a null public value and stable cause; its internal diagnostic cannot leak through the strict Phase 1 public projection."
    - "STAT-02 boundary: observed n=29 suppresses and n=30 can pass; one contributing UPM suppresses and two can pass; CV at 15 enters REVIEW and at 30 suppresses."
    - "STAT-02 adjacency: CV immediately below 15, exactly 15, immediately below 30 and exactly 30 produce the documented states and visibility."
    - "STAT-02 empty: absent estimate/SE and zero-support domains keep null precision and a reason; design counts are never mislabeled effective sample size."
    - "STAT-02 ordering: concurrent support failures appear in a deterministic stable reason order."
    - "STAT-02 precision: totals/means have finite normal IC90 and interior proportions finite logit-delta IC90 enclosing the point estimate."
    - "STAT-03 boundary: proportions at exactly 0 or 100 percent and SE zero suppress a false certainty with a stable cause."
    - "STAT-03 empty: a null public value always has a reason and no fallback to the internal diagnostic estimate."
    - "STAT-03 encoding: nested or serialized diagnostic sentinels cannot bypass the public projection allowlist."
    - "STAT-03 precision: suppressed estimate-equivalent weighted denominator, support total, SE, CV and IC90 limits are absent or null throughout public JSON."
    - "The request inventory evaluates every named metric/cell for all eight quarters' national context/cohort/focal-three domains and latest-quarter national identifiable fields plus 32 entity and two sex slices of the cohort and each focal field."
    - "A requested cell has a computed estimate or genuine computed suppression metadata; absence of computation cannot be represented by a fabricated null or n<30 reason."
  artifacts:
    - path: brujula/estimates.py
      provides: "Full-frame metric/domain estimates and strict v2 internal payload"
    - path: brujula/survey.py
      provides: "Contributing-stratum support and accurate suppression metadata"
    - path: tests/test_estimates.py
      provides: "Synthetic end-to-end v2 estimate and projection controls"
    - path: tests/phase2_prohibitions_02.test.cjs
      provides: "Executable p3/p4 negative controls with bad and clean subjects"
  key_links:
    - from: brujula/estimates.py
      to: brujula/enoe_adapter.py
      via: "load_snapshot_frame once per snapshot"
    - from: brujula/estimates.py
      to: brujula/research_contract.py
      via: "validate_research_v2, public_research_projection, validate_public_research_v2"
  prohibitions:
    - statement: "The project's singleton adjustment must not be described as official INEGI precision or promote an estimate to MEASURED by itself."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase2_prohibitions_02.test.cjs
      check_violation_fixture: tests/fixtures/phase2_prohibitions/02-p3.bad.json
      check_clean_fixture: tests/fixtures/phase2_prohibitions/02.clean.json
    - statement: "A suppressed or unsupported cell must not reappear as a number through weighted support, confidence interval limits, exported precision, or another public field."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase2_prohibitions_02.test.cjs
      check_violation_fixture: tests/fixtures/phase2_prohibitions/02-p4.bad.json
      check_clean_fixture: tests/fixtures/phase2_prohibitions/02.clean.json
---

<objective>
Estimate all versioned measures and selected domains with auditable design support and precision gates.
Purpose: A diagnostic number becomes an authorized public value only after the full-frame design, support and v2 contract agree.
Output: Taylor estimates, v2 internal records, validated public projection and focused gates.
</objective>

<execution_context>
@${GSD_CORE}/workflows/execute-plan.md
@${GSD_CORE}/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/phases/02-defensible-survey-estimates/02-CONTEXT.md
@.planning/phases/02-defensible-survey-estimates/02-RESEARCH.md
@.planning/phases/02-defensible-survey-estimates/02-EDGE-PROBE.json
@.planning/phases/02-defensible-survey-estimates/02-01-SUMMARY.md
@brujula/survey.py
@brujula/research_contract.py
@contracts/research-v2.schema.json
@contracts/research-v2-public.schema.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete design support and suppression semantics</name>
  <files>brujula/survey.py, tests/test_survey.py</files>
  <behavior>Design df is full-design PSU minus full-design strata; contributing PSU and strata count from denominator support for ratios. Zero-domain PSU remains in variance. n=29 suppresses, n=30 can pass; one contributing PSU suppresses, two can pass; CV just below 15 is accepted precision review only if singleton adjusted, 15 to below 30 is REVIEW with value, 30 suppresses. Zero/negative-null denominator, SE zero, boundary 0/100 proportion and undefined CV at estimate zero suppress with stable causes. Normal IC90 applies to totals and means; nondegenerate proportions use logit delta IC90.</behavior>
  <action>Extend `SurveyDesign` result to include `n_strata_domain` and full design PSU count while retaining its default singleton_policy=`fail`. Compute domain support from nonzero denominator contributions, not the numerator and not all observed rows. Keep all original strata/PSUs in Taylor variance. The ENOE caller explicitly chooses `adjust`, matching R `survey.lonely.psu="adjust"` with `survey.adjust.domain.lonely=FALSE`; label it project approximation, REVIEW, `official_precision=false`, even when otherwise supported. Preserve internal `estimate` and exact numeric SE/CV/CI, while setting `value=null` for n<30, contributing PSU<2, CV>=30, denominator zero, degenerate proportion/SE or undefined CV. Never describe observed n as effective sample size. Test exact and one-step-off thresholds, equal/touching boundaries, empty/singleton support, stable reason order and float nonfinite/overflow. Cover STAT-02/03 edge candidates.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_survey.py -q</automated></verify>
  <done>Support and precision fields match full-frame design semantics; every suppression boundary has a deterministic test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assemble strict v2 estimates and validate the only public boundary</name>
  <files>brujula/estimates.py, tests/test_estimates.py</files>
  <behavior>One snapshot frame computes the complete declared request inventory without rereading individual rows: all eight quarters' national context, professional cohort and three focal fields; the latest quarter's national all identifiable professional fields plus all 32 entity and two recorded-sex slices for cohort and each focal field separately. Every requested metric/cell is evaluated; its estimate or suppression reason derives from actual domain/support computation. Internal and public records validate with the actual Phase 1 functions. A suppressed diagnostic estimate, weighted denominator, weighted support total, SE, CV and CI are absent or null in the serialized public projection; observed n and unweighted support remain. Output record order is stable by full grain even if source row order changes. No synthetic fixture gains MEASURED status.</behavior>
  <action>Implement `required_estimation_domains(snapshot_id: str, *, latest_snapshot_id: str, field_ids: list[str]) -> list[dict]` and `estimate_snapshot(snapshot_id: str, output_root: Path, *, domains=None, registry_path=None) -> dict` using the Phase 2 adapter and metrics. Derive field_ids from actual identifiable CMPE codes in the verified loaded frame intersected with the quarter official catalog; reject an unverified caller list. The deterministic inventory requests every committed metric for each of eight quarters' national 15+ context, national professional cohort and three focal fields; for the latest quarter it additionally requests national every identifiable professional field and each of 32 states and each of two recorded-sex slices for cohort and each focal field separately. Use quarter-catalog identities and exact sex/entity codes, without treating all-fields and each named field as the same domain. Inspect the finalized Phase 1 schema and signatures before integration; `validate_research_v2(payload) -> list[dict]`, `public_research_projection(payload) -> dict`, and `validate_public_research_v2(payload) -> list[dict]` must be present and complete. Build the full `SurveyDesign` once per snapshot; reuse full-length NumPy/grouped domain masks and metric vectors across requests rather than rereading rows or changing survey formulas. Return an aggregate request/evaluation ledger keyed by exact v2 grain: each request must reach real numerator/denominator/support calculation and either an estimate or computed suppression metadata; a missing computation is an error, never a placeholder null with n<30. Map metric metadata and survey results into the exact v2 grain and required record keys; records have no `id` property. Supply source snapshot, method/design/version and dictionary evidence references, with method_version derived from adapter, metric-manifest hash and survey algorithm version so definition changes cannot appear comparable under one method. Match population IDs and source period exactly. Return internal v2 payload plus separate aggregate-only audit and validated public projection; raise on either schema/semantic failure. Do not mutate Phase 1 contract files. Add hidden-number sentinels including weighted denominator equal to estimate and confidence interval limits, nested JSON scan, and no-person-row serialization check. Cover STAT-01/03/06 and encoding/ordering edges.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_estimates.py tests/test_research_contract.py -q</automated></verify>
  <done>All requested domain/metric records pass both strict v2 validators and suppressed diagnostics cannot reappear publicly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove precision-label and suppression prohibitions</name>
  <files>tests/phase2_prohibitions_02.py, tests/phase2_prohibitions_02.test.cjs, tests/fixtures/phase2_prohibitions/02.clean.json, tests/fixtures/phase2_prohibitions/02-p3.bad.json, tests/fixtures/phase2_prohibitions/02-p4.bad.json</files>
  <behavior>Current adjusted singleton result and public projection pass named tests. p3 bad subject flips a computed REVIEW/official_precision=false result into MEASURED/official_precision=true and the named assertion goes red. p4 bad subject injects a suppressed estimate-equivalent weighted support number or CI limit into the computed public payload; the nested assertion goes red. Clean subject stays green for both.</behavior>
  <action>Create the portable Node/Python control pair using current `SurveyDesign`, `estimate_snapshot`, `validate_research_v2` and `public_research_projection` on synthetic aggregate fixtures. Resolve Python via `BRUJULA_TEST_PYTHON` or platform venv path as in Plan 01. For p3 assert the actual singleton-adjusted result remains REVIEW, explicitly nonofficial, and that the v2 semantic validator rejects an official/MEASURED mutation. For p4 compute a suppressed internal/public pair, then assert the entire serialized public payload contains no numeric estimate, weighted denominator/support, SE, CV, confidence limit or nested equivalent and that the public validator rejects an injected leak. Bad subjects mutate only computed output after the API call; clean subject does not mutate it. Run each named Node test normally and with its own bad/clean fixture; require the canonical GSD producer to machine-prove current green, targeted red, clean green and non-vacuity for both descriptors before closure. Do not claim these planned controls already pass.</action>
  <verify><automated>node --test tests/phase2_prohibitions_02.test.cjs</automated></verify>
  <done>Both prohibitions have actual result assertions and content-dependent red/clean controls enforceable by the GSD producer.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Complete-frame local arrays become internal diagnostic records, then a strict public aggregate projection.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-05 | Tampering | domain masks | mitigate | Full-length mask and zero-domain-PSU tests; reference one complete design. |
| T-02-06 | Repudiation | precision result | mitigate | Record method/version, df, support, singleton policy and explicit nonofficial precision. |
| T-02-07 | Information disclosure | v2 projection | mitigate | Phase 1 allowlist, strict public validator and nested numeric sentinel scan. |
| T-02-08 | Denial of service | domain expansion | mitigate | Load/design each snapshot once; vectorized masks; explicit bounded domain selections. |
| T-02-SC | Tampering | package install | accept | This plan makes no package install. |
</threat_model>

<verification>
Run focused survey/v2 tests, the two Node prohibition controls and full pytest. Require canonical prohibition-enforcement results with `located:true`, proven bad red, clean green and `flagged:false` for both descriptors before closure. The eight real snapshots remain an integration gate in Plan 03; synthetic fixtures do not establish real acceptance.
</verification>

<success_criteria>
STAT-01/02/03/06 produce correct design-based v2 records with explicit precision and fail-closed public suppression.
</success_criteria>

<artifacts_created>
Artifacts this phase produces in this plan: `brujula/survey.py` preserves `SurveyDesign(weights, strata, psu, *, allow_zero_weights=False, singleton_policy="fail")`, `total(values, domain=None)` and `ratio(numerator, denominator, domain=None, *, percent=False)` while adding `n_psu_design` and `n_strata_domain` to each result. `brujula/estimates.py` exports `required_estimation_domains(snapshot_id: str, *, latest_snapshot_id: str, field_ids: list[str]) -> list[dict]` and `estimate_snapshot(snapshot_id: str, output_root: Path, *, domains: list[dict] | None = None, registry_path: Path | None = None) -> dict` returning `internal`, `public`, and aggregate-only `audit` with `requested_cells` and `evaluated_cells` keyed by exact grain; inventory omissions raise. The internal v2 records use the exact Phase 1 grain (no record `id`) and fields `sample_size`, `weighted_denominator`, `support`, `precision`, `estimate`, `value`, `reason`, `method_version`, `design_id`, and `evidence_refs`; public records come only from `public_research_projection` and pass `validate_public_research_v2`.
`tests/phase2_prohibitions_02.test.cjs` and `.py` implement p3/p4 over computed survey/v2 payloads; `tests/fixtures/phase2_prohibitions/02.clean.json`, `02-p3.bad.json` and `02-p4.bad.json` are the projected GSD producer's clean and violation subjects.
</artifacts_created>

<output>Create `.planning/phases/02-defensible-survey-estimates/02-02-SUMMARY.md` when done.</output>
