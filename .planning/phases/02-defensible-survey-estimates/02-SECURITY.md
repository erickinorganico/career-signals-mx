---
phase: 2
slug: defensible-survey-estimates
status: verified
threats_open: 0
asvs_level: 1
register_authored_at_plan_time: true
created: 2026-09-22
---

# Phase 2 — Security verification

L1 verification of the authored Plan 02-01/02/03 threat registers. The configured blocking threshold is high. All 16 authored entries have verified code mitigations at `86406f6`; this security result does not substitute for final numerical replay, Phase 2 goal verification or release acceptance. No authentication/session surface exists in this local batch phase.

## Trust boundaries

Pinned source ZIP/member bytes enter a complete local person frame. Full-design metric vectors become internal estimates and a strict public projection. Pinned official workbook/PDF and independent R outputs become aggregate numerical acceptance evidence. Person records remain ignored/local and cannot enter published artifacts.

## Authored register

The plans did not assign severity; integrity/disclosure threats are conservatively high, resource exhaustion medium, and unchanged dependency acceptance low. Repeated SC IDs are qualified by plan.

| Plan / threat | Component | Severity | Disposition | Implementation and check evidence | Status |
|---|---|---|---|---|---|
| 01 / T-02-01 | ZIP/current/member identity | high | mitigate | `resolve_snapshot`, `inventory_snapshot`, adapter binding; failed-current, member/hash and geography-alias tests | closed |
| 01 / T-02-02 | SDEM codes and weights | high | mitigate | ASCII/U+0020 parser, official dictionary/CMPE membership, positive finite FAC_TRI; malformed/sentinel/overflow tests | closed |
| 01 / T-02-03 | Person-row disclosure | high | mitigate | Aggregate adapter audit, captured stdout/stderr/logs and staged/packaged output; original control plus five independent prefixed/multiline/repr/CSV/untracked-package negatives; independent WR-02 recheck passes | closed |
| 01 / T-02-04 | Large-frame resource use | medium | mitigate | One streaming read into aligned columns, vector masks and bounded per-frame domain cache; eight-quarter run completed before review fixes | closed |
| 01 / T-02-SC | NumPy dependency | low | mitigate | Direct pinned NumPy 2.5.3 in pyproject; existing verified runtime, no new install in this execution | closed |
| 02 / T-02-05 | Domain masks | high | mitigate | Complete-design/zero-domain PSU and alias checks; frame-owned read-only arrays, fresh per-frame cache and copied returned exclusions; caller mutation regressions and independent recheck pass | closed |
| 02 / T-02-06 | Precision provenance | high | mitigate | Method/version, support, design df, singleton policy and explicit official_precision=false; survey and current-output p3 controls | closed |
| 02 / T-02-07 | Suppressed diagnostics | high | mitigate | Strict central public projection and schema, nested sentinel checks, current-output p4 negative control; no estimate fallback | closed |
| 02 / T-02-08 | Domain expansion resource use | medium | mitigate | One frame/design per snapshot, vectorized masks, bounded explicit request domains; real 6,739-cell execution | closed |
| 02 / T-02-SC | Dependencies | low | accept | No package installation in this plan; final license/runtime audit remains Phase 5 | closed |
| 03 / T-02-09 | Official source identity | high | mitigate | Exact source/workbook/PDF hashes, asserted workbook headers/cells and edition, strict CLI v2 identity and duplicate/period/universe negative checks | closed |
| 03 / T-02-10 | Oracle/fixture tampering | high | mitigate | Predeclared R tolerances, independent required-domain inventory, complete public and hash-only internal pins; existing internal map cannot be overwritten; clocks/order excluded while identity/numeric mutations block; independent final recheck and 30 affected tests pass | closed |
| 03 / T-02-11 | Acceptance attribution | high | mitigate | Entry/exit implementation hashes, source/dictionary/method/runtime metadata, immutable per-attempt receipts and blocked-current wrapper | closed |
| 03 / T-02-12 | Oracle/output disclosure | high | mitigate | Ignored temporary person CSV removed in finally; R writes aggregate-only cases; acceptance stores aggregate ledgers/public records; output controls inspect structural person keys | closed |
| 03 / T-02-13 | Offline replay resource use | medium | mitigate | Eight snapshots processed sequentially, one frame load per snapshot, bounded R subprocess timeout, controlled output paths | closed |
| 03 / T-02-SC | Dependencies | low | accept | Existing local R 4.6.1/survey 4.5 and Python dependencies; no package installation in this plan | closed |

## Accepted risks and limits

The two no-install SC dispositions retain only their plan-time scope. They do not replace final dependency/license review. Singleton-adjusted precision remains explicitly nonofficial REVIEW; matching R does not establish official INEGI precision. The public suppression rule is a precision boundary, not a claim of confidentiality or protection against arbitrary reader algebra.

## Audit trail

Initial root source/fixture inspection recorded 16 authored entries, 14 closed and two open. Independent core and acceptance reviews preserved the concrete findings. After corrective commits through `86406f6`, all 16 are closed. Canonical `02-REVIEW.md` is clean; full local regression passes 366 tests with no skips (115.25 seconds), and the unchanged Node suite has 18 passing controls, with affected numerical controls rechecked after the baseline changes.

The active GSD `verify:post` hook enables `secure-phase`, ASVS level 1, and a high blocking threshold. The authored-register, zero-open-threat L1 short circuit applies; source/test inspection supplies the required depth, so no redundant security-agent scan is needed. No integrity or disclosure gap is accepted as risk. Final real numerical initialization/replay and independent goal verification remain separate pending gates.
