---
phase: 02-defensible-survey-estimates
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - scripts/enoe_survey_oracle.R
  - scripts/official_reconciliation.py
  - scripts/accept_enoe_estimates.py
  - tests/test_survey_oracle.py
  - tests/test_official_reconciliation.py
  - tests/test_enoe_integration.py
  - data/fixtures/enoe-aggregate-golden.json
  - tests/phase2_prohibitions_03.py
  - tests/phase2_prohibitions_03.test.cjs
  - tests/fixtures/phase2_prohibitions/03.clean.json
  - tests/fixtures/phase2_prohibitions/03-p5.bad.json
  - tests/fixtures/phase2_prohibitions/03-p6.bad.json
  - tests/fixtures/phase2_prohibitions/03-p7.bad.json
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04, STAT-05, STAT-06]
must_haves:
  truths:
    - "An independent R survey oracle reproduces Python point estimates and SE on final-population real cases and analytic controls with predeclared tolerances."
    - "The compatible official 2025-Q2 national/Baja California cells reconcile exact counts and separately rounded rates while raw, untuned SE discrepancies remain visible."
    - "All eight approved quarters yield strict internal and public v2 aggregates, with explicit unavailable/suppressed cells and aggregate-only evidence."
    - "STAT-04 boundary: point and SE differences at and just outside predeclared rtol=1e-10/atol=1e-8 produce distinct oracle verdicts."
    - "STAT-04 adjacency: final professional cohort includes known age 97 and excludes unknown age 98 in 2026-Q2 focal-field oracle cases."
    - "STAT-04 empty: a missing or duplicate oracle case fails acceptance rather than passing a partial comparison."
    - "STAT-04 ordering: permuting case order leaves keyed comparison identities and verdicts unchanged."
    - "STAT-04 precision: signed point and SE errors are retained before tolerance comparison; a nonfinite value fails."
    - "STAT-05 boundary: six compatible official totals require exact equality while rates use the official four-decimal rounding interval."
    - "STAT-05 adjacency: rates at a permitted rounding endpoint and just outside it receive different official reconciliation verdicts."
    - "STAT-05 empty: absent official cell, source hash or workbook hash fails the comparison with an explicit reason."
    - "STAT-05 ordering: keyed national/Baja California comparison is unchanged by workbook row order."
    - "STAT-05 precision: every compatible official SE retains untuned signed/relative difference, including documented nonzero national population discrepancy."
    - "Eight-snapshot acceptance verifies the full requested-domain inventory: each quarter's national context/cohort/focal-three metrics and latest-quarter all identifiable fields, 32 entities and two sex slices for cohort plus each focal field; requested and evaluated grain sets match exactly."
  artifacts:
    - path: scripts/enoe_survey_oracle.R
      provides: "Versioned independent survey calculation"
    - path: scripts/official_reconciliation.py
      provides: "Pinned official point/rate/precision ledger"
    - path: scripts/accept_enoe_estimates.py
      provides: "Offline eight-snapshot acceptance entry point"
    - path: data/fixtures/enoe-aggregate-golden.json
      provides: "Aggregate-only pinned expected values and provenance"
    - path: tests/phase2_prohibitions_03.test.cjs
      provides: "Executable p5/p6/p7 negative controls with bad and clean subjects"
  key_links:
    - from: scripts/accept_enoe_estimates.py
      to: brujula/estimates.py
      via: "estimate_snapshot for eight approved snapshot IDs"
    - from: scripts/enoe_survey_oracle.R
      to: scripts/accept_enoe_estimates.py
      via: "independent R survey point/SE ledger, no raw-row output"
    - from: scripts/official_reconciliation.py
      to: data/fixtures/enoe-aggregate-golden.json
      via: "pinned 2025-Q2 workbook and source hashes"
  prohibitions:
    - statement: "A matching Python/R project approximation must not be presented as official precision or used to erase an untuned official SE discrepancy."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase2_prohibitions_03.test.cjs
      check_violation_fixture: tests/fixtures/phase2_prohibitions/03-p5.bad.json
      check_clean_fixture: tests/fixtures/phase2_prohibitions/03.clean.json
    - statement: "Eight quarterly samples must not be summed as distinct people or treated as independent evidence of temporal significance."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase2_prohibitions_03.test.cjs
      check_violation_fixture: tests/fixtures/phase2_prohibitions/03-p6.bad.json
      check_clean_fixture: tests/fixtures/phase2_prohibitions/03.clean.json
    - statement: "A failed source or numerical acceptance attempt must not reuse an older success as the current accepted result."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/phase2_prohibitions_03.test.cjs
      check_violation_fixture: tests/fixtures/phase2_prohibitions/03-p7.bad.json
      check_clean_fixture: tests/fixtures/phase2_prohibitions/03.clean.json
---

<objective>
Independently accept numerical and source compatibility for all eight real ENOE estimates.
Purpose: A passing fixture or a prototype comparison cannot establish final-cohort, official or all-period validity.
Output: Versioned R oracle, official reconciliation, aggregate golden fixtures and offline eight-period acceptance command.
</objective>

<execution_context>
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/erick/OneDrive/Documentos/Snowplow/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CONTRACT.md
@docs/CONTRACT-V2.md
@.planning/phases/02-defensible-survey-estimates/02-CONTEXT.md
@.planning/phases/02-defensible-survey-estimates/02-RESEARCH.md
@.planning/phases/02-defensible-survey-estimates/02-VALIDATION.md
@.planning/phases/02-defensible-survey-estimates/02-EDGE-PROBE.json
@.planning/phases/02-defensible-survey-estimates/02-01-SUMMARY.md
@.planning/phases/02-defensible-survey-estimates/02-02-SUMMARY.md
@data/catalog/enoe-snapshots.json
@brujula/estimates.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Rebuild final-cohort independent R oracle</name>
  <files>scripts/enoe_survey_oracle.R, tests/test_survey_oracle.py, data/fixtures/enoe-aggregate-golden.json</files>
  <behavior>Analytic controls and 2026-Q2 national, Derecho, Comunicación y periodismo, Ciencias políticas and Baja California cases compare totals, rates and positive-known nominal income points/SE with rtol=1e-10 and atol=1e-8. Derecho uses EDA 15..97 and approved CMPE catalog, not the prototype's age-98 cohort. Both signed absolute and relative errors remain in an aggregate ledger. Reordering cases does not change identity or verdict; absent/duplicate case or nonfinite result fails.</behavior>
  <action>Promote the proven ignored `.cache/research/oracle-adjust.R` logic into the versioned script, but regenerate all expected aggregates using the Phase 1 final cohort and Phase 2 masks. In R set `survey.lonely.psu="adjust"`, `survey.adjust.domain.lonely=FALSE`; use complete valid respondent/resident design and `svytotal`/`svyratio` with zero-contribution domain PSUs retained. Record R/survey versions, source hash, design df, cohort/metric IDs and singleton policy. Python test invokes local R only in the explicit offline oracle gate; ordinary tests use committed aggregate golden cases and analytic fixture controls. Declare point and SE tolerances before calculation as rtol=1e-10/atol=1e-8; reject any failure and preserve signed raw differences. Do not commit or print person rows, `.cache/research/oracle-frame.csv`, or any row-level fixture. Cover STAT-04 boundary/adjacency/empty/ordering/precision candidates.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_survey_oracle.py -q</automated></verify>
  <done>Versioned R oracle and aggregate golden evidence reproduce final cohort across required real and analytic cases at original tolerances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile compatible official points, rates and precision</name>
  <files>scripts/official_reconciliation.py, tests/test_official_reconciliation.py, data/fixtures/enoe-aggregate-golden.json</files>
  <behavior>The pinned 2025-Q2 workbook and ZIP yield exact six national/Baja California population, PEA and unemployed counts. Official four-decimal rates pass their rounding intervals separately, with raw signed rate errors retained. Every compatible SE comparison records signed/relative difference, including a nonzero national population difference near -0.0037148697; no official_precision=true claim or tuned SE threshold. Wrong workbook/source hash, edition, universe, geography or missing cell fails.</behavior>
  <action>Promote the ignored official precision probe into a versioned aggregate-only script with Phase 1 resolver/inventory custody and workbook SHA-256 gate (`.cache/research/precision_2025q2.xlsx`, expected full digest from approved research evidence). Parse bounded, asserted workbook headers, national and Baja California edition/universe cells. Check six totals with exact numeric equality and rates against the official four-decimal half-unit interval; preserve the original strict raw comparison result too. Compare available official SEs as an explicit, untuned signed and relative discrepancy ledger, label the project Taylor singleton adjustment approximate REVIEW and `official_precision=false`; do not invent official collapsed strata. Include the approved 2026-Q2 national PDF benchmark and any compatible 2024-Q3/2025-Q3 official cross-alias edition only after hash, measure/universe and rounding compatibility checks. If one is incompatible, record a reason without converting it to a passing comparison. Cover STAT-05 boundary/adjacency/empty/ordering/precision candidates.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_official_reconciliation.py -q</automated></verify>
  <done>Exact official counts, separately rounded rates and raw precision differences are reproducible and explicitly nonofficial where they differ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute the eight-snapshot acceptance gate</name>
  <files>scripts/accept_enoe_estimates.py, tests/test_enoe_integration.py, tests/phase2_prohibitions_03.py, tests/phase2_prohibitions_03.test.cjs, tests/fixtures/phase2_prohibitions/03.clean.json, tests/fixtures/phase2_prohibitions/03-p5.bad.json, tests/fixtures/phase2_prohibitions/03-p6.bad.json, tests/fixtures/phase2_prohibitions/03-p7.bad.json</files>
  <behavior>Exactly eight approved IDs run in catalog order; each source/current/member hash and dictionary succeeds; internal/public v2 validation succeeds; audit contains only aggregate counts and method/precision metadata. The acceptance inventory contains every named metric in national context/cohort/focal-three domains for all eight quarters, latest national all identifiable fields, and latest 32 entity plus two recorded-sex slices for cohort and each focal field. Every requested grain has computed output or genuine support/suppression metadata; a missing request, missing computation, fabricated null or n<30 without calculation, missing snapshot, failed latest attempt, wrong period, duplicate grain, suppressed numeric leak or changed dictionary stops the gate. Golden aggregate changes require explicit evidence update. Repeated run on unchanged snapshots produces identical numeric content independent of timestamps.</behavior>
  <action>Create an offline entry point that invokes `inventory_all`, `required_estimation_domains`, and the adapter/estimator once for each approved 2024-Q3..2026-Q2 snapshot, strict internal/public v2 validators, final-cohort R oracle and official reconciliation. Build the explicit expected request-grain set from the catalog: all eight quarters national context/professional cohort/focal-three, latest national all identifiable professional fields, and latest all 32 entity and both recorded-sex slices separately for cohort and each focal field. Compare expected requests to `requested_cells`, `evaluated_cells` and actual internal/public record grains; fail on missing, duplicate or unevaluated cells, and verify every public null is backed by a real computed support/precision cause. Reuse the one full frame and grouped NumPy masks per snapshot without changing formulas. Do not access the network. Write only aggregate-only ledgers under an ignored local output directory and an explicit manifest of inputs, code/method versions and acceptance verdict; no individual records or public release promotion. Include per-quarter raw lexeme/category/exclusion counts, full design support, metric coverage/suppression causes, and source/dictionary hashes; assert the corrected CLASE2 occupied, SUB_O×CLASE2 and DUR9C×HRSOCUP invariant against prior aggregate audit. Require test fixture path for ordinary unit tests; keep real eight-ZIP run as a named offline integration command executed during Phase 2 verification. Compare numeric content on replay, not run timestamps. Cover STAT-01–06 and the empty/ordering/precision edge backstops. In the same task, add portable `tests/phase2_prohibitions_03.test.cjs` plus Python runner using actual generated aggregate ledgers: p5 asserts official_precision=false and preserves the nonzero 2025-Q2 signed SE difference despite Python/R agreement, p6 asserts no distinct-person sum or independent-quarter significance field, p7 runs success then a failed-current or numerical-failure attempt in a disposable root and asserts acceptance is blocked instead of reusing prior success. Use `BRUJULA_TEST_PYTHON` or platform venv path. Each bad JSON subject mutates a computed ledger/result only after the API or script call; shared clean subject leaves it intact. Require normal/clean Node green, each named bad control red, and canonical GSD producer proof for all three descriptors. These are planned controls, not current green evidence.</action>
  <verify><automated>.venv/Scripts/python.exe -m pytest tests/test_enoe_integration.py -q</automated><automated>node --test tests/phase2_prohibitions_03.test.cjs</automated></verify>
  <done>All eight verified snapshots pass a reproducible offline aggregate gate, or a specific failure prevents acceptance without promoting stale results.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Ignored local microdata and official workbooks enter independent numeric tests; only aggregate ledgers may leave the local research boundary.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-09 | Spoofing | official workbook/PDF | mitigate | Pin SHA-256, edition, universe, geography and table headers before reconciliation. |
| T-02-10 | Tampering | oracle fixtures/tolerance | mitigate | Predeclared rtol/atol and checked aggregate golden identities; retain signed raw discrepancies. |
| T-02-11 | Repudiation | acceptance ledger | mitigate | Record exact source/method/R versions, per-case errors and failure reasons. |
| T-02-12 | Information disclosure | oracle/integration output | mitigate | Aggregate-only output schema and no-person-row fixture/log scan. |
| T-02-13 | Denial of service | eight-snapshot replay | mitigate | One full-frame load per snapshot, bounded sequential processing and controlled ignored output. |
| T-02-SC | Tampering | package install | accept | No new Python or R package install is required for this plan. |
</threat_model>

<verification>
Run focused tests including `tests/test_enoe_integration.py`, all three Node prohibition controls, full `.venv/Scripts/python.exe -m pytest -q`, and the real offline eight-snapshot acceptance entry point with R and official pinned sources. Capture actual commands, exit codes and aggregate ledger paths. For all seven Phase 2 descriptors require canonical GSD producer `located:true`, non-vacuous current pass, machine-proven bad red, clean green and `flagged:false`; any missing control blocks canonical completion. Verify that only aggregate fixtures/scripts are tracked. Independent reviewer checks masks, official edition compatibility and the unchanged SE discrepancy before Phase 2 is marked complete.
</verification>

<success_criteria>
STAT-01–06 pass the full final-cohort oracle, compatible official reconciliation and all-eight strict-v2 gate; any failed numerical or custody gate is explicit and blocks phase acceptance.
</success_criteria>

<artifacts_created>
Artifacts this phase produces in this plan: `scripts/enoe_survey_oracle.R` accepts `--frame` (ignored local person-frame path), `--cases` (aggregate case specification) and `--output` (aggregate-only signed point/SE ledger); it writes no person rows. `scripts/official_reconciliation.py` accepts `--output-root` (approved acquisition root), `--workbook` (pinned local official workbook) and `--ledger` (aggregate-only comparison JSON). `scripts/accept_enoe_estimates.py` accepts `--output-root artifacts/enoe` and `--audit-dir .cache/research/phase2-acceptance`; its ignored output holds eight keyed aggregate audit/acceptance ledgers and a source/method/version manifest. `data/fixtures/enoe-aggregate-golden.json` holds source/workbook hashes, keyed expected aggregate cases, official rounding units and signed discrepancy controls; it contains no person records.
`tests/phase2_prohibitions_03.test.cjs` and `.py` execute p5/p6/p7 against actual oracle/official/acceptance output; `tests/fixtures/phase2_prohibitions/03.clean.json`, `03-p5.bad.json`, `03-p6.bad.json`, and `03-p7.bad.json` provide the producer's content-dependent clean/violation controls.
</artifacts_created>

<output>Create `.planning/phases/02-defensible-survey-estimates/02-03-SUMMARY.md` when done.</output>
